{
 "title: DeepEval 5-min Quickstart": "title: DeepEval 5 分钟快速上手",
 "This quickstart takes you from installing DeepEval to your first passing eval in a few": "本快速上手将带你在几分钟内从安装 DeepEval 走到第一次通过的评估。你将创建一个小的测试用例、选择一个指标，并用 `deepeval test run` 运行它。",
 "By the end of this quickstart, you should be able to:": "读完本快速上手，你应当能够：",
 "- Run your first local eval with a test case, metric, and `deepeval test run`.": "- 用测试用例、指标和 `deepeval test run` 运行你的第一次本地评估。",
 "- Evaluate an AI agent's complete trajectory and diagnose its internal components with tracing.": "- 评估 AI 智能体的完整轨迹，并借助追踪诊断其内部组件。",
 "- Know where to go next for datasets, integrations, and the Confident AI": "- 知道接下来去哪里了解数据集、集成与 Confident AI",
 "platform.": "  平台。",
 "New to DeepEval? Checkout the [introduction](/docs/introduction) to learn more about this framework.": "初次接触 DeepEval？阅读[简介](/docs/introduction)进一步了解这个框架。",
 "**Prefer to have your coding agent do this for you?**": "**更想让编码智能体替你完成？**",
 "This page walks you through setting up DeepEval **by hand**. If you'd rather install a skill in **Cursor, Claude Code, Codex, Windsurf**, or any other AI coding tool — and have your coding agent write the test suite, run `deepeval test run`, and iterate on failures for you — start at the **[5-min Vibe Coder Quickstart →](/docs/vibe-coder-quickstart)** instead.": "本页带你**亲手**完成 DeepEval 的安装配置。如果你更想在 **Cursor、Claude Code、Codex、Windsurf** 或任何其他 AI 编码工具中安装一个 skill——让编码智能体替你编写测试套件、运行 `deepeval test run` 并针对失败进行迭代——请直接从 **[5 分钟 Vibe Coder 快速上手 →](/docs/vibe-coder-quickstart)** 开始。",
 "## Installation": "## 安装",
 "In a newly created virtual environment, run:": "在新建的虚拟环境中运行：",
 "`deepeval` plugs into Pytest, so `deepeval test run` collects and runs your eval files the same way `pytest` would.": "`deepeval` 插入 Pytest，因此 `deepeval test run` 会像 `pytest` 一样收集并运行你的评估文件。",
 "In your project, run:": "在你的项目中运行：",
 "The package ships a `deepeval` binary, so every command below runs through `npx`. `deepeval` plugs into [Vitest](https://vitest.dev), so `npx deepeval test run` collects and runs your eval files the same way `vitest` would.": "该包自带 `deepeval` 可执行文件，因此下面所有命令都通过 `npx` 运行。`deepeval` 插入 [Vitest](https://vitest.dev)，因此 `npx deepeval test run` 会像 `vitest` 一样收集并运行你的评估文件。",
 "`deepeval` runs evaluations locally on your environment. To keep your testing reports in a centralized place on the cloud, use [Confident AI](https://www.confident-ai.com), an AI quality platform with observability, evals, and monitoring that DeepEval integrates with natively:": "`deepeval` 在你的环境中本地运行评估。要把测试报告集中保存到云端，请使用 [Confident AI](https://www.confident-ai.com)——一个 DeepEval 与之原生集成、具备可观测性、评估与监控能力的 AI 质量平台：",
 "Your browser handles authentication only. After you sign in or create an": "浏览器只负责认证。在你登录或创建账号后，回到终端输入你的姓名与组织，确认预填的第一个项目名称，或选择一个现有项目。DeepEval 会自动创建并保存专属的项目 API key。对于 CI 或其他非交互环境，用 `deepeval login --api-key ...` 传入已有密钥。",
 "DeepEval autoloads environment files (at import time)": "DeepEval 会自动加载环境文件（在导入时）",
 "- **Precedence:** existing process env -> `.env.local` -> `.env`": "- **优先级：**现有进程环境变量 -> `.env.local` -> `.env`",
 "- **Opt-out:** set `DEEPEVAL_DISABLE_DOTENV=1`": "- **退出加载：**设置 `DEEPEVAL_DISABLE_DOTENV=1`",
 "More information on `env` settings can be [found here.](/docs/evaluation-flags-and-configs#environment-flags)": "关于 `env` 设置的更多信息见[这里](/docs/evaluation-flags-and-configs#environment-flags)。",
 "Confident AI is free and allows you to keep all evaluation results on the cloud. Sign up [here.](https://app.confident-ai.com)": "Confident AI 免费，并允许你把所有评估结果保存在云端。在[这里](https://app.confident-ai.com)注册。",
 "## Create Your First Test Run": "## 创建你的第一次测试运行",
 "Create a test file to run your first **end-to-end evaluation**.": "创建一个测试文件，来运行你的第一次**端到端评估**。",
 "An [LLM test case](/docs/evaluation-test-cases#llm-test-case) in `deepeval` represents a **single unit of LLM app interaction**, and contains mandatory fields such as the `input` and `actual_output` (LLM generated output), and optional ones like `expected_output`.": "`deepeval` 中的 [LLM 测试用例](/docs/evaluation-test-cases#llm-test-case)表示**单次 LLM 应用交互单元**，包含 `input` 和 `actual_output`（LLM 生成的输出）等必填字段，以及 `expected_output` 等可选字段。",
 "![LLM Test Case](https://deepeval-docs.s3.amazonaws.com/docs:llm-test-case.png)": "![LLM 测试用例](https://deepeval-docs.s3.amazonaws.com/docs:llm-test-case.png)",
 "Run `touch test_example.py`（TypeScript 中为 `touch example.test.ts`） in your terminal and paste in the following code:": "在终端运行 `touch test_example.py`（TypeScript 中为 `touch example.test.ts`），并粘贴以下代码：",
 "Importing `deepeval/vitest` registers the `toPass()` matcher. `npx deepeval test run` injects it for you, so the import only matters when you run the same file with `vitest` directly.": "导入 `deepeval/vitest` 会注册 `toPass()` 匹配器。`npx deepeval test run` 会自动注入它，因此只有当你直接用 `vitest` 运行同一文件时这个导入才有意义。",
 "Then, run `deepeval test run` from the root directory of your project to evaluate your LLM app **end-to-end**:": "然后，在项目根目录运行 `deepeval test run`，对 LLM 应用做**端到端**评估：",
 "Congratulations! Your test case should have passed ✅ Let's breakdown what happened.": "恭喜！你的测试用例应该已经通过 ✅ 我们来分解一下发生了什么。",
 "- The variable `input` mimics a user input, and `actual_output` is a placeholder for what your application's supposed to output based on this input.": "- 变量 `input` 模拟用户输入，`actual_output` 是你的应用基于该输入应当输出的内容的占位。",
 "- The variable `expected_output` represents the ideal answer for a given `input`, and [`GEval`](/docs/metrics-llm-evals) is a research-backed metric provided by `deepeval` for you to evaluate your LLM output's on any custom metric with human-like accuracy.": "- 变量 `expected_output` 表示给定 `input` 的理想答案；[`GEval`](/docs/metrics-llm-evals) 是 `deepeval` 提供的、有研究支撑的指标，让你以类人准确度按任意自定义标准评估 LLM 输出。",
 "- In this example, the metric `criteria` is correctness of the `actual_output` based on the provided `expected_output`, but not all metrics require an `expected_output`.": "- 在本例中，指标 `criteria` 是基于给定 `expected_output` 的 `actual_output` 正确性，但并非所有指标都需要 `expected_output`。",
 "- All metric scores range from 0 - 1, which the `threshold=0.5`（TypeScript 中为 `threshold: 0.5`） threshold ultimately determines if your test have passed or not.": "- 所有指标分数范围都是 0 - 1，`threshold=0.5`（TypeScript 中为 `threshold: 0.5`）阈值最终决定你的测试是否通过。",
 "If you run more than one test run, you will be able to **catch regressions** by comparing test cases side-by-side. This is also made easier if you're using `deepeval` alongside Confident AI ([see below](/docs/getting-started#save-results) for video demo).": "如果你运行了不止一次测试运行，就能通过并排比较测试用例来**发现回归**。如果把 `deepeval` 与 Confident AI 搭配使用会更容易（[见下文](/docs/getting-started#save-results)视频演示）。",
 "A [conversational test case](/docs/evaluation-multiturn-test-cases#conversational-test-case) in `deepeval` represents a **multi-turn interaction with your LLM app**, and contains information such as the actual conversation that took place in the format of `turn`s, and optionally the scenario of which a conversation happened.": "`deepeval` 中的[对话式测试用例](/docs/evaluation-multiturn-test-cases#conversational-test-case)表示**与 LLM 应用的一次多轮交互**，包含以 `turn` 形式组织的实际对话内容，以及可选的对话场景。",
 "![Conversational Test Case](https://deepeval-docs.s3.amazonaws.com/docs:conversational-test-case.png)": "![对话式测试用例](https://deepeval-docs.s3.amazonaws.com/docs:conversational-test-case.png)",
 "🎉 Congratulations! Your test case should have passed ✅ Let's breakdown what happened.": "🎉 恭喜！你的测试用例应该已经通过 ✅ 我们来分解一下发生了什么。",
 "- The variable `role` distinguishes between the end user and your LLM application, and `content` contains either the user’s input or the LLM’s output.": "- 变量 `role` 区分最终用户与你的 LLM 应用，`content` 包含用户输入或 LLM 输出。",
 "- In this example, the `criteria` metric evaluates the professionalism of the sequence of `content`.": "- 在本例中，`criteria` 指标评估 `content` 序列的专业度。",
 "Since almost all `deepeval` metrics including `GEval` are LLM-as-a-Judge metrics, you'll need to set your `OPENAI_API_KEY` as an env variable. You can also customize the model used for evals:": "由于包括 `GEval` 在内的几乎所有 `deepeval` 指标都是 LLM-as-a-Judge 指标，你需要把 `OPENAI_API_KEY` 设为环境变量。你也可以自定义评估使用的模型：",
 "DeepEval also integrates with these model providers: [Ollama](https://deepeval.com/integrations/models/ollama), [Azure OpenAI](https://deepeval.com/integrations/models/azure-openai), [Anthropic](https://deepeval.com/integrations/models/anthropic), [Gemini](https://deepeval.com/integrations/models/gemini), etc.": "DeepEval 还与这些模型提供商集成：[Ollama](https://deepeval.com/integrations/models/ollama)、[Azure OpenAI](https://deepeval.com/integrations/models/azure-openai)、[Anthropic](https://deepeval.com/integrations/models/anthropic)、[Gemini](https://deepeval.com/integrations/models/gemini) 等。",
 "To use **ANY** custom LLM of your choice, [check out this part of the": "要使用**任意**你选择的自定义 LLM，请参阅[文档的这一部分](/guides/guides-using-custom-llms)。",
 "Most likely your evaluation LLM is failing and this might be due to rate limits or insufficient quotas.": "最可能是你的评估 LLM 出了问题，原因可能是速率限制或配额不足。",
 "By default, `deepeval` retries **transient** LLM errors once (2 attempts total):": "默认情况下，`deepeval` 会对**瞬时** LLM 错误重试一次（共尝试 2 次）：",
 "- **Retried:** network/timeout errors and **5xx** server errors.": "- **会重试：**网络/超时错误与 **5xx** 服务器错误。",
 "- **Rate limits (429):** retried unless the provider marks them non-retryable": "- **速率限制（429）：**会重试，除非提供商标记为不可重试",
 "(for OpenAI, `insufficient_quota` is treated as non-retryable).": "  （对 OpenAI 而言，`insufficient_quota` 被视为不可重试）。",
 "- **Backoff:** exponential with jitter (initial **1s**, base **2**, jitter **2s**, cap **5s**).": "- **退避：**带抖动的指数退避（初始 **1s**，基数 **2**，抖动 **2s**，上限 **5s**）。",
 "You can tune these via environment flags (no code changes). See [environment variables](/docs/environment-variables) for details.": "你可以通过环境开关调整这些行为（无需改代码）。详见[环境变量](/docs/environment-variables)。",
 "`deepeval` doesn't add its own retry layer around LLM calls — retries are handled by each provider's SDK, so configure them on the client you pass in:": "`deepeval` 不会在 LLM 调用外再包一层重试——重试由各提供商的 SDK 处理，因此请在你传入的客户端上配置：",
 "See [environment variables](/docs/environment-variables) for the rest of the knobs.": "其余可调项见[环境变量](/docs/environment-variables)。",
 "### Save Results": "### 保存结果",
 "It is recommended that you push your test runs to Confident AI — an AI quality platform `deepeval` integrates with natively for observability, evals, and monitoring.": "建议你把测试运行推送到 Confident AI——一个 `deepeval` 与之原生集成、面向可观测性、评估与监控的 AI 质量平台。",
 "Confident AI is an AI quality platform with observability, evals, and monitoring that `deepeval` integrates with natively, and helps you build the best LLM evals pipeline.": "Confident AI 是一个具备可观测性、评估与监控能力的 AI 质量平台，`deepeval` 与之原生集成，帮助你构建最好的 LLM 评估流水线。",
 "Run `deepeval view` to view your newly ran test run on the platform:": "运行 `deepeval view` 在平台上查看你刚运行的测试运行：",
 "The `deepeval view` command requires that the test run that you ran above has been successfully cached locally. If something errors, simply run a new test run after logging in with `deepeval login`:": "`deepeval view` 命令要求你上面运行的测试运行已成功缓存在本地。如果出错，用 `deepeval login` 登录后重新运行一次测试运行即可：",
 "Once that's set up, Confident AI will **generate testing reports and automate regression testing** whenever you run a test run to evaluate your LLM application inside any environment, at any scale, anywhere.": "设置完成后，无论你在何种环境、何种规模、何地运行测试运行来评估你的 LLM 应用，Confident AI 都会**生成测试报告并自动化回归测试**。",
 "**Once you've run more than one test run**, you'll be able to use the [regression testing page](https://www.confident-ai.com/docs/llm-evaluation/dashboards/ab-regression-testing) shown near the end of the video. Green rows indicate that your LLM has shown improvement on specific test cases, whereas red rows highlight areas of regression.": "**当你运行了不止一次测试运行**，你就能使用视频末尾附近展示的[回归测试页面](https://www.confident-ai.com/docs/llm-evaluation/dashboards/ab-regression-testing)。绿色行表示你的 LLM 在特定测试用例上有改进，红色行则突出回归之处。",
 "Simply set the `DEEPEVAL_RESULTS_FOLDER` environment variable to your relative path of choice.": "只需把 `DEEPEVAL_RESULTS_FOLDER` 环境变量设置为你选择的相对路径。",
 "## Evaluate an AI Agent": "## 评估 AI 智能体",
 "AI agents often take many steps before producing a result. To evaluate whether an agent completed its task and how it got there, first instrument it with [LLM tracing](/docs/evaluation-llm-tracing), then score its complete trajectory.": "AI 智能体在产出结果之前往往要经历许多步骤。要评估智能体是否完成了任务以及它是如何完成的，先用 [LLM 追踪](/docs/evaluation-llm-tracing)对它做埋点，再对它的完整轨迹打分。",
 "### Build Dataset": "### 构建数据集",
 "Create a small dataset of representative tasks for your agent:": "为你的智能体创建一个由代表性任务组成的小数据集：",
 "### Instrument and Evaluate Trajectory": "### 埋点并评估轨迹",
 "Each example instruments the agent, captures one complete trace per golden, and applies `TaskCompletionMetric` to the full trajectory.": "每个示例都会对智能体做埋点，为每个金标（golden）捕获一条完整追踪，并把 `TaskCompletionMetric` 应用到整条轨迹。",
 "Pick your stack below, paste the snippet, and run it. Every integration ships an **Async** sample (the default — runs goldens concurrently) and a **Sync** sample (one golden at a time, useful for debugging or rate-limited providers):": "在下面选择你的技术栈，粘贴代码片段并运行。每个集成都提供 **Async** 示例（默认——并发运行金标）和 **Sync** 示例（一次一个金标，适合调试或受速率限制的提供商）：",
 "Paste the snippet below and run it — goldens are evaluated concurrently as your app produces their traces:": "粘贴下面的代码片段并运行——当你的应用产出追踪时，金标会被并发评估：",
 "Wrap the top-level function with `@observe` and call `update_current_trace(...)` to set the trace-level test case fields:": "用 `@observe` 包装顶层函数，并调用 `update_current_trace(...)` 设置追踪级测试用例字段：",
 "See [tracing](/docs/evaluation-llm-tracing) for the full `@observe` and `update_current_trace` surface.": "完整的 `@observe` 与 `update_current_trace` 用法见[追踪](/docs/evaluation-llm-tracing)。",
 "Build your agent with `create_agent`, then pass `deepeval`'s `CallbackHandler` to its `invoke` / `ainvoke` method inside the loop:": "用 `create_agent` 构建你的智能体，然后在循环中把 `deepeval` 的 `CallbackHandler` 传给它的 `invoke` / `ainvoke` 方法：",
 "See the [LangChain integration](/integrations/frameworks/langchain) for the full surface.": "完整用法见 [LangChain 集成](/integrations/frameworks/langchain)。",
 "Wire your `StateGraph`, then pass `deepeval`'s `CallbackHandler` to its `invoke` / `ainvoke` method inside the loop:": "接好你的 `StateGraph`，然后在循环中把 `deepeval` 的 `CallbackHandler` 传给它的 `invoke` / `ainvoke` 方法：",
 "See the [LangGraph integration](/integrations/frameworks/langgraph) for the full surface.": "完整用法见 [LangGraph 集成](/integrations/frameworks/langgraph)。",
 "Drop-in replace `from openai import OpenAI` with `from deepeval.openai import OpenAI` (or `AsyncOpenAI`). Wrap the call in `with trace():` so the LLM call becomes a trace:": "把 `from openai import OpenAI` 直接替换为 `from deepeval.openai import OpenAI`（或 `AsyncOpenAI`）。把调用包在 `with trace():` 中，LLM 调用就会成为追踪：",
 "See the [OpenAI integration](/integrations/frameworks/openai) for streaming and tool-calling.": "流式与工具调用相关内容见 [OpenAI 集成](/integrations/frameworks/openai)。",
 "Pass `DeepEvalInstrumentationSettings()` to your `Agent`'s `instrument` keyword:": "把 `DeepEvalInstrumentationSettings()` 传给你 `Agent` 的 `instrument` 关键字参数：",
 "See the [Pydantic AI integration](/integrations/frameworks/pydanticai) for the full surface.": "完整用法见 [Pydantic AI 集成](/integrations/frameworks/pydanticai)。",
 "Call `instrument_agentcore()` before creating your agent. The same call also instruments [Strands](https://strandsagents.com/) agents running inside AgentCore:": "在创建智能体之前调用 `instrument_agentcore()`。同一次调用也会对运行在 AgentCore 内的 [Strands](https://strandsagents.com/) 智能体做埋点：",
 "See the [AgentCore integration](/integrations/frameworks/agentcore) for the full surface (including the `BedrockAgentCoreApp` entrypoint pattern).": "完整用法（包括 `BedrockAgentCoreApp` 入口模式）见 [AgentCore 集成](/integrations/frameworks/agentcore)。",
 "Call `instrument_strands()` before invoking your Strands agent (for AgentCore-hosted Strands, use the AgentCore tab instead):": "在调用你的 Strands 智能体之前调用 `instrument_strands()`（对托管在 AgentCore 上的 Strands，请改用 AgentCore 标签页）：",
 "See the [Strands integration](/integrations/frameworks/strands) for the full surface.": "完整用法见 [Strands 集成](/integrations/frameworks/strands)。",
 "Drop-in replace `from anthropic import Anthropic` with `from deepeval.anthropic import Anthropic` (or `AsyncAnthropic`). Wrap the call in `with trace():` so the LLM call becomes a trace:": "把 `from anthropic import Anthropic` 直接替换为 `from deepeval.anthropic import Anthropic`（或 `AsyncAnthropic`）。把调用包在 `with trace():` 中，LLM 调用就会成为追踪：",
 "See the [Anthropic integration](/integrations/frameworks/anthropic) for streaming and tool-use.": "流式与工具使用相关内容见 [Anthropic 集成](/integrations/frameworks/anthropic)。",
 "Register `deepeval`'s event handler against LlamaIndex's instrumentation dispatcher. `agent.run(...)` is async-only, so the sync variant uses `asyncio.run(...)`:": "把 `deepeval` 的事件处理器注册到 LlamaIndex 的 instrumentation 调度器上。`agent.run(...)` 仅支持异步，同步变体使用 `asyncio.run(...)`：",
 "See the [LlamaIndex integration](/integrations/frameworks/llamaindex) for the full surface.": "完整用法见 [LlamaIndex 集成](/integrations/frameworks/llamaindex)。",
 "Register `DeepEvalTracingProcessor` once, then build your agent with `deepeval`'s `Agent` and `function_tool` shims:": "注册一次 `DeepEvalTracingProcessor`，然后用 `deepeval` 的 `Agent` 与 `function_tool` 垫片构建你的智能体：",
 "See the [OpenAI Agents integration](/integrations/frameworks/openai-agents) for the full surface.": "完整用法见 [OpenAI Agents 集成](/integrations/frameworks/openai-agents)。",
 "Call `instrument_google_adk()` once before building your `LlmAgent`. ADK's `runner.run_async(...)` is async-only, so the sync variant uses `asyncio.run(...)`:": "在构建 `LlmAgent` 之前调用一次 `instrument_google_adk()`。ADK 的 `runner.run_async(...)` 仅支持异步，同步变体使用 `asyncio.run(...)`：",
 "See the [Google ADK integration](/integrations/frameworks/google-adk) for the full surface.": "完整用法见 [Google ADK 集成](/integrations/frameworks/google-adk)。",
 "Call `instrument_crewai()` once, then build your crew with `deepeval`'s `Crew`, `Agent`, and `@tool` shims:": "调用一次 `instrument_crewai()`，然后用 `deepeval` 的 `Crew`、`Agent` 与 `@tool` 垫片构建你的 crew：",
 "See the [CrewAI integration](/integrations/frameworks/crewai) for the full surface.": "完整用法见 [CrewAI 集成](/integrations/frameworks/crewai)。",
 "`evalsIterator()` is async-only, so there's no sync variant to choose between — goldens are evaluated concurrently and awaited by the loop.": "`evalsIterator()` 仅支持异步，因此没有同步变体可选——金标被并发评估，由循环等待完成。",
 "Wrap the top-level function with `observe` and call `updateCurrentTrace(...)` to set the trace-level test case fields:": "用 `observe` 包装顶层函数，并调用 `updateCurrentTrace(...)` 设置追踪级测试用例字段：",
 "See [tracing](/docs/evaluation-llm-tracing) for the full `observe` and `updateCurrentTrace` surface.": "完整的 `observe` 与 `updateCurrentTrace` 用法见[追踪](/docs/evaluation-llm-tracing)。",
 "Build your agent with `createAgent`, then pass `deepeval`'s `DeepEvalCallbackHandler` to its `invoke` method inside the loop:": "用 `createAgent` 构建你的智能体，然后在循环中把 `deepeval` 的 `DeepEvalCallbackHandler` 传给它的 `invoke` 方法：",
 "Register a `DeepEvalExporter` on your `Mastra` instance's `Observability` config, then run your goldens through the agent:": "在你的 `Mastra` 实例的 `Observability` 配置上注册一个 `DeepEvalExporter`，然后让金标流经该智能体：",
 "`evalsIterator()` waits for the exporter to settle before scoring, so no manual flush is needed inside an eval. See the [Mastra integration](/integrations/frameworks/mastra) for the full surface.": "`evalsIterator()` 在打分前会等待 exporter 落定，因此无需在评估内手动 flush。完整用法见 [Mastra 集成](/integrations/frameworks/mastra)。",
 "Wire your `StateGraph`, then pass the same `DeepEvalCallbackHandler` to its `invoke` method inside the loop:": "接好你的 `StateGraph`，然后在循环中把同一个 `DeepEvalCallbackHandler` 传给它的 `invoke` 方法：",
 "Call `instrumentOpenAI(client)` once on the client you already construct — every completion or response call it makes becomes an LLM span under the trace:": "对你已有的 client 调用一次 `instrumentOpenAI(client)`——它发出的每次补全或响应调用都会成为追踪下的一个 LLM span：",
 "See the [OpenAI integration](/integrations/frameworks/openai) for the full surface.": "完整用法见 [OpenAI 集成](/integrations/frameworks/openai)。",
 "Register `DeepEvalTracingProcessor` once with the agents SDK, then run your goldens through the agent:": "向 agents SDK 注册一次 `DeepEvalTracingProcessor`，然后让金标流经该智能体：",
 "Call `configureAiSdkTracing(...)` once at startup, then pass the returned tracer into `experimental_telemetry` on every call you want traced:": "启动时调用一次 `configureAiSdkTracing(...)`，然后把返回的 tracer 传入每次想追踪的调用的 `experimental_telemetry`：",
 "A call without `isEnabled` and a `tracer` emits no spans at all. See the [Vercel AI SDK integration](/integrations/frameworks/ai-sdk) for the full surface.": "没有 `isEnabled` 和 `tracer` 的调用不会产生任何 span。完整用法见 [Vercel AI SDK 集成](/integrations/frameworks/ai-sdk)。",
 "Then run the file:": "然后运行该文件：",
 "🎉 Congratulations! Your eval should have run ✅ A quick recap of what happened:": "🎉 恭喜！你的评估应该已经运行 ✅ 快速回顾一下发生了什么：",
 "- `evals_iterator()` looped through your dataset, capturing one trace per golden.": "- `evals_iterator()` 遍历你的数据集，为每个金标捕获一条追踪。",
 "- <Tabs><Tab title=\"Python\">Your integration's adapter (or `@observe`) created spans</Tab><Tab title=\"TypeScript\">Your integration (or `observe`)</Tab></Tabs> recorded the agent's internal steps.": "- <Tabs><Tab title=\"Python\">你集成的适配器（或 `@observe`）创建了 span</Tab><Tab title=\"TypeScript\">你的集成（或 `observe`）</Tab></Tabs>记录了智能体的内部步骤。",
 "- `TaskCompletionMetric` analyzed the complete ordered trace after the agent finished.": "- `TaskCompletionMetric` 在智能体结束后分析完整的有序追踪。",
 "- DeepEval aggregated everything into one test run.": "- DeepEval 把一切汇总为一个测试运行。",
 "### Evaluate Individual Components": "### 评估单个组件",
 "Trajectory-based evaluation tells you whether the complete path was effective. [Component-level evaluation](/docs/evaluation-component-level-llm-evals) uses the same trace to score one span at a time, helping you identify which planner, retriever, tool call, LLM generation, or sub-agent caused a failure.": "基于轨迹的评估告诉你完整路径是否有效。[组件级评估](/docs/evaluation-component-level-llm-evals)用同一条追踪逐个对 span 打分，帮你定位是哪个规划器、检索器、工具调用、LLM 生成或子智能体造成了失败。",
 "![component-level metrics attached to individual spans within an agent trace](https://deepeval-docs.s3.us-east-1.amazonaws.com/component-level-evals.png)": "![智能体追踪中挂在单个 span 上的组件级指标](https://deepeval-docs.s3.us-east-1.amazonaws.com/component-level-evals.png)",
 "Once your agent is traced, attach component metrics only to the spans you need to diagnose. Trajectory and component scores can run together in the same test run. See the [component-level guide](/docs/evaluation-component-level-llm-evals) for stack-specific examples, sub-agent evaluation, retriever scoring, and span customization.": "智能体被追踪后，只把组件指标挂到你需要诊断的 span 上。轨迹分数与组件分数可以在同一次测试运行中一起产出。针对具体技术栈的示例、子智能体评估、检索器打分与 span 自定义，见[组件级指南](/docs/evaluation-component-level-llm-evals)。",
 "To run the trajectory eval inside your test suite, invoke the traced agent from the assertion and pass the trajectory metric. Any component metrics attached to its spans run during the same test:": "要在测试套件中运行轨迹评估，在断言中调用被追踪的智能体并传入轨迹指标。挂在其 span 上的组件指标会在同一次测试中运行：",
 "Either way, run it with `deepeval test run` like any other test file.": "无论哪种方式，都用 `deepeval test run` 像运行其他测试文件一样运行它。",
 "Every `evals_iterator()` run is snapshotted to disk, so you can open it in a trace-tree TUI — with trajectory scores, per-span scores, and metric reasons — by running bare `deepeval inspect`. See the [`deepeval inspect` reference](/docs/command-line-interface#inspect) for full details.": "每次 `evals_iterator()` 运行都会快照到磁盘，因此运行 `deepeval inspect` 即可在追踪树 TUI 中查看它——包含轨迹分数、每个 span 的分数与指标原因。完整细节见 [`deepeval inspect` 参考](/docs/command-line-interface#inspect)。",
 "![deepeval inspect TUI showing a trace tree with per-span scores and metric reasons](https://deepeval-docs.s3.us-east-1.amazonaws.com/docs:tui-trace-inspect.png)": "![deepeval inspect TUI 显示带每个 Span 分数与指标原因的追踪树](https://deepeval-docs.s3.us-east-1.amazonaws.com/docs:tui-trace-inspect.png)",
 "## DeepEval for Online Evals": "## DeepEval 在线评估",
 "When you do LLM tracing using `deepeval`, you can automatically run online evals to monitor **traces, spans, and threads (conversations) in production**.": "使用 `deepeval` 做 LLM 追踪时，你可以自动运行在线评估，监控**生产环境中的追踪、Span 与线程（对话）**。",
 "You'll need to use Confident AI to provide the necessary backend infrastructure and dashboard for this.": "你需要使用 Confident AI 来提供所需的后端基础设施与仪表盘。",
 "Simply get an [API key from Confident AI](https://app.confident-ai.com) and set it in the CLI:": "只需从 Confident AI 获取一个 [API key](https://app.confident-ai.com)，并在 CLI 中设置：",
 "Then add a \"metric collection\" to your trace:": "然后向你的追踪添加一个「指标集合」（metric collection）：",
 "✅ Done. All invocations of your AI agent will now have online evals ran on it.": "✅ 完成。你的 AI 智能体的所有调用现在都会运行在线评估。",
 "To learn more on what a \"metric collection\" is, and how to pair observability with online evals, checkout the [docs on Confident AI.](https://www.confident-ai.com/docs/llm-tracing/quickstart)": "想进一步了解什么是「指标集合」，以及如何把可观测性与在线评估配对，请查看 [Confident AI 文档](https://www.confident-ai.com/docs/llm-tracing/quickstart)。",
 "`deepeval`'s LLM tracing implementation is **non-instrusive**, meaning it will not affect any part of your code.": "`deepeval` 的 LLM 追踪实现是**非侵入的**，不会影响你代码的任何部分。",
 "Trace-level evals can score the observable result with [end-to-end evaluation](/docs/evaluation-end-to-end-llm-evals) or analyze an agent's complete internal path with [trajectory-based evaluation](/docs/evaluation-trajectory-based-llm-evals).": "追踪级评估可以用[端到端评估](/docs/evaluation-end-to-end-llm-evals)给可观察结果打分，也可以用[基于轨迹的评估](/docs/evaluation-trajectory-based-llm-evals)分析智能体完整的内部路径。",
 "Spans make up a trace and evals on spans represents [component-level evaluations](/docs/evaluation-component-level-llm-evals), where individual components in your LLM app are being evaluated.": "Span 组成追踪，对 span 的评估即[组件级评估](/docs/evaluation-component-level-llm-evals)，评估的是 LLM 应用中的各个组件。",
 "Threads are made up of **one or more traces**, and represents a multi-turn interaction to be evaluated.": "线程由**一条或多条追踪**组成，表示一次待评估的多轮交互。",
 "## Framework Integrations": "## 框架集成",
 "Already building with an agent framework? DeepEval integrations capture its traces and spans so the same metrics can evaluate complete trajectories and individual components with minimal instrumentation.": "已经在用智能体框架？DeepEval 的集成会捕获框架的追踪与 Span，让同一批指标以最少的埋点评估完整轨迹与单个组件。",
 "Here are a few examples available for your selected SDK:": "以下是你所选 SDK 可用的一些示例：",
 "description=\"Evaluate complete agent runs, model calls, tools, and retrievers.\"": "        description=\"评估完整的智能体运行、模型调用、工具与检索器。\"",
 "description=\"Evaluate Pydantic AI agents and their instrumented spans.\"": "        description=\"评估 Pydantic AI 智能体及其被埋点的 span。\"",
 "description=\"Evaluate AgentCore-hosted agents and their nested spans.\"": "        description=\"评估托管在 AgentCore 上的智能体及其嵌套 span。\"",
 "description=\"Evaluate ADK agent runs, tools, and model calls.\"": "        description=\"评估 ADK 智能体运行、工具与模型调用。\"",
 "description=\"Evaluate Mastra agents and their OpenTelemetry spans.\"": "        description=\"评估 Mastra 智能体及其 OpenTelemetry span。\"",
 "description=\"Evaluate AI SDK generations, tools, and telemetry spans.\"": "        description=\"评估 AI SDK 的生成、工具与遥测 span。\"",
 "description=\"Evaluate graph trajectories and the nodes inside each run.\"": "        description=\"评估图轨迹以及每次运行内的节点。\"",
 "[Browse all framework integrations →](/integrations)": "[浏览全部框架集成 →](/integrations)",
 "## Next Steps": "## 下一步",
 "- Learn the core concepts if you want to build a repeatable eval suite:": "- 如果想构建可复现的评估套件，先学习核心概念：",
 "- [Test cases](/docs/evaluation-test-cases)": "  - [测试用例](/docs/evaluation-test-cases)",
 "- [Metrics](/docs/metrics-introduction)": "  - [指标](/docs/metrics-introduction)",
 "- [Datasets](/docs/evaluation-datasets)": "  - [数据集](/docs/evaluation-datasets)",
 "- Follow a use-case quickstart if you want a path tailored to your system:": "- 如果想要针对你的系统的路径，跟随场景快速上手：",
 "- [AI agents](/docs/getting-started-agents)": "  - [AI 智能体](/docs/getting-started-agents)",
 "- [Chatbots](/docs/getting-started-chatbots)": "  - [聊天机器人](/docs/getting-started-chatbots)",
 "- Explore other workflows when you're ready to go beyond a single eval:": "- 如果准备好超越单个评估，探索其他工作流：",
 "- [Generate synthetic data](/docs/synthesizer-introduction)": "  - [生成合成数据](/docs/synthesizer-introduction)",
 "- [Simulate conversations](/docs/conversation-simulator)": "  - [模拟对话](/docs/conversation-simulator)",
 "- [Use integrations](/integrations) with LangChain, LangGraph, OpenAI, CrewAI, and more": "  - [使用集成](/integrations)：LangChain、LangGraph、OpenAI、CrewAI 等更多",
 "If your team needs shared reports, regression analysis, or production monitoring,": "如果你的团队需要共享报告、回归分析或生产监控，",
 "DeepEval integrates natively with [Confident AI](https://www.confident-ai.com/docs).": "DeepEval 与 [Confident AI](https://www.confident-ai.com/docs) 原生集成。",
 "Most LLM-as-a-judge metrics call an evaluation model. If the provider is rate-limited, out of quota, or slow to respond, the eval may appear stuck. Check your model provider key, quota, and network access.": "大多数 LLM-as-a-judge 指标会调用一个评估模型。如果提供商被限速、配额不足或响应缓慢，评估可能看起来卡住了。请检查你的模型提供商密钥、配额与网络访问。",
 "No. DeepEval runs locally. Confident AI is optional and useful when you want shared reports, regression tracking, observability, or production monitoring.": "不需要。DeepEval 在本地运行。Confident AI 是可选的，当你想要共享报告、回归追踪、可观测性或生产监控时才有用。",
 "Put it anywhere your test runner can discover it, usually alongside your app or in a `tests/` folder. Then run `deepeval test run path/to/test_file.py`（TypeScript 中为 `npx deepeval test run path/to/file.test.ts`） .": "放在你的测试运行器能发现的地方即可，通常与你的应用放在一起，或放在 `tests/` 文件夹里。然后运行 `deepeval test run path/to/test_file.py`（TypeScript 中为 `npx deepeval test run path/to/file.test.ts`）。",
 "Yes. DeepEval supports multiple model providers and custom/local models for evaluation. OpenAI is only the quickest default path for many examples.": "可以。DeepEval 支持多种模型提供商以及自定义/本地模型做评估。OpenAI 只是许多示例中最快的默认路径。",
 "If you're evaluating an agent, start with tracing. If you're building a repeatable eval suite, start with datasets and metrics.": "如果你在评估智能体，从追踪开始。如果想要构建可复现的评估套件，从数据集与指标开始。",
 "You can find the full example [here on our Github](https://github.com/confident-ai/deepeval/blob/main/examples/getting_started/test_example.py).": "你可以在我们的 [GitHub 上找到完整示例](https://github.com/confident-ai/deepeval/blob/main/examples/getting_started/test_example.py)。",
 "title: Vibe Coder 5-min Quickstart": "title: Vibe Coder 5 分钟快速上手",
 "This page sets your coding agent (Cursor, Claude Code, Codex, Windsurf, OpenCode, …) up to drive a real DeepEval loop on your repo — install the skill, point it at our LLM-friendly docs, paste the starter prompt, and you're off.": "本页把你的编码智能体（Cursor、Claude Code、Codex、Windsurf、OpenCode 等）配置为在你的仓库上驱动真实的 DeepEval 循环——安装 skill、把它指向我们对 LLM 友好的文档、粘贴起始提示词，就可以开始了。",
 "If you want to understand the loop _before_ wiring it up, read [Vibe Coding with DeepEval](/docs/vibe-coding) first.": "如果你想_先_了解这个循环再动手接线，请先阅读[用 DeepEval 进行 Vibe Coding](/docs/vibe-coding)。",
 "## Install the Agent Skill": "## 安装智能体 Skill",
 "The [`deepeval` Agent Skill](https://github.com/confident-ai/deepeval/tree/main/skills/deepeval) teaches your coding assistant how to pick the right test shape (single-turn / multi-turn / component-level), reuse or generate goldens, write a committed `tests/evals/` suite with `pytest`（TypeScript 中为 `vitest`）, run `deepeval test run`, read failures, and iterate.": "[`deepeval` 智能体 Skill](https://github.com/confident-ai/deepeval/tree/main/skills/deepeval)会教你的编码助手如何选择正确的测试形态（单轮 / 多轮 / 组件级）、复用或生成金标（golden）、用 `pytest`（TypeScript 中为 `vitest`）编写纳入版本管理的 `tests/evals/` 套件、运行 `deepeval test run`、解读失败并迭代。",
 "Install with any [Skills](https://github.com/anthropics/skills)-compatible installer:": "使用任何兼容 [Skills](https://github.com/anthropics/skills) 标准的安装器安装：",
 "Works with Claude Code, Codex, Cursor, Windsurf, OpenCode, and any other assistant that supports the Skills standard.": "适用于 Claude Code、Codex、Cursor、Windsurf、OpenCode 以及任何支持 Skills 标准的助手。",
 "Copy or symlink [`skills/deepeval`](https://github.com/confident-ai/deepeval/tree/main/skills/deepeval) into your agent's skills directory.": "把 [`skills/deepeval`](https://github.com/confident-ai/deepeval/tree/main/skills/deepeval) 复制或软链接到你智能体的 skills 目录。",
 "A first-class **Cursor plugin** for DeepEval is coming soon — it'll let Cursor discover the `deepeval` skill (and future ones) automatically without going through the skills CLI. Until then, use the skills CLI install above.": "一个一等公民的 DeepEval **Cursor 插件**即将推出——它会让 Cursor 自动发现 `deepeval` skill（以及未来的 skill），无需经过 skills CLI。在那之前，请使用上面的 skills CLI 安装方式。",
 "The skill triggers automatically on prompts like _\"eval the refund agent and fix any regressions\"_, _\"add evals to this repo\"_, or _\"why is faithfulness dropping?\"_ — you don't need to invoke it explicitly.": "当提示词形如 _\"eval the refund agent and fix any regressions\"_、_\"add evals to this repo\"_ 或 _\"why is faithfulness dropping?\"_ 时，skill 会自动触发——你无需显式调用它。",
 "## LLM-Friendly Docs": "## 对 LLM 友好的文档",
 "Every page in these docs is reachable in a form your coding agent can ingest directly:": "这些文档的每一页都有你的编码智能体可以直接摄取的形式：",
 "- [llms.txt](https://www.deepeval.com/llms.txt) — index of every page (per the [llms.txt standard](https://llmstxt.org/))": "- [llms.txt](https://www.deepeval.com/llms.txt) —— 每一页的索引（遵循 [llms.txt 标准](https://llmstxt.org/)）",
 "- [llms-full.txt](https://www.deepeval.com/llms-full.txt) — every page concatenated into one document": "- [llms-full.txt](https://www.deepeval.com/llms-full.txt) —— 所有页面拼接成一个文档",
 "- Append `.md` (or `/content.md`) to any docs URL for the raw markdown of that page only — useful when you want to feed your assistant one specific concept (e.g. [Faithfulness](https://www.deepeval.com/docs/metrics-faithfulness.md)) instead of the whole site": "- 在任何文档 URL 后追加 `.md`（或 `/content.md`）即可获得该页单独的原始 markdown——当你只想把某个具体概念（例如 [Faithfulness](https://www.deepeval.com/docs/metrics-faithfulness.md)）喂给助手而不是整个站点时很有用",
 "## Universal Starter Prompt": "## 通用起始提示词",
 "Paste this into Cursor, Claude Code, Codex, or any other AI tool to bootstrap the loop:": "把下面的内容粘贴到 Cursor、Claude Code、Codex 或任何其他 AI 工具中，即可启动这个循环：",
 "With the [Agent Skill](#install-the-agent-skill) installed, you can shorten the prompt to _\"Use DeepEval to fix the refund agent — run 5 rounds of the iteration loop\"_. The skill carries the workflow, the templates, and the guardrails.": "装好 [Agent Skill](#install-the-agent-skill) 后，你可以把提示词缩短为 _\"Use DeepEval to fix the refund agent — run 5 rounds of the iteration loop\"_。skill 自带工作流、模板与护栏。",
 "## Connect to Confident AI (optional)": "## 连接 Confident AI（可选）",
 "DeepEval is local-first, so the loop above works fully offline. Connecting to [Confident AI](https://www.confident-ai.com) extends the loop across your team:": "DeepEval 是本地优先的，因此上面的循环完全离线可用。连接 [Confident AI](https://www.confident-ai.com) 可以把这个循环扩展到整个团队：",
 "Every `deepeval test run` your agent kicks off pushes a testing report your reviewers can open with `deepeval view`. Production monitoring sends new failure cases straight back into the dataset, so the next iteration round picks up real regressions automatically.": "你的智能体发起的每次 `deepeval test run` 都会推送一份测试报告，评审者可以用 `deepeval view` 打开。生产监控会把新的失败用例直接送回数据集，让下一轮迭代自动纳入真实的回归。",
 "You've got the install — if you want to understand what's actually running when your coding agent calls `deepeval test run`, the loop walkthrough breaks it down stage by stage.": "安装已经就绪——如果你想了解你的编码智能体调用 `deepeval test run` 时究竟发生了什么，循环详解会逐阶段拆解。",
 "title=\"Vibe Coding with DeepEval\"": "    title=\"用 DeepEval 进行 Vibe Coding\"",
 "description=\"The loop diagram, what runs under the hood, and how to prompt your coding agent to drive it.\"": "    description=\"循环图解、底层运行机制，以及如何提示你的编码智能体来驱动它。\"",
 "title=\"CLI Reference\"": "    title=\"CLI 参考\"",
 "description=\"Every flag your coding agent reaches for: `deepeval generate`, `deepeval test run`, `deepeval view`.\"": "    description=\"你的编码智能体会用到的每一个开关：`deepeval generate`、`deepeval test run`、`deepeval view`。\"",
 "title: Vibe Coding with DeepEval": "title: 用 DeepEval 进行 Vibe Coding",
 "Although DeepEval is great as an AI quality validation suite — `pytest`（TypeScript 中为 `vitest`） assertions, regression gates, CI/CD failure tracking — that's only half the use case.": "虽然 DeepEval 作为 AI 质量验证套件非常出色——`pytest`（TypeScript 中为 `vitest`） 断言、回归把关、CI/CD 失败追踪——但这只是它用例的一半。",
 "The other half is using the same evals **during development**: your coding agent runs them, reads the failing metrics and traces, and uses the results to decide what to change next in your agent, RAG pipeline, or chatbot. Then re-runs to confirm.": "另一半是在**开发过程中**使用同一批评估：你的编码智能体运行它们、读取失败的指标与追踪，并利用结果决定接下来对你的智能体、RAG 流水线或聊天机器人改什么。然后再重跑确认。",
 "In short: **DeepEval helps you vibe code your agent without vibe coding your agents.**": "简而言之：**DeepEval 帮你 vibe code 你的智能体，而不让你的智能体真的凭感觉乱来。**",
 "If you just want to install the skill and paste the starter prompt into Cursor / Claude Code / Codex, jump to the [5-min Vibe Coder Quickstart](/docs/vibe-coder-quickstart). The rest of this page is the loop itself — what actually runs, why it works, and how to drive it.": "如果你只想安装 skill 并把起始提示词粘贴到 Cursor / Claude Code / Codex，直接跳到 [5 分钟 Vibe Coder 快速上手](/docs/vibe-coder-quickstart)。本页其余部分就是这个循环本身——实际运行的是什么、为什么有效、如何驱动它。",
 "## The Loop": "## 这个循环",
 "Vibe coding with DeepEval is a feedback loop between your eval suite and your coding agent:": "用 DeepEval 做 vibe coding，是在你的评估套件与编码智能体之间建立一个反馈循环：",
 "1. Define a dataset, or let DeepEval generate one from your docs, traces, or existing examples.": "1. 定义一个数据集，或让 DeepEval 从你的文档、追踪或现有示例生成一个。",
 "2. Add an eval suite that calls your agent against that dataset and scores the outputs with the metrics you care about.": "2. 添加一个评估套件，让智能体针对该数据集运行，并用你关心的指标对输出打分。",
 "3. Let your coding agent run the suite, read the failures, and make targeted changes to the relevant prompts, retrieval logic, tools, or application code.": "3. 让你的编码智能体运行该套件、读取失败项，并对相关的提示词、检索逻辑、工具或应用代码做针对性修改。",
 "4. Re-run the same evals until the scores and metric reasons show that the behavior has improved.": "4. 重跑同样的评估，直到分数与指标原因表明行为已经改善。",
 "A trace from `deepeval test run` gives the coding agent more than a pass/fail result. It includes scores, span-level context, and metric reasons, so a failure can be traced back to the part of the system that produced it.": "`deepeval test run` 产生的追踪给编码智能体的不只是一个通过/失败结果。它包含分数、span 级上下文与指标原因，因此失败可以回溯到产生它的系统部分。",
 "For example, if a run reports `faithfulness 0.64`, the agent can open the retriever span that produced the off-source claim, narrow retrieval to active refund policies, and re-run the eval to confirm the fix. The workflow is similar to a tight unit-test cycle, except the assertions are scored model outputs and the runner is your coding agent.": "例如，一次运行报告 `faithfulness 0.64`，智能体可以打开产生偏离原文断言的检索器 span，把检索收窄到有效的退款政策，然后重跑评估确认修复。这个工作流类似紧凑的单元测试循环，只不过断言是带打分的模型输出，运行者是你的编码智能体。",
 "## Under the Hood": "## 幕后机制",
 "When the [Agent Skill](/docs/vibe-coder-quickstart#install-the-agent-skill) is installed and you say _\"add evals to this repo and fix the failing ones\"_, your coding agent doesn't invent an evaluation framework — it shells out to DeepEval's CLI. Concretely, every iteration round walks through these stages, each backed by a single CLI command documented in the [CLI reference](/docs/command-line-interface):": "当 [Agent Skill](/docs/vibe-coder-quickstart#install-the-agent-skill) 已安装而你说 _\"add evals to this repo and fix the failing ones\"_ 时，你的编码智能体不会发明一套评估框架——它会转而调用 DeepEval 的 CLI。具体来说，每一轮迭代都会走这几个阶段，每个阶段都由 [CLI 参考](/docs/command-line-interface)中记录的一条命令支撑：",
 "### 1. Load (or generate) the dataset": "### 1. 加载（或生成）数据集",
 "The agent first looks for an existing dataset under `tests/evals/`, on Confident AI, or as a Hugging Face dataset.": "智能体首先在 `tests/evals/`、Confident AI 或 Hugging Face 数据集中寻找现有数据集。",
 "If none exists, it generates one with [`deepeval generate`](/docs/command-line-interface#generate). That single command synthesizes goldens from your docs, contexts, scratch, or existing goldens — single-turn or multi-turn — without any custom Python:": "如果不存在，它会用 [`deepeval generate`](/docs/command-line-interface#generate) 生成一个。这一条命令就能从你的文档、上下文、空白或已有金标（golden）合成金标——单轮或多轮——无需任何自定义 Python：",
 "The generated `.dataset.json` is committed to the repo. Future runs reuse it; new edge cases append to it.": "生成的 `.dataset.json` 会提交进仓库。后续运行复用它；新的边界场景追加进去。",
 "Until it lands, commit a `.dataset.json` of goldens you write by hand and load it with `addGoldensFromJSON()`, or pull a dataset from Confident AI. Either way the rest of the loop is unchanged — it only needs goldens on disk.": "在它落地之前，提交一份手写的 `.dataset.json` 金标文件并用 `addGoldensFromJSON()` 加载，或者从 Confident AI 拉取数据集。无论哪种方式，循环的其余部分不变——它只需要磁盘上有金标。",
 "### 2. Build the eval suite": "### 2. 构建评估套件",
 "The skill ships [pytest templates](https://github.com/confident-ai/deepeval/tree/main/skills/deepeval/templates) for the four common shapes — single-turn end-to-end, multi-turn end-to-end, single-turn component-level, plus a shared `conftest.py`. The agent picks the closest template, fills placeholders (dataset path, app entrypoint, metrics, thresholds), and writes a committed file like `tests/evals/test_<app>.py`. No throwaway scripts, no hidden goldens — the suite reruns without an agent.": "该 skill 自带针对四种常见形态的 [pytest 模板](https://github.com/confident-ai/deepeval/tree/main/skills/deepeval/templates)——单轮端到端、多轮端到端、单轮组件级，外加一个共享的 `conftest.py`。智能体挑选最接近的模板，填入占位符（数据集路径、应用入口、指标、阈值），写出一个纳入版本管理的文件，如 `tests/evals/test_<app>.py`。没有一次性脚本，没有隐藏的金标——这套件离开智能体也能重跑。",
 "The metrics it picks are not invented either; they come from the [50+ metrics catalog](/docs/metrics-introduction) — `GEval`, `AnswerRelevancyMetric`, `FaithfulnessMetric`, `ToolCorrectnessMetric`, `ConversationalGEval`, etc. — each with a default threshold and a `reason` field the agent can read.": "它挑选的指标也不是凭空发明；它们来自 [50+ 指标目录](/docs/metrics-introduction)——`GEval`、`AnswerRelevancyMetric`、`FaithfulnessMetric`、`ToolCorrectnessMetric`、`ConversationalGEval` 等——每个都带默认阈值和一个智能体可读的 `reason` 字段。",
 "### 3. Run the suite": "### 3. 运行套件",
 "Now the loop's heartbeat: [test run](/docs/command-line-interface#test-run). Same command every round, no flake from rerunning a UI:": "现在到了循环的心脏：[test run](/docs/command-line-interface#test-run)。每一轮都是同一条命令，不会因为重跑 UI 而产生波动：",
 "The CLI prints per-test, per-metric scores plus the metric `reason` strings — that's the structured output the agent parses to pick the next change.": "CLI 会打印每个测试、每个指标的分数以及指标 `reason` 字符串——这就是智能体解析后决定下一步改什么的结构化输出。",
 "### 4. Localize the failure": "### 4. 定位失败",
 "If `@observe`（TypeScript 中为 `observe()`） is on, every span (`retriever`, `lookup_order`, `classify_intent`, `draft_response`) carries its own scored metrics. A failing Faithfulness score isn't \"the app is bad\" — it's \"the `retrieve_policy_docs` span scored 0.64 because the response cited a deprecated policy.\" The agent opens _that_ file, not anything else.": "如果开启了 `@observe`（TypeScript 中为 `observe()`），每个 span（`retriever`、`lookup_order`、`classify_intent`、`draft_response`）都带有自己的指标打分。一个失败的忠实度分数不再是\"这个应用不行\"——而是\"`retrieve_policy_docs` span 得分 0.64，因为响应引用了一条已废弃的政策\"。智能体会打开_那个_文件，而不是别的。",
 "This is the linchpin that makes the loop actionable. See [component-level evals](/docs/evaluation-component-level-llm-evals) for the full mechanics.": "这是让循环可付诸行动的关键。完整机制见[组件级评估](/docs/evaluation-component-level-llm-evals)。",
 "### 5. Patch and verify": "### 5. 修补并验证",
 "The agent edits the smallest thing that could plausibly fix the failing metric — a prompt, a retriever filter, a tool argument schema, a parser. Then it reruns the same `deepeval test run` command. If the failing metric moves green and nothing else regresses, the round closes. If not, it picks the next-smallest change.": "智能体会修改最有可能修复失败指标的最小对象——一句提示词、一个检索器过滤条件、一个工具参数 schema、一个解析器。然后重跑同一条 `deepeval test run` 命令。如果失败的指标转绿且没有其他回归，本轮结束；否则它选择下一个最小的修改。",
 "The skill's [iteration-loop reference](https://github.com/confident-ai/deepeval/blob/main/skills/deepeval/references/iteration-loop.md) bakes in guardrails the agent follows automatically: don't lower thresholds to make failures vanish, don't delete hard goldens, don't swap models or frameworks without asking.": "该 skill 的[迭代循环参考](https://github.com/confident-ai/deepeval/blob/main/skills/deepeval/references/iteration-loop.md)内置了智能体会自动遵循的护栏：不要为了消除失败而降低阈值，不要删除困难金标，未经询问不要更换模型或框架。",
 "## Why This Works": "## 为什么这招管用",
 "Three properties of DeepEval make it a uniquely good signal source for a coding agent — the things that turn \"an eval ran\" into \"the agent knew what to change\":": "DeepEval 的三个特性使它成为编码智能体 uniquely 出色的信号源——这些正是把\"跑了一次评估\"变成\"智能体知道该改什么\"的东西：",
 "- **Structured outputs.** Every metric returns a numeric score, a pass/fail against a threshold, and a natural-language `reason`. That's parseable by an agent without scraping logs.": "- **结构化输出。**每个指标返回数值分数、相对阈值的通过/失败判定，以及自然语言的 `reason`。智能体无需刮取日志即可解析。",
 "- **Span-level localization.** With `@observe(metrics=[...])`（TypeScript 中为 `observe({ metrics: [...] })`）, a failure points at the file that owns the failing span — not the whole app.": "- **Span 级定位。**使用 `@observe(metrics=[...])`（TypeScript 中为 `observe({ metrics: [...] })`），失败会指向拥有该失败 span 的文件——而不是整个应用。",
 "- **A single reproducible CLI.** Same `deepeval test run` command, same dataset, same metrics. The agent has one command to confirm a fix actually moved the score.": "- **单一可复现的 CLI。**同一条 `deepeval test run` 命令、同一数据集、同一批指标。智能体只需一条命令就能确认修复确实移动了分数。",
 "## How to Prompt Your Coding Agent": "## 如何提示你的编码智能体",
 "The single biggest mindset shift: stop asking the coding agent to \"add DeepEval and call it done.\" Ask it to **drive the loop**.": "最大的思维转变是：别再让编码智能体\"装上 DeepEval 就完事\"。要让它**驱动这个循环**。",
 "Good prompts for the build phase:": "构建阶段的好提示词：",
 "That last prompt maps directly to the iteration loop the skill enforces. With the skill installed, _\"Use DeepEval to fix the refund agent — run 5 rounds\"_ is enough.": "最后一条提示词与 skill 强制执行的迭代循环直接对应。装好 skill 后，_\"Use DeepEval to fix the refund agent — run 5 rounds\"_ 就够了。",
 "## Connect to Confident AI": "## 连接 Confident AI",
 "DeepEval is local-first and the loop above works fully offline. Connecting to [Confident AI](https://www.confident-ai.com) extends the loop across your team:": "DeepEval 是本地优先的，上面的循环完全离线可用。连接 [Confident AI](https://www.confident-ai.com) 可以把这个循环扩展到整个团队：",
 "Every `deepeval test run` your coding agent kicks off pushes a testing report your reviewers can open with `deepeval view`. Production monitoring sends new failure cases straight back into the dataset, so the next iteration round picks up real regressions automatically.": "你的编码智能体发起的每次 `deepeval test run` 都会推送一份测试报告，评审者可以用 `deepeval view` 打开。生产监控会把新的失败用例直接送回数据集，让下一轮迭代自动纳入真实的回归。",
 "Now go drive the loop on your own repo — and if you want to know exactly which command your coding agent runs at each stage, the CLI reference has the full surface.": "现在去你自己的仓库上驱动这个循环吧——如果你想确切知道你的编码智能体在每个阶段运行哪条命令，CLI 参考有完整说明。",
 "title=\"5-min Vibe Coder Quickstart\"": "    title=\"5 分钟 Vibe Coder 快速上手\"",
 "description=\"Install the skill, paste the starter prompt, and hand the loop to your coding agent.\"": "    description=\"安装 skill、粘贴起始提示词，把这个循环交给你的编码智能体。\"",
 "description=\"Every flag the loop reaches for, in both Python and TypeScript.\"": "    description=\"这个循环会用到的每一个开关，Python 与 TypeScript 双版本。\"",
 "title: AI Agent Evaluation Quickstart": "title: AI 智能体评估快速上手",
 "Learn how to evaluate AI Agents using `deepeval`, including multi-agent systems and tool-using agents.": "学习如何用 `deepeval` 评估 AI 智能体，包括多智能体系统与使用工具的智能体。",
 "## Overview": "## 概述",
 "AI agent evaluation is different from other types of evals because agentic workflows are complex and **consist of multiple interacting components**, such as tools, chained LLM calls, and RAG modules. Therefore, it’s important to evaluate your AI agents both end-to-end and at the component level to understand how each part performs.": "AI 智能体评估之所以不同于其他类型的评估，是因为智能体工作流非常复杂，**由多个交互组件组成**，例如工具、链式 LLM 调用与 RAG 模块。因此，既要端到端评估你的 AI 智能体，也要在组件级评估，才能了解每个部分的表现。",
 "**In this 5 min quickstart, you'll learn how to:**": "**在这 5 分钟快速上手中，你将学会：**",
 "- Evaluate your agent end-to-end in CI/CD": "- 在 CI/CD 中端到端评估你的智能体",
 "- Evaluate individual components, including sub-agents, in your agent": "- 评估智能体中的单个组件，包括子智能体",
 "## How It Works": "## 工作原理",
 "Agent evals in `deepeval` are powered by **tracing**:": "`deepeval` 的智能体评估由**追踪**驱动：",
 "2. **Every run emits a trace** — with a span per component: LLM calls, tools, retrievers, sub-agents.": "2. **每次运行都会产生一条追踪** —— 每个组件一个 span：LLM 调用、工具、检索器、子智能体。",
 "3. **Attach metrics where you want evals** — the trace for end-to-end, individual spans for component-level.": "3. **把指标挂到你想评估的位置** —— 追踪用于端到端，单个 span 用于组件级。",
 "![component level evals](https://deepeval-docs.s3.us-east-1.amazonaws.com/component-level-evals.png)": "![组件级评估](https://deepeval-docs.s3.us-east-1.amazonaws.com/component-level-evals.png)",
 "Test cases are built from traces automatically, and everything in this quickstart — CI/CD, `evals_iterator()`, sub-agents — runs on this one setup.": "测试用例会从追踪自动构建，本快速上手的一切——CI/CD、`evals_iterator()`、子智能体——都建立在这一套设置之上。",
 "The `[inspect]` sub-module is optional and can bloat up `deepeval`'s package size so we highly recommend that you don't install `deepeval` with `[inspect]` outside of dev environments.": "`[inspect]` 子模块是可选的，会显著增大 `deepeval` 的包体积，因此我们强烈建议不要在开发环境之外用 `[inspect]` 安装 `deepeval`。",
 "The trace-tree TUI ships with the package, so there's no extra sub-module to install.": "追踪树 TUI 随包附带，无需安装额外的子模块。",
 "You should also log in:": "你还应当登录：",
 "It connects you to [Confident AI](https://www.confident-ai.com/) so you can store results, annotate, and inspect evaluated agent traces on the cloud.": "它会把你连接到 [Confident AI](https://www.confident-ai.com/)，让你可以在云端保存结果、做标注并检查已评估的智能体追踪。",
 "As you'll see in the final [next steps](#next-steps) section, Confident AI also lets you monitor and inspect your agent's traces in production — and run online evals on them as they come in.": "正如最后的[下一步](#next-steps)一节所述，Confident AI 还让你能在生产环境中监控并检查智能体的追踪——并在追踪到达时对其运行在线评估。",
 "## Unit Test Agents in CI/CD": "## 在 CI/CD 中对智能体做单元测试",
 "### Create a dataset": "### 创建数据集",
 "[Datasets](/docs/evaluation-datasets) in `deepeval` store [`Golden`s](/docs/evaluation-datasets#what-are-goldens) — the inputs you'll invoke your agent with at test time:": "`deepeval` 中的[数据集](/docs/evaluation-datasets)存储 [`Golden`](/docs/evaluation-datasets#what-are-goldens)——即测试时用来调用智能体的输入：",
 "The dataset lives only for this run — no push, no save. Perfect for quickstarts and one-off evaluations.": "数据集只在本次运行中存在——不推送、不保存。适合快速上手与一次性评估。",
 "You can load entire datasets on Confident AI's cloud in one line of code.": "你可以用一行代码把整个数据集从 Confident AI 的云端加载。",
 "Non-technical domain experts can **create, annotate, and comment** on datasets on Confident AI. You can also upload datasets in CSV format, or push synthetic datasets created in `deepeval` to Confident AI in one line of code.": "非技术的领域专家可以在 Confident AI 上**创建、标注与评论**数据集。你还可以用 CSV 格式上传数据集，或用一行代码把在 `deepeval` 中创建的合成数据集推送到 Confident AI。",
 "For more information, visit the [Confident AI datasets section.](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens)": "更多信息请访问 [Confident AI 数据集板块](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens)。",
 "Column names default to `deepeval`'s own, so `keys` only has to name the ones that differ.": "列名默认采用 `deepeval` 自己的命名，因此 `keys` 只需列出不同的那些。",
 "For more advanced options, like loading `context` and `tools_called` columns or renaming every column, see [loading a dataset](/docs/evaluation-datasets#load-dataset).": "更多高级选项（例如加载 `context` 与 `tools_called` 列或重命名所有列）见[加载数据集](/docs/evaluation-datasets#load-dataset)。",
 "For more advanced options, like loading `context` and `tools_called` keys or reading goldens a line at a time from a `.jsonl` file, see [loading a dataset](/docs/evaluation-datasets#load-dataset).": "更多高级选项（例如加载 `context` 与 `tools_called` 键，或从 `.jsonl` 文件逐行读取金标）见[加载数据集](/docs/evaluation-datasets#load-dataset)。",
 "An `EvaluationDataset` holds either single-turn or multi-turn goldens, never both. A file that mixes the two raises an error.": "一个 `EvaluationDataset` 只容纳单轮或多轮金标中的一种，不能混用。混有两种的文件会抛出错误。",
 "### Write your test file": "### 编写测试文件",
 "Wrap your agent's top-level function with `@observe` and set the trace-level test case fields with `update_current_trace(...)`. See [LLM tracing](/docs/evaluation-llm-tracing) for the full surface.": "用 `@observe` 包装智能体的顶层函数，并用 `update_current_trace(...)` 设置追踪级测试用例字段。完整用法见 [LLM 追踪](/docs/evaluation-llm-tracing)。",
 "Pass `deepeval`'s `CallbackHandler` to your agent's `invoke` method. See the [LangChain integration](/integrations/frameworks/langchain) for the full surface.": "把 `deepeval` 的 `CallbackHandler` 传给你智能体的 `invoke` 方法。完整用法见 [LangChain 集成](/integrations/frameworks/langchain)。",
 "Pass `deepeval`'s `CallbackHandler` to your `StateGraph`'s `invoke` method. See the [LangGraph integration](/integrations/frameworks/langgraph) for the full surface.": "把 `deepeval` 的 `CallbackHandler` 传给你的 `StateGraph` 的 `invoke` 方法。完整用法见 [LangGraph 集成](/integrations/frameworks/langgraph)。",
 "Drop-in replace `from openai import OpenAI` with `from deepeval.openai import OpenAI` — every completion call becomes an LLM span automatically. See the [OpenAI integration](/integrations/frameworks/openai) for the full surface.": "把 `from openai import OpenAI` 直接替换为 `from deepeval.openai import OpenAI` —— 每次补全调用都会自动成为 LLM span。完整用法见 [OpenAI 集成](/integrations/frameworks/openai)。",
 "Pass `DeepEvalInstrumentationSettings()` to your `Agent`'s `instrument` keyword. See the [Pydantic AI integration](/integrations/frameworks/pydanticai) for the full surface.": "把 `DeepEvalInstrumentationSettings()` 传给你 `Agent` 的 `instrument` 关键字参数。完整用法见 [Pydantic AI 集成](/integrations/frameworks/pydanticai)。",
 "Call `instrument_agentcore()` before creating your AgentCore app — it also instruments [Strands](https://strandsagents.com/) agents running inside AgentCore. See the [AgentCore integration](/integrations/frameworks/agentcore) for the full surface.": "在创建 AgentCore 应用之前调用 `instrument_agentcore()`——它也会对运行在 AgentCore 内的 [Strands](https://strandsagents.com/) 智能体做埋点。完整用法见 [AgentCore 集成](/integrations/frameworks/agentcore)。",
 "Call `instrument_strands()` before creating or invoking your agent (for AgentCore-hosted Strands, use the AgentCore tab). See the [Strands integration](/integrations/frameworks/strands) for the full surface.": "在创建或调用你的智能体之前调用 `instrument_strands()`（对托管在 AgentCore 上的 Strands，请改用 AgentCore 标签页）。完整用法见 [Strands 集成](/integrations/frameworks/strands)。",
 "Drop-in replace `from anthropic import Anthropic` with `from deepeval.anthropic import Anthropic` — every `messages.create(...)` call becomes an LLM span automatically. See the [Anthropic integration](/integrations/frameworks/anthropic) for the full surface.": "把 `from anthropic import Anthropic` 直接替换为 `from deepeval.anthropic import Anthropic` —— 每次 `messages.create(...)` 调用都会自动成为 LLM span。完整用法见 [Anthropic 集成](/integrations/frameworks/anthropic)。",
 "Register `deepeval`'s event handler against LlamaIndex's instrumentation dispatcher. See the [LlamaIndex integration](/integrations/frameworks/llamaindex) for the full surface.": "把 `deepeval` 的事件处理器注册到 LlamaIndex 的 instrumentation 调度器上。完整用法见 [LlamaIndex 集成](/integrations/frameworks/llamaindex)。",
 "Register `DeepEvalTracingProcessor` once, then build your agent with `deepeval`'s `Agent` shim. See the [OpenAI Agents integration](/integrations/frameworks/openai-agents) for the full surface.": "注册一次 `DeepEvalTracingProcessor`，然后用 `deepeval` 的 `Agent` 垫片构建你的智能体。完整用法见 [OpenAI Agents 集成](/integrations/frameworks/openai-agents)。",
 "Call `instrument_google_adk()` once before building your `LlmAgent`. See the [Google ADK integration](/integrations/frameworks/google-adk) for the full surface.": "在构建 `LlmAgent` 之前调用一次 `instrument_google_adk()`。完整用法见 [Google ADK 集成](/integrations/frameworks/google-adk)。",
 "Call `instrument_crewai()` once, then build your crew with `deepeval`'s `Crew` and `Agent` shims. See the [CrewAI integration](/integrations/frameworks/crewai) for the full surface.": "调用一次 `instrument_crewai()`，然后用 `deepeval` 的 `Crew` 与 `Agent` 垫片构建你的 crew。完整用法见 [CrewAI 集成](/integrations/frameworks/crewai)。",
 "Every tab below uses the same matcher: the golden is the subject, and `task` produces the trace judged against it. Importing `deepeval/vitest` registers `toPass()`, and `npx deepeval test run` injects it for you.": "下面的每个标签页都用同一个匹配器：金标是被测主体，`task` 产出供评判的追踪。导入 `deepeval/vitest` 会注册 `toPass()`，`npx deepeval test run` 会为你自动注入。",
 "Wrap your agent's top-level function with `observe` and set the trace-level test case fields with `updateCurrentTrace(...)`. See [LLM tracing](/docs/evaluation-llm-tracing) for the full surface.": "用 `observe` 包装智能体的顶层函数，并用 `updateCurrentTrace(...)` 设置追踪级测试用例字段。完整用法见 [LLM 追踪](/docs/evaluation-llm-tracing)。",
 "Pass `deepeval`'s `DeepEvalCallbackHandler` to your agent's `invoke` method. See the [LangChain integration](/integrations/frameworks/langchain) for the full surface.": "把 `deepeval` 的 `DeepEvalCallbackHandler` 传给你智能体的 `invoke` 方法。完整用法见 [LangChain 集成](/integrations/frameworks/langchain)。",
 "`toPass()` waits for the exporter to settle before scoring, so no manual flush is needed inside a test. See the [Mastra integration](/integrations/frameworks/mastra) for the full surface.": "`toPass()` 在打分前会等待 exporter 落定，因此测试内无需手动 flush。完整用法见 [Mastra 集成](/integrations/frameworks/mastra)。",
 "The same `DeepEvalCallbackHandler` covers LangGraph, since LangGraph runs on LangChain's callback system. See the [LangGraph integration](/integrations/frameworks/langgraph) for the full surface.": "同一个 `DeepEvalCallbackHandler` 也覆盖 LangGraph，因为 LangGraph 运行在 LangChain 的回调系统之上。完整用法见 [LangGraph 集成](/integrations/frameworks/langgraph)。",
 "`instrumentOpenAI` patches the client in place, so every call it makes becomes an LLM span. See the [OpenAI integration](/integrations/frameworks/openai) for the full surface.": "`instrumentOpenAI` 会就地修补 client，它发出的每次调用都会成为 LLM span。完整用法见 [OpenAI 集成](/integrations/frameworks/openai)。",
 "Register the processor once and every `run(...)` becomes a trace. See the [OpenAI Agents integration](/integrations/frameworks/openai-agents) for the full surface.": "注册一次处理器，每次 `run(...)` 都会成为一条追踪。完整用法见 [OpenAI Agents 集成](/integrations/frameworks/openai-agents)。",
 "Telemetry is opt-in per call: a call without `isEnabled` and a `tracer` is never traced. See the [Vercel AI SDK integration](/integrations/frameworks/ai-sdk) for the full surface.": "遥测按调用选择加入：没有 `isEnabled` 和 `tracer` 的调用永远不会被追踪。完整用法见 [Vercel AI SDK 集成](/integrations/frameworks/ai-sdk)。",
 "There are **ONE** mandatory and **ONE** optional parameter for `assert_test()` in this mode:": "此模式下 `assert_test()` 有 **一个** 必填参数和 **一个** 可选参数：",
 "- `golden`: the `Golden` you pass in through your test function.": "- `golden`：你通过测试函数传入的 `Golden`。",
 "- [Optional] `metrics`: a list of `BaseMetric`s that you wish to run on your trace (aka. end-to-end evals).": "- [可选] `metrics`：你希望对追踪运行（即端到端评估）的 `BaseMetric` 列表。",
 "The golden goes to `expect()`, and `toPass()` takes one optional argument:": "金标传入 `expect()`，`toPass()` 接受一个可选参数：",
 "- [Optional] `metrics`: an array of `BaseMetric`s that you wish to run on your trace (aka. end-to-end evals).": "- [可选] `metrics`：你希望对追踪运行（即端到端评估）的 `BaseMetric` 数组。",
 "### Run your test file": "### 运行你的测试文件",
 "The plain `pytest` command works but is highly not recommended. `deepeval test run` adds a range of functionalities on top of Pytest for unit-testing LLMs, enabled by [8+ optional flags](/docs/evaluation-flags-and-configs#flags-for-deepeval-test-run) — async behavior, error handling, repeats, identifiers, and more.": "直接用 `pytest` 命令也能跑，但强烈不推荐。`deepeval test run` 在 Pytest 之上为 LLM 单元测试增加了一系列功能，由 [8+ 个可选开关](/docs/evaluation-flags-and-configs#flags-for-deepeval-test-run)启用——异步行为、错误处理、重复运行、标识符等等。",
 "The plain `vitest` command works but is highly not recommended. `npx deepeval test run` adds a range of functionalities on top of Vitest for unit-testing LLMs, enabled by [8+ optional flags](/docs/evaluation-flags-and-configs#flags-for-deepeval-test-run) — error handling, repeats, identifiers, and more. It also injects the `toPass()` matcher for you.": "直接用 `vitest` 命令也能跑，但强烈不推荐。`npx deepeval test run` 在 Vitest 之上为 LLM 单元测试增加了一系列功能，由 [8+ 个可选开关](/docs/evaluation-flags-and-configs#flags-for-deepeval-test-run)启用——错误处理、重复运行、标识符等等。它还会为你注入 `toPass()` 匹配器。",
 "Every test run is also saved locally. Run `deepeval inspect` to view your agent's full execution trace — per-span scores and metric reasons included — right in your terminal:": "每次测试运行也会保存在本地。运行 `deepeval inspect` 即可在终端中查看智能体的完整执行追踪——包含每个 span 的分数与指标原因：",
 "This is what the `[inspect]` sub-module you installed earlier is for — full details in the [`deepeval inspect` reference](/docs/command-line-interface#inspect).": "这就是你先前安装的 `[inspect]` 子模块的用途——完整细节见 [`deepeval inspect` 参考](/docs/command-line-interface#inspect)。",
 "### Add it to your CI/CD pipeline": "### 加入你的 CI/CD 流水线",
 "Drop `deepeval test run` into a `.yml` to unit test your agent on every push or PR. This example uses `OPENAI_API_KEY` as your LLM judge to run evals locally. Add `CONFIDENT_API_KEY` to send results to Confident AI.": "把 `deepeval test run` 放进 `.yml`，即可在每次推送或 PR 时对你的智能体做单元测试。本例使用 `OPENAI_API_KEY` 作为 LLM 裁判在本地运行评估。添加 `CONFIDENT_API_KEY` 可把结果发送到 Confident AI。",
 "✅ Done. Failing metrics now fail the build, so agent regressions get caught before they ship.": "✅ 完成。失败的指标现在会让构建失败，智能体的回归在上线前就会被发现。",
 "## Evaluate Agents in a Loop": "## 在循环中评估智能体",
 "Unit testing is built for CI/CD, but during development you'll usually iterate on your agent in a script or notebook. `dataset.evals_iterator()` does this in a plain `for` loop — it yields each golden, builds a test case from the captured trace, scores your metrics, and bundles traces + scores into one test run.": "单元测试是为 CI/CD 设计的，但在开发期你通常会在脚本或笔记本中迭代你的智能体。`dataset.evals_iterator()` 用一个普通的 `for` 循环完成这件事——它逐个产出金标、从捕获的追踪构建测试用例、为你的指标打分，并把追踪与分数汇总为一次测试运行。",
 "### Reuse your dataset": "### 复用你的数据集",
 "`evals_iterator()` works off the same `EvaluationDataset` of goldens you created [earlier](#create-a-dataset):": "`evals_iterator()` 复用你[之前](#create-a-dataset)创建的同一个 `EvaluationDataset` 金标集合：",
 "### Trace your agent and run the loop": "### 追踪你的智能体并运行循环",
 "Every integration comes in an **Async** and a **Sync** flavor:": "每个集成都提供 **Async** 与 **Sync** 两种形式：",
 "- **Async** (default, fastest): wrap each invocation in `asyncio.create_task(...)` + `dataset.evaluate(task)` so goldens run concurrently.": "- **Async**（默认，最快）：把每次调用包在 `asyncio.create_task(...)` + `dataset.evaluate(task)` 中，让金标并发运行。",
 "- **Sync**: pass `AsyncConfig(run_async=False)` to run one golden at a time — handy for debugging, rate-limited providers, or Jupyter event-loop quirks.": "- **Sync**：传入 `AsyncConfig(run_async=False)` 一次运行一个金标——便于调试、应对受速率限制的提供商或 Jupyter 事件循环的特殊情况。",
 "✅ Done. The `metrics` you pass to `evals_iterator()` score each trace end-to-end; for its other optional arguments, see the [single-turn end-to-end guide](/docs/evaluation-end-to-end-single-turn#approach-1-evals_iterator-with-tracing-recommended).": "✅ 完成。你传给 `evals_iterator()` 的 `metrics` 会对每条追踪做端到端打分；其他可选参数见[单轮端到端指南](/docs/evaluation-end-to-end-single-turn#approach-1-evals_iterator-with-tracing-recommended)。",
 "Like `deepeval test run`, every `evals_iterator()` run is saved locally — view the full execution trace in the same trace-tree TUI:": "与 `deepeval test run` 一样，每次 `evals_iterator()` 运行都会保存在本地——在同一个追踪树 TUI 中查看完整执行追踪：",
 "## Evaluate Sub-Agents": "## 评估子智能体",
 "In multi-agent systems, every sub-agent invocation — a delegation, handoff, or nested call — emits its own **agent span** inside the trace. To evaluate a sub-agent in isolation, attach metrics to its agent span instead of the trace.": "在多智能体系统中，每次子智能体调用——委托、移交或嵌套调用——都会在追踪中产生自己的**智能体 span**。要单独评估某个子智能体，把指标挂到它的智能体 span 上而不是追踪上。",
 "Only integrations that emit agent spans are shown below. The OpenAI and Anthropic clients don't — they produce LLM spans only, which you can target with `LlmSpanContext` instead. See [component-level evaluation](/docs/evaluation-component-level-llm-evals).": "下面只展示会产生智能体 span 的集成。OpenAI 与 Anthropic 客户端不会——它们只产生 LLM span，你可以改用 `LlmSpanContext` 来定位。见[组件级评估](/docs/evaluation-component-level-llm-evals)。",
 "Mark your sub-agent with `@observe(type=\"agent\")` and pass `metrics=[...]` to it:": "用 `@observe(type=\"agent\")` 标记你的子智能体，并传入 `metrics=[...]`：",
 "Stage a metric for the next agent span with `next_agent_span(...)` — the `CallbackHandler` drains it onto the next agent span opened during the run:": "用 `next_agent_span(...)` 为下一个智能体 span 预置一个指标——`CallbackHandler` 会把它交给运行期间打开的下一个智能体 span：",
 "Like `next_llm_span`, this is one-shot — only the first agent span in the run picks up the metric.": "与 `next_llm_span` 一样，这是一次性的——只有运行中的第一个智能体 span 会拾取该指标。",
 "Stage a metric for the next agent span with `next_agent_span(...)` — the `CallbackHandler` drains it onto the next agent span opened during the graph run (e.g. a sub-agent node or subgraph):": "用 `next_agent_span(...)` 为下一个智能体 span 预置一个指标——`CallbackHandler` 会把它交给图运行期间打开的下一个智能体 span（例如子智能体节点或子图）：",
 "Like `next_llm_span`, this is one-shot — only the first agent span in the graph run picks up the metric.": "与 `next_llm_span` 一样，这是一次性的——只有图运行中的第一个智能体 span 会拾取该指标。",
 "Stage a metric for the next agent span with `next_agent_span(...)` — delegations and handoffs nest as their own agent spans:": "用 `next_agent_span(...)` 为下一个智能体 span 预置一个指标——委托与移交会作为各自的智能体 span 嵌套：",
 "Stage a metric for the next agent span with `next_agent_span(...)`:": "用 `next_agent_span(...)` 为下一个智能体 span 预置一个指标：",
 "Stage a metric for the agent span with `AgentSpanContext(metrics=[...])` inside `with trace(...)`:": "在 `with trace(...)` 内用 `AgentSpanContext(metrics=[...])` 为智能体 span 指定指标：",
 "Attach `agent_metrics=[...]` to the sub-agent's `Agent` shim — it scores that agent's span on every run, including when it's reached through a handoff:": "把 `agent_metrics=[...]` 挂到子智能体的 `Agent` 垫片上——每次运行都会对该智能体的 span 打分，包括通过移交到达该智能体的情况：",
 "Attach `metrics=[...]` to the specific `Agent` shim — it scores that agent's span on every execution, independent of the rest of the crew:": "把 `metrics=[...]` 挂到指定的 `Agent` 垫片上——每次执行都会对该智能体的 span 打分，与 crew 的其余部分无关：",
 "Mark your sub-agent with `observe({ type: \"agent\" })` and pass `metrics` to it:": "用 `observe({ type: \"agent\" })` 标记你的子智能体，并传入 `metrics`：",
 "Every nested `observe` call becomes its own span, so the metrics you attach to a sub-agent score that sub-agent alone.": "每个嵌套的 `observe` 调用都成为自己的 span，因此你挂在子智能体上的指标只对该子智能体打分。",
 "Stage a metric for the next agent span with `nextAgentSpan(...)` — the `DeepEvalCallbackHandler` drains it onto the next agent span opened during the run:": "用 `nextAgentSpan(...)` 为下一个智能体 span 预置一个指标——`DeepEvalCallbackHandler` 会把它交给运行期间打开的下一个智能体 span：",
 "Like `nextLlmSpan`, this is one-shot — only the first agent span in the run picks up the metric, which is the root span `invoke(...)` opens.": "与 `nextLlmSpan` 一样，这是一次性的——只有运行中的第一个智能体 span 会拾取该指标，即 `invoke(...)` 打开的根 span。",
 "Stage a metric for the next agent span with `nextAgentSpan(...)` — Mastra's `AGENT_RUN` and `WORKFLOW_RUN` spans both arrive as agent spans, so this scores a sub-agent or a workflow step in isolation:": "用 `nextAgentSpan(...)` 为下一个智能体 span 预置一个指标——Mastra 的 `AGENT_RUN` 与 `WORKFLOW_RUN` span 都会作为智能体 span 到达，因此可以单独对子智能体或工作流步骤打分：",
 "Stage a metric for the next agent span with `nextAgentSpan(...)` — the `DeepEvalCallbackHandler` drains it onto the next agent span opened during the graph run:": "用 `nextAgentSpan(...)` 为下一个智能体 span 预置一个指标——`DeepEvalCallbackHandler` 会把它交给图运行期间打开的下一个智能体 span：",
 "Like `nextLlmSpan`, this is one-shot — only the first agent span in the graph run picks up the metric. Nested graph nodes are recorded for hierarchy but do not open agent spans of their own, so scoring a specific node means invoking that subgraph directly.": "与 `nextLlmSpan` 一样，这是一次性的——只有图运行中的第一个智能体 span 会拾取该指标。嵌套的图节点会记录层级但不会打开自己的智能体 span，因此要对某个节点打分，需要直接调用那个子图。",
 "Stage a metric for the next agent span with `nextAgentSpan(...)` — the `DeepEvalTracingProcessor` opens one agent span per `Agent` invocation, including handoffs:": "用 `nextAgentSpan(...)` 为下一个智能体 span 预置一个指标——`DeepEvalTracingProcessor` 会为每次 `Agent` 调用打开一个智能体 span，包括移交：",
 "One-shot again: the metric lands on the agent you invoked, not on an agent it later hands off to. To score a handoff target on its own, run that agent directly as its own eval.": "同样是一次性的：指标落在你调用的那个智能体上，而不是它随后移交的对象。要单独对移交目标打分，请把该智能体作为独立评估直接运行。",
 "The Vercel AI SDK has no tab here because its root is the generation call itself — there is no agent span to stage onto. Score those runs end-to-end, or with `nextLlmSpan` / `nextToolSpan`.": "Vercel AI SDK 没有出现在这里的标签页中，因为它的根就是生成调用本身——没有可供预置的智能体 span。对这类运行请做端到端打分，或使用 `nextLlmSpan` / `nextToolSpan`。",
 "Now that you have run your first agentic evals, you should:": "现在你已经运行了第一次智能体评估，接下来应当：",
 "1. **Customize your metrics**: Update the [list of metrics](/docs/metrics-introduction) for each component.": "1. **自定义你的指标**：为每个组件更新[指标列表](/docs/metrics-introduction)。",
 "2. **Customize tracing**: It helps benchmark and identify different components on the UI.": "2. **自定义追踪**：这有助于在界面上对不同的组件做基准对比与识别。",
 "3. **Explore the integration docs**: Each [framework integration](/integrations/frameworks/openai) has its own page with end-to-end and component-level patterns.": "3. **探索集成文档**：每个[框架集成](/integrations/frameworks/openai)都有独立页面，提供端到端与组件级模式。",
 "You'll be able to analyze performance over time on **traces** (end-to-end) and **spans** (component-level).": "你将能在**追踪**（端到端）与 **Span**（组件级）两个层面分析随时间变化的性能。",
 "Evals on traces are [end-to-end evaluations](/docs/evaluation-end-to-end-llm-evals), where a single LLM interaction is being evaluated.": "对追踪的评估即[端到端评估](/docs/evaluation-end-to-end-llm-evals)，评估的是一次完整的 LLM 交互。",
 "<summary>Configure Environment Variables</summary>": "<summary>配置环境变量</summary>",
 "<Tab title=\"Single-Turn\">": "<Tab title=\"单轮\">",
 "<Tab title=\"Multi-Turn\">": "<Tab title=\"多轮\">",
 "<summary>Evaluations getting \"stuck\"?</summary>": "<summary>评估\"卡住\"了？</summary>",
 "<figcaption>A complete walkthrough of running and analyzing your first evals.</figcaption>": "  <figcaption>完整演示如何运行并分析你的第一次评估。</figcaption>",
 "<Tab title=\"Locally in JSON\">": "<Tab title=\"本地 JSON\">",
 "<Tab title=\"Manual Instrumentation\">": "<Tab title=\"手动埋点\">",
 "<Tab title=\"Trace-Level Evals in Prod\">": "<Tab title=\"生产中的追踪级评估\">",
 "<figcaption>Score complete results or agent trajectories on live traffic.</figcaption>": "  <figcaption>对线上流量中的完整结果或智能体轨迹打分。</figcaption>",
 "<Tab title=\"Span (component-level) Evals in Prod\">": "<Tab title=\"生产中的 Span（组件级）评估\">",
 "<figcaption>Evaluate individual components like tool calls and retrievals inside each trace.</figcaption>": "  <figcaption>评估每条追踪内的工具调用、检索等单个组件。</figcaption>",
 "<Tab title=\"Thread (conversation) Evals in Prod\">": "<Tab title=\"生产中的线程（对话）评估\">",
 "<figcaption>Group traces into threads to evaluate whole conversations.</figcaption>": "  <figcaption>把追踪分组为线程，以评估完整对话。</figcaption>",
 "<Accordion title=\"Why did my eval get stuck?\">": "<Accordion title=\"为什么我的评估卡住了？\">",
 "<Accordion title=\"Do I need Confident AI for this quickstart?\">": "<Accordion title=\"这个快速上手需要 Confident AI 吗？\">",
 "<Accordion title=\"Where should I put this test file?\">": "<Accordion title=\"这个测试文件应该放在哪里？\">",
 "<Accordion title=\"Can I use a model other than OpenAI?\">": "<Accordion title=\"可以用 OpenAI 以外的模型吗？\">",
 "<Accordion title=\"What should I read after this?\">": "<Accordion title=\"读完之后该看什么？\">",
 "<figcaption>See every agent execution as a full trace of LLM, tool, and retriever calls.</figcaption>": "  <figcaption>把每次智能体执行看作 LLM、工具与检索器调用的完整追踪。</figcaption>",
 "<Tab title=\"In Code\">": "<Tab title=\"在代码中\">",
 "<Tab title=\"Pull from Confident AI\">": "<Tab title=\"从 Confident AI 拉取\">",
 "<Tab title=\"Load from CSV\">": "<Tab title=\"从 CSV 加载\">",
 "<Tab title=\"Load from JSON\">": "<Tab title=\"从 JSON 加载\">",
 "<Tab title=\"End-to-end (traces) in prod\">": "<Tab title=\"生产中的端到端（追踪）\">",
 "<figcaption>Score entire agent executions end-to-end on live traffic.</figcaption>": "  <figcaption>在线上流量中对完整的智能体执行做端到端打分。</figcaption>",
 "<Tab title=\"Component-level (spans) in prod\">": "<Tab title=\"生产中的组件级（Span）\">",
 "title: Chatbot Evaluation Quickstart": "title: 聊天机器人评估快速上手",
 "Learn to evaluate any multi-turn chatbot using `deepeval` - including QA agents, customer support chatbots, and even chatrooms.": "学习如何用 `deepeval` 评估任意多轮聊天机器人——包括 QA 智能体、客服聊天机器人，甚至群聊室。",
 "Chatbot Evaluation is different from other types of evaluations because unlike single-turn tasks, conversations happen over multiple \"turns\". This means your chatbot must stay context-aware across the conversation, and not just accurate in individual responses.": "聊天机器人评估之所以不同于其他类型的评估，是因为与单轮任务不同，对话会跨越多个「轮次」发生。这意味着你的聊天机器人必须在整个对话中保持上下文感知，而不只是在单条回复上准确。",
 "**In this 10 min quickstart, you'll learn how to:**": "**在这 10 分钟快速上手中，你将学会：**",
 "- Prepare conversational test cases": "- 准备对话式测试用例",
 "- Evaluate chatbot conversations": "- 评估聊天机器人对话",
 "- Simulate users interactions": "- 模拟用户交互",
 "## Prerequisites": "## 前置条件",
 "Install `deepeval`:": "安装 `deepeval`：",
 "You'll also want a Confident AI API key (recommended). Sign up for one [here.](https://app.confident-ai.com)": "你还需要一个 Confident AI API key（推荐）。在[这里](https://app.confident-ai.com)注册获取。",
 "Confident AI allows you to view and share your chatbot testing reports. Set your API key in the CLI:": "Confident AI 让你查看并分享你的聊天机器人测试报告。在 CLI 中设置你的 API key：",
 "## Understanding Multi-Turn Evals": "## 理解多轮评估",
 "Multi-turn evals are tricky because of the ad-hoc nature of conversations. The nth AI output will depend on the (n-1)th user input, and this depends on all prior turns up until the initial message.": "多轮评估很棘手，因为对话是临时发生的。第 n 条 AI 输出取决于第 n-1 条用户输入，而后者又依赖于直到首条消息之前的所有轮次。",
 "Hence, when running evals for the purpose of benchmarking we cannot compare different conversations by looking at their turns. In `deepeval`, multi-turn interactions are grouped by **scenarios** instead. If two conversations occur under the same scenario, we consider those the same.": "因此，在为基准测试运行评估时，我们无法通过查看轮次来比较不同的对话。在 `deepeval` 中，多轮交互改用**场景（scenario）**来分组。如果两段对话发生在同一场景下，我们认为它们是相同的。",
 "Scenarios are optional in the diagram because not all users start with conversations with labelled scenarios.": "图中场景是可选的，因为并非所有用户的对话都带有标注好的场景。",
 "## Run A Multi-Turn Eval": "## 运行一次多轮评估",
 "In `deepeval`, chatbots are evaluated as multi-turn **interactions**. In code, you'll have to format them into test cases, which adheres to OpenAI's messages format.": "在 `deepeval` 中，聊天机器人作为多轮**交互**来评估。在代码里，你需要把它们格式化成测试用例，其格式遵循 OpenAI 的 messages 格式。",
 "`deepeval` provides a wide selection of LLM models that you can easily choose from and run evaluations with.": "`deepeval` 提供了丰富的 LLM 模型选择，你可以轻松选用并运行评估。",
 "<Tab title=\"Open AI\">": "<Tab title=\"OpenAI\">",
 "To use OpenAI for `deepeval`'s LLM metrics, supply your `OPENAI_API_KEY` in the CLI:": "要在 `deepeval` 的 LLM 指标中使用 OpenAI，请在 CLI 中提供你的 `OPENAI_API_KEY`：",
 "Alternatively, if you're working in a notebook environment (Jupyter or Colab), set your `OPENAI_API_KEY` in a cell:": "或者，如果你在笔记本环境（Jupyter 或 Colab）中工作，请在单元格里设置 `OPENAI_API_KEY`：",
 "Please **do not include** quotation marks when setting your `API_KEYS` as environment variables if you're working in a notebook environment.": "在笔记本环境中设置 `API_KEYS` 作为环境变量时，请**不要加**引号。",
 "<Tab title=\"Azure Open AI\">": "<Tab title=\"Azure OpenAI\">",
 "`deepeval` also allows you to use Azure OpenAI for metrics that are evaluated using an LLM. Run the following command in the CLI to configure your `deepeval` environment to use Azure OpenAI for **all** LLM-based metrics.": "`deepeval` 也允许你对使用 LLM 评估的指标使用 Azure OpenAI。在 CLI 中运行以下命令，把 `deepeval` 环境配置为对**所有**基于 LLM 的指标使用 Azure OpenAI。",
 "Your OpenAI API version must be at least `2024-08-01-preview`, when structured output was released.": "你的 OpenAI API 版本必须至少是 `2024-08-01-preview`，即结构化输出发布之时。",
 "Note that the `model-version` is **optional**. If you ever wish to stop using Azure OpenAI and move back to regular OpenAI, simply run:": "注意 `model-version` 是**可选的**。如果你以后想停用 Azure OpenAI、换回常规 OpenAI，运行：",
 "Before getting started, make sure your [Ollama model](https://ollama.com/search) is installed and running. You can also see the full list of available models by clicking on the previous link.": "开始之前，请确认你的 [Ollama 模型](https://ollama.com/search)已安装并正在运行。点击前述链接可以查看可用模型的完整列表。",
 "To use **Ollama** models for your metrics, run `deepeval set-ollama --model=<model>` in your CLI. For example:": "要在你的指标中使用 **Ollama** 模型，在 CLI 中运行 `deepeval set-ollama --model=<model>`。例如：",
 "Optionally, you can specify the **base URL** of your local Ollama model instance if you've defined a custom port. The default base URL is set to `http://localhost:11434`.": "可选地，如果你定义了自定义端口，可以指定本地 Ollama 模型实例的**基础 URL**。默认基础 URL 为 `http://localhost:11434`。",
 "To stop using your local Ollama model and move back to OpenAI, run:": "要停用本地 Ollama 模型、换回 OpenAI，运行：",
 "The `deepeval set-ollama` command is used exclusively to configure LLM models. If you intend to use a custom embedding model from Ollama with the synthesizer, please [refer to this section of the guide](/guides/guides-using-custom-embedding-models).": "`deepeval set-ollama` 命令专用于配置 LLM 模型。如果你想在合成器中使用 Ollama 的自定义嵌入模型，请[参阅指南的这一节](/guides/guides-using-custom-embedding-models)。",
 "To use Gemini models with `deepeval`, run the following command in your CLI.": "要在 `deepeval` 中使用 Gemini 模型，在 CLI 中运行以下命令。",
 "<Tab title=\"Custom LLM example\">": "<Tab title=\"自定义 LLM 示例\">",
 "`deepeval` allows you to use **ANY** custom LLM for evaluation. This includes LLMs from langchain's chat model integrations, Hugging Face's `transformers` library, or even LLMs in GGML format.": "`deepeval` 允许你使用**任意**自定义 LLM 做评估。包括来自 langchain 聊天模型集成的 LLM、Hugging Face 的 `transformers` 库，甚至 GGML 格式的 LLM。",
 "This includes any of your favorite models such as:": "这包括你喜欢的任何模型，例如：",
 "All the examples can be [found here](/guides/guides-using-custom-llms#more-examples), but down below is a quick example of a custom Azure OpenAI model through langchain's `AzureChatOpenAI` module for evaluation:": "所有示例都[在这里](/guides/guides-using-custom-llms#more-examples)，下面是通过 langchain 的 `AzureChatOpenAI` 模块接入自定义 Azure OpenAI 模型做评估的快速示例：",
 "When creating a custom LLM evaluation model you should **ALWAYS**:": "创建自定义 LLM 评估模型时，你应当**始终**：",
 "- inherit `DeepEvalBaseLLM`.": "- 继承 `DeepEvalBaseLLM`。",
 "- implement the `get_model_name()` method, which simply returns a string representing your custom model name.": "- 实现 `get_model_name()` 方法，它只返回一个表示你自定义模型名称的字符串。",
 "- implement the `load_model()` method, which will be responsible for returning a model object.": "- 实现 `load_model()` 方法，它负责返回一个模型对象。",
 "- implement the `generate()` method with **one and only one** parameter of type string that acts as the prompt to your custom LLM.": "- 实现 `generate()` 方法，它有**且只有一个**字符串类型参数，作为你的自定义 LLM 的提示词。",
 "- the `generate()` method should return the final output string of your custom LLM. Note that we called `chat_model.invoke(prompt).content` to access the model generations in this particular example, but this could be different depending on the implementation of your custom model object.": "- `generate()` 方法应返回你的自定义 LLM 的最终输出字符串。注意在本例中我们用 `chat_model.invoke(prompt).content` 访问模型生成结果，但具体取决于你的自定义模型对象的实现，可能有所不同。",
 "- implement the `a_generate()` method, with the same function signature as `generate()`. **Note that this is an async method**. In this example, we called `await chat_model.ainvoke(prompt)`, which is an asynchronous wrapper provided by LangChain's chat models.": "- 实现 `a_generate()` 方法，函数签名与 `generate()` 相同。**注意这是一个异步方法**。本例中我们用了 `await chat_model.ainvoke(prompt)`，它是 LangChain 聊天模型提供的异步包装。",
 "The `a_generate()` method is what `deepeval` uses to generate LLM outputs when you execute metrics / run evaluations asynchronously.": "当你异步执行指标 / 运行评估时，`deepeval` 会用 `a_generate()` 方法生成 LLM 输出。",
 "If your custom model object does not have an asynchronous interface, simply reuse the same code from `generate()` (scroll down to the `Mistral7B` example for more details). However, this would make `a_generate()` a blocking process, regardless of whether you've turned on `async_mode` for a metric or not.": "如果你的自定义模型对象没有异步接口，直接复用 `generate()` 的代码即可（往下翻到 `Mistral7B` 示例查看更多细节）。但这会让 `a_generate()` 变成阻塞过程，无论你是否为指标开启了 `async_mode`。",
 "Lastly, to use it for evaluation for an LLM-Eval:": "最后，把它用于 LLM-Eval 评估：",
 "While the Azure OpenAI command configures `deepeval` to use Azure OpenAI globally for all LLM-Evals, a custom LLM has to be set each time you instantiate a metric. Remember to provide your custom LLM instance through the `model` parameter for metrics you wish to use it for.": "虽然 Azure OpenAI 命令把 `deepeval` 配置为对所有 LLM-Eval 全局使用 Azure OpenAI，但自定义 LLM 必须在每次实例化指标时单独设置。记得通过 `model` 参数为你想用它的指标提供自定义 LLM 实例。",
 "We **CANNOT** guarantee that evaluations will work as expected when using a custom model. This is because evaluation requires high levels of reasoning and the ability to follow instructions such as outputting responses in valid JSON formats. [**To better enable custom LLMs output valid JSONs, read this guide**](/guides/guides-using-custom-llms).": "我们**无法**保证使用自定义模型时评估会如期工作。这是因为评估需要高水平的推理能力以及遵循指令的能力，例如以合法 JSON 格式输出响应。[**要让自定义 LLM 更好地输出合法 JSON，请阅读本指南**](/guides/guides-using-custom-llms)。",
 "Alternatively, if you find yourself running into JSON errors and would like to ignore it, use the [`-c` and `-i` flag during test run](/docs/evaluation-flags-and-configs#flags-for-deepeval-test-run):": "另外，如果你经常遇到 JSON 错误并想忽略它，可以在[测试运行时使用 `-c` 和 `-i` 开关](/docs/evaluation-flags-and-configs#flags-for-deepeval-test-run)：",
 "The `-i` flag ignores errors while the `-c` flag utilizes the local `deepeval` cache, so for a partially successful test run you don't have to rerun test cases that didn't error.": "`-i` 开关会忽略错误，`-c` 开关利用本地 `deepeval` 缓存，这样部分成功的测试运行不必重跑没有出错的测试用例。",
 "Alternatively, `deepeval` autoloads `.env.local` then `.env` at import time, so you can keep the key out of your shell entirely:": "另外，`deepeval` 会在导入时依次自动加载 `.env.local` 和 `.env`，因此你可以完全不把密钥放进 shell：",
 "The [Vercel AI SDK](/integrations/models/ai-sdk) is configured in code rather than through a `set-*` command, by wrapping any AI SDK `LanguageModel` in an `AISDKModel`. Install the AI SDK core package alongside the provider you want to evaluate with:": "[Vercel AI SDK](/integrations/models/ai-sdk) 通过代码而非 `set-*` 命令配置：把任意 AI SDK `LanguageModel` 包进一个 `AISDKModel`。先安装 AI SDK 核心包与你想用的提供商包：",
 "Each AI SDK provider reads its own API key from the environment, following that provider's convention:": "每个 AI SDK 提供商按其自身约定从环境中读取自己的 API key：",
 "Then pass the wrapped model to any metric:": "然后把包装好的模型传给任意指标：",
 "`deepeval` has a ready-made model class for OpenAI, Anthropic, Gemini and a handful of others, but not for every provider out there. If the one you want to judge with is missing — Mistral, Cohere, Groq, Together — install its AI SDK package and wrap it in an `AISDKModel` to use it anyway.": "`deepeval` 为 OpenAI、Anthropic、Gemini 等少数提供商提供了现成的模型类，但并非覆盖所有提供商。如果你想用的裁判模型不在其中——Mistral、Cohere、Groq、Together——安装其 AI SDK 包并包进 `AISDKModel` 即可使用。",
 "To use Anthropic models for `deepeval`'s LLM metrics, supply your `ANTHROPIC_API_KEY` in the CLI:": "要在 `deepeval` 的 LLM 指标中使用 Anthropic 模型，请在 CLI 中提供你的 `ANTHROPIC_API_KEY`：",
 "Then select the Claude model you want to judge with for **all** LLM-based metrics:": "然后为**所有**基于 LLM 的指标选择你想用的 Claude 裁判模型：",
 "To stop using Anthropic and move back to OpenAI, run:": "要停用 Anthropic、换回 OpenAI，运行：",
 "To use **Ollama** models for your metrics, run `npx deepeval set-ollama --model=<model>` in your CLI. For example:": "要在你的指标中使用 **Ollama** 模型，在 CLI 中运行 `npx deepeval set-ollama --model=<model>`。例如：",
 "`deepeval` allows you to use **ANY** custom LLM for evaluation. This includes LLMs from LangChain's chat model integrations, any provider reachable through the Vercel AI SDK, or a model you serve yourself.": "`deepeval` 允许你使用**任意**自定义 LLM 做评估。包括来自 LangChain 聊天模型集成的 LLM、通过 Vercel AI SDK 可达的任何提供商，或你自己托管的模型。",
 "All the examples can be [found here](/guides/guides-using-custom-llms#more-examples), but down below is a quick example of a custom Azure OpenAI model through LangChain's `AzureChatOpenAI` module for evaluation:": "所有示例都[在这里](/guides/guides-using-custom-llms#more-examples)，下面是通过 LangChain 的 `AzureChatOpenAI` 模块接入自定义 Azure OpenAI 模型做评估的快速示例：",
 "- extend `DeepEvalBaseLLM`.": "- 继承 `DeepEvalBaseLLM`。",
 "- implement the `getModelName()` method, which simply returns a string representing your custom model name.": "- 实现 `getModelName()` 方法，它只返回一个表示你自定义模型名称的字符串。",
 "- implement the `generate()` method, which is always `async` — there is no `generate()` / `a_generate()` split to mirror, and no `loadModel()` to implement.": "- 实现 `generate()` 方法，它始终是 `async`——没有需要对应的 `generate()` / `a_generate()` 之分，也没有要实现的 `loadModel()`。",
 "- return `{ output, cost }` from `generate()`, where `cost` may be `null` if your provider doesn't report one.": "- 从 `generate()` 返回 `{ output, cost }`，其中 `cost` 在你的提供商不上报时可以是 `null`。",
 "- respect the optional `schema` argument. Metrics pass a zod schema whenever they need structured output, and expect `output` to be the parsed object rather than a string.": "- 遵循可选的 `schema` 参数。指标需要结构化输出时会传一个 zod schema，并期望 `output` 是解析后的对象而非字符串。",
 "If your provider can't produce structured output natively, ask for JSON in the prompt and run the response through `schema.parse()` yourself — that is exactly what `deepeval`'s built-in models do.": "如果你的提供商无法原生产出结构化输出，就在提示词里要求 JSON，并自己把响应跑一遍 `schema.parse()`——这正是 `deepeval` 内置模型的做法。",
 "### Create a test case": "### 创建测试用例",
 "Create a `ConversationalTestCase` by passing in a list of `Turn`s from an existing conversation, similar to OpenAI's message format.": "通过传入一段现有对话的 `Turn` 列表（类似 OpenAI 的消息格式）来创建 `ConversationalTestCase`。",
 "You can learn about a `Turn`'s data model [here.](/docs/evaluation-multiturn-test-cases#turns)": "`Turn` 的数据模型见[这里](/docs/evaluation-multiturn-test-cases#turns)。",
 "### Run an evaluation": "### 运行一次评估",
 "Run an evaluation on the test case using `deepeval`'s multi-turn metrics, or create your own using [Conversational G-Eval](/docs/metrics-conversational-g-eval).": "用 `deepeval` 的多轮指标对该测试用例运行评估，或用[对话式 G-Eval](/docs/metrics-conversational-g-eval) 创建你自己的指标。",
 "Finally run your file:": "最后运行你的文件：",
 "🎉🥳 **Congratulations!** You've just ran your first multi-turn eval. Here's what happened:": "🎉🥳 **恭喜！**你刚完成了第一次多轮评估。发生的 things 如下：",
 "- When you call `evaluate()`, `deepeval` runs all your metrics against all your test cases": "- 当你调用 `evaluate()` 时，`deepeval` 会对你所有的测试用例运行你的所有指标",
 "- Every metric outputs a score between `0-1`, with a `threshold` defaulted to `0.5`": "- 每个指标输出一个介于 `0-1` 之间的分数，`threshold` 默认为 `0.5`",
 "- A test case passes only if all metrics pass": "- 只有所有指标都通过，测试用例才算通过",
 "This creates a test run, which is a \"snapshot\"/benchmark of your multi-turn chatbot at any point in time.": "这会创建一次测试运行，即你的多轮聊天机器人在任意时间点的「快照」/基准。",
 "### View on Confident AI (recommended)": "### 在 Confident AI 上查看（推荐）",
 "If you've set your `CONFIDENT_API_KEY`, test runs will appear automatically on [Confident AI](https://app.confident-ai.com), which `deepeval` integrates with natively.": "如果你设置好了 `CONFIDENT_API_KEY`，测试运行会自动出现在 [Confident AI](https://app.confident-ai.com) 上，`deepeval` 与之原生集成。",
 "<figcaption>Review full conversations alongside their metric scores and reasons.</figcaption>": "  <figcaption>查看完整对话及其指标分数与原因。</figcaption>",
 "If you haven't logged in, you can still upload the test run to Confident AI from local cache:": "如果你还没有登录，仍然可以从本地缓存把测试运行上传到 Confident AI：",
 "## Working With Datasets": "## 使用数据集",
 "Although we ran an evaluation in the previous section, it's not very useful because it is far from a standardized benchmark. To create a standardized benchmark for evals, use `deepeval`'s datasets:": "虽然我们在上一节运行了一次评估，但它远非标准化基准，用处有限。要为评估创建标准化基准，请使用 `deepeval` 的数据集：",
 "A dataset is a collection of goldens in `deepeval`, and in a multi-turn context this these are represented by `ConversationalGolden`s.": "数据集是 `deepeval` 中金标（golden）的集合；在多轮语境下，它们由 `ConversationalGolden` 表示。",
 "![Evaluation Dataset](https://deepeval-docs.s3.us-east-1.amazonaws.com/docs:evaluation-dataset.png)": "![评估数据集](https://deepeval-docs.s3.amazonaws.com/docs:evaluation-dataset.png)",
 "The idea is simple - we start with a list of standardized `scenario`s for each golden, and we'll simulate turns during evaluation time for more robust evaluation.": "思路很简单——我们为每个金标准备一组标准化的 `scenario`，并在评估时模拟轮次，以获得更稳健的评估。",
 "## Simulate Turns for Evals": "## 为评估模拟轮次",
 "Evaluating your chatbot from [simulated turns](/docs/getting-started-chatbots#evaluate-chatbots-from-simulations) is **the best** approach for multi-turn evals, because it:": "用[模拟轮次](/docs/getting-started-chatbots#evaluate-chatbots-from-simulations)来评估你的聊天机器人是**最好的**多轮评估方式，因为它：",
 "- Standardizes your test bench, unlike ad-hoc evals": "- 把你的测试台标准化，不同于临时起意的评估",
 "- Automates the process of manual prompting, which can take hours": "- 把可能耗费数小时的手工提示过程自动化",
 "Both of which are solved using `deepeval`'s `ConversationSimulator`.": "这两点都可以用 `deepeval` 的 `ConversationSimulator` 解决。",
 "### Create dataset of goldens": "### 创建金标数据集",
 "Create a `ConversationalGolden` by providing who the user is, the scenario, and the expected outcome, for the conversation you wish to simulate.": "通过提供用户是谁、场景以及预期结果，为你想模拟的对话创建一个 `ConversationalGolden`。",
 "If you've set your `CONFIDENT_API_KEY` correctly, you can save them on the platform to collaborate with your team:": "如果你正确设置了 `CONFIDENT_API_KEY`，可以把它们保存到平台上与团队协作：",
 "<figcaption>Push conversational goldens to the platform and edit them with your team.</figcaption>": "  <figcaption>把对话式金标推送到平台并与团队一起编辑。</figcaption>",
 "### Wrap chatbot in callback": "### 用回调包装聊天机器人",
 "Define a callback function to generate the **next chatbot response** in a conversation, given the conversation history.": "定义一个回调函数，在给定对话历史的情况下生成对话中的**下一条聊天机器人回复**。",
 "Your model callback is handed the `input`, the `turns` so far, and a stable `thread_id` — take only the ones you need. It should return a `Turn` object.": "你的模型回调会拿到 `input`、目前的 `turns` 以及一个稳定的 `thread_id`——只取你需要的即可。它应当返回一个 `Turn` 对象。",
 "### Simulate turns": "### 模拟轮次",
 "Use `deepeval`'s `ConversationSimulator` to simulate turns using goldens in your dataset:": "使用 `deepeval` 的 `ConversationSimulator`，基于你数据集中的金标模拟轮次：",
 "Here, we only have 1 test case, but in reality you'll want to simulate from at least 20 goldens.": "这里我们只有 1 个测试用例，但实际中你应当至少从 20 个金标开始模拟。",
 "<summary>Click to view an example simulated test case</summary>": "<summary>点击查看一个模拟出的测试用例示例</summary>",
 "Your generated test cases should be populated with simulated `Turn`s, along with the `scenario` and `expected_outcome` from the conversation golden.": "你生成的测试用例应当填入模拟出的 `Turn`，以及对话金标中的 `scenario` 与 `expected_outcome`。",
 "Run an evaluation like how you learnt in the previous section:": "像你在上一节学到的那样运行评估：",
 "✅ Done. You've successfully learnt how to benchmark your chatbot.": "✅ 完成。你已成功学会如何为你的聊天机器人做基准测试。",
 "Now that you have run your first chatbot evals, you should:": "现在你已经运行了第一次聊天机器人评估，接下来应当：",
 "1. **Customize your metrics**: Update the [list of metrics](/docs/metrics-introduction) based on your use case.": "1. **自定义你的指标**：根据你的使用场景更新[指标列表](/docs/metrics-introduction)。",
 "2. **Setup tracing**: It helps you [log multi-turn](https://www.confident-ai.com/docs/llm-tracing/advanced-features/threads) interactions in production.": "2. **设置追踪**：它帮助你在生产环境中[记录多轮](https://www.confident-ai.com/docs/llm-tracing/advanced-features/threads)交互。",
 "3. **Enable evals in production**: Monitor performance over time [using the metrics](https://www.confident-ai.com/docs/llm-tracing/evaluations#offline-evaluations) you've defined on Confident AI.": "3. **在生产中启用评估**：在 Confident AI 上[使用你定义的指标](https://www.confident-ai.com/docs/llm-tracing/evaluations#offline-evaluations)持续监控表现。",
 "You'll be able to analyze performance over time on **threads** this way, and add them back to your evals dataset for further evaluation.": "这样你就能在**线程**层面分析随时间变化的性能，并把它们加回你的评估数据集做进一步评估。",
 "<figcaption>Group traces into threads to evaluate live conversations.</figcaption>": "  <figcaption>把追踪分组为线程，以评估实时对话。</figcaption>",
 "title: RAG Evaluation Quickstart": "title: RAG 评估快速上手",
 "Learn to evaluate retrieval-augmented-generation (RAG) pipelines and systems using `deepeval`, such as RAG QA, summarizaters, and customer support chatbots.": "学习如何用 `deepeval` 评估检索增强生成（RAG）流水线与系统，例如 RAG 问答、摘要器和客服聊天机器人。",
 "RAG evaluation involves evaluating the retriever and generator as separately components. This is because in a RAG pipeline, the final output is only as good as the context you've fed into your LLM.": "RAG 评估把检索器与生成器作为相互独立的组件分别评估。这是因为在 RAG 流水线中，最终输出好不好，取决于你喂给 LLM 的上下文。",
 "- Evaluate your RAG pipeline end-to-end": "- 端到端评估你的 RAG 流水线",
 "- Test the retriever and generator as separate components": "- 把检索器与生成器作为独立组件分别测试",
 "- Evaluate multi-turn RAG": "- 评估多轮 RAG",
 "No. The trace-tree TUI is built on [Ink](https://github.com/vadimdemedes/ink), declared as an optional dependency that `npm install` pulls in by default. If you installed with `--omit=optional`, add it back with `npm install ink react`.": "不需要。追踪树 TUI 基于 [Ink](https://github.com/vadimdemedes/ink) 构建，声明为可选依赖，`npm install` 默认会安装。如果你用了 `--omit=optional` 安装，用 `npm install ink react` 补装即可。",
 "You should also run `deepeval login`:": "你还应当运行 `deepeval login`：",
 "## Unit Test RAG in CI/CD": "## 在 CI/CD 中对 RAG 做单元测试",
 "[Datasets](/docs/evaluation-datasets) in `deepeval` store [`Golden`s](/docs/evaluation-datasets#what-are-goldens) — the inputs you'll invoke your RAG pipeline with at test time:": "`deepeval` 中的[数据集](/docs/evaluation-datasets)存储 [`Golden`](/docs/evaluation-datasets#what-are-goldens)——即测试时用来调用 RAG 流水线的输入：",
 "Pick based on whether you can modify your RAG pipeline's code:": "根据你能否修改 RAG 流水线的代码来选择：",
 "- **Without tracing** — you're testing a deployed or black-box system (e.g. as a QA engineer). Build the `LLMTestCase` yourself from your pipeline's outputs.": "- **无追踪** —— 你在测试已部署的黑盒系统（例如作为 QA 工程师）。用流水线的输出自行构建 `LLMTestCase`。",
 "- **With tracing** — you own the code. Instrument it once and `deepeval` builds test cases from traces automatically, with a full trace per test case.": "- **有追踪** —— 代码归你所有。做一次埋点，`deepeval` 就会从追踪自动构建测试用例，每个测试用例附带完整追踪。",
 "<Tab title=\"Without Tracing\">": "<Tab title=\"无追踪\">",
 "`retrieval_context` is crucial — RAG metrics like `FaithfulnessMetric` evaluate against the chunks retrieved at evaluation time.": "`retrieval_context` 至关重要——`FaithfulnessMetric` 等 RAG 指标是针对评估时检索到的文本块来打分的。",
 "<Tab title=\"With Tracing\">": "<Tab title=\"有追踪\">",
 "`assert_test()` only needs the golden — the test case is built from the captured trace. This same setup unlocks [retriever & generator evals](#evaluate-retriever--generator) below.": "`assert_test()` 只需要金标——测试用例从捕获的追踪自动构建。这套设置同时也解锁了下面的[检索器与生成器评估](#evaluate-retriever--generator)。",
 "`toPass()` only needs the golden — the test case is built from the captured trace. This same setup unlocks [retriever & generator evals](#evaluate-retriever--generator) below.": "`toPass()` 只需要金标——测试用例从捕获的追踪自动构建。这套设置同时也解锁了下面的[检索器与生成器评估](#evaluate-retriever--generator)。",
 "- Drop this command into a `.yml` to run on every push or PR — see [unit testing in CI/CD](/docs/evaluation-unit-testing-in-ci-cd#yaml-file-for-cicd-evals) for a full pipeline example.": "- 把这条命令放进 `.yml`，即可在每次推送或 PR 时运行——完整流水线示例见[CI/CD 中的单元测试](/docs/evaluation-unit-testing-in-ci-cd#yaml-file-for-cicd-evals)。",
 "- Test runs are saved locally — `deepeval inspect` opens them in a trace-tree TUI with per-span scores and metric reasons.": "- 测试运行会保存在本地——`deepeval inspect` 会在追踪树 TUI 中打开它们，含每个 span 的分数与指标原因。",
 "## Evaluate RAG with `evaluate()`": "## 用 `evaluate()` 评估 RAG",
 "✅ Done. Each test case is scored against every metric, and the results roll up into a test run — a snapshot of your RAG pipeline's quality at this point in time.": "✅ 完成。每个测试用例都会对每个指标打分，结果汇总为一次测试运行——即你的 RAG 流水线在此刻质量的快照。",
 "`deepeval` offers 5 RAG metrics — generator-focused:": "`deepeval` 提供 5 个 RAG 指标——面向生成器的：",
 "- [Answer Relevancy](/docs/metrics-answer-relevancy)": "- [答案相关性](/docs/metrics-answer-relevancy)",
 "- [Faithfulness](/docs/metrics-faithfulness)": "- [忠实度](/docs/metrics-faithfulness)",
 "And retriever-focused:": "以及面向检索器的：",
 "- [Contextual Relevancy](/docs/metrics-contextual-relevancy)": "- [上下文相关性](/docs/metrics-contextual-relevancy)",
 "- [Contextual Precision](/docs/metrics-contextual-precision)": "- [上下文精确率](/docs/metrics-contextual-precision)",
 "- [Contextual Recall](/docs/metrics-contextual-recall)": "- [上下文召回率](/docs/metrics-contextual-recall)",
 "Contextual precision and recall also require an `expected_output` on your test cases. See the [RAG evaluation guide](/guides/guides-rag-evaluation) for how to pick.": "上下文精确率与召回率还需要测试用例带 `expected_output`。如何取舍见 [RAG 评估指南](/guides/guides-rag-evaluation)。",
 "A single end-to-end score can't tell you whether a bad answer came from bad retrieval or bad generation. If you own the code, trace your pipeline's components and attach metrics directly to them:": "单一的端到端分数无法告诉你糟糕的答案究竟来自糟糕的检索还是糟糕的生成。如果代码归你所有，就对流水线的组件做追踪并把指标直接挂上去：",
 "✅ Done. A plain loop is all it takes:": "✅ 完成。一个普通循环就够了：",
 "- **Retriever span** — scored by retriever metrics like `ContextualRelevancyMetric` against its `retrieval_context`.": "- **检索器 span** —— 由 `ContextualRelevancyMetric` 等检索器指标针对其 `retrieval_context` 打分。",
 "- **Generator span** — scored by generator metrics like `AnswerRelevancyMetric` against its `actual_output`.": "- **生成器 span** —— 由 `AnswerRelevancyMetric` 等生成器指标针对其 `actual_output` 打分。",
 "See [component-level evaluation](/docs/evaluation-component-level-llm-evals) for the full surface, including framework integrations.": "完整用法（包括框架集成）见[组件级评估](/docs/evaluation-component-level-llm-evals)。",
 "For chatbots that rely on RAG — like customer support bots — the unit of evaluation is the whole conversation, and each assistant turn carries its own `retrieval_context`.": "对于依赖 RAG 的聊天机器人——比如客服机器人——评估单元是整段对话，且助手每个轮次都带有自己的 `retrieval_context`。",
 "Multi-turn datasets store [`ConversationalGolden`s](/docs/evaluation-datasets) — scenarios to simulate, not pre-written turns:": "多轮数据集存储 [`ConversationalGolden`](/docs/evaluation-datasets)——待模拟的场景，而不是预先写好的轮次：",
 "Multi-turn goldens describe a scenario to simulate, not pre-written turns.": "多轮金标描述的是待模拟的场景，而不是预先写好的轮次。",
 "A row carrying a `scenario` column becomes a `ConversationalGolden`, so no `keys` override is needed when your columns already use `deepeval`'s names.": "带 `scenario` 列的行会成为 `ConversationalGolden`，因此当你的列已使用 `deepeval` 的命名时，无需 `keys` 覆盖。",
 "For more advanced options, like loading `turns` and `context` columns or renaming every column, see [loading a dataset](/docs/evaluation-datasets#load-dataset).": "更多高级选项（例如加载 `turns` 与 `context` 列或重命名所有列）见[加载数据集](/docs/evaluation-datasets#load-dataset)。",
 "For more advanced options, like loading `turns` and `context` keys or reading goldens a line at a time from a `.jsonl` file, see [loading a dataset](/docs/evaluation-datasets#load-dataset).": "更多高级选项（例如加载 `turns` 与 `context` 键，或从 `.jsonl` 文件逐行读取金标）见[加载数据集](/docs/evaluation-datasets#load-dataset)。",
 "### Simulate conversations": "### 模拟对话",
 "Wrap your RAG chatbot in a `model_callback` that returns each reply as a `Turn` — **including its `retrieval_context`** — then let `ConversationSimulator` generate the conversations:": "把你的 RAG 聊天机器人包装在一个 `model_callback` 中，它把每条回复作为一个 `Turn` 返回——**包括其 `retrieval_context`**——然后让 `ConversationSimulator` 生成对话：",
 "The `retrieval_context` on each assistant turn is what lets RAG metrics check grounding turn-by-turn.": "助手每个轮次上的 `retrieval_context` 让 RAG 指标能够逐轮检查内容的依据。",
 "Score the simulated conversations with multi-turn RAG metrics:": "用多轮 RAG 指标为模拟出的对话打分：",
 "✅ Done. Each assistant turn is evaluated against the chunks it retrieved:": "✅ 完成。助手的每个轮次都会针对它检索到的文本块被评估：",
 "- `TurnFaithfulnessMetric` — does each reply stay grounded in its `retrieval_context`?": "- `TurnFaithfulnessMetric` —— 每条回复是否忠实于其 `retrieval_context`？",
 "- `TurnContextualRelevancyMetric` — were the retrieved chunks relevant to that point in the conversation?": "- `TurnContextualRelevancyMetric` —— 检索到的文本块与对话中该时点相关吗？",
 "- Add general conversational metrics like `TurnRelevancyMetric` too — see the [chatbot quickstart](/docs/getting-started-chatbots) for the full multi-turn surface.": "- 也可以加上 `TurnRelevancyMetric` 等通用对话指标——完整多轮能力见[聊天机器人快速上手](/docs/getting-started-chatbots)。",
 "Now that you have run your first RAG evals, you should:": "现在你已经运行了第一次 RAG 评估，接下来应当：",
 "1. **Customize your metrics**: Include all 5 [RAG metrics](/docs/metrics-introduction) based on your use case.": "1. **自定义你的指标**：根据你的使用场景纳入全部 5 个 [RAG 指标](/docs/metrics-introduction)。",
 "2. **Prepare a dataset**: If you don't have one, [generate one](/docs/golden-synthesizer) as a starting point.": "2. **准备数据集**：如果还没有，[生成一个](/docs/golden-synthesizer)作为起点。",
 "<figcaption>Trace retrievals and generations, and run evals on live traffic.</figcaption>": "  <figcaption>对检索与生成做追踪，并在实时流量上运行评估。</figcaption>",
 "title: MCP Evaluation Quickstart": "title: MCP 评估快速上手",
 "Learn to evaluate model-context-protocol (MCP) based applications using `deepeval`, for both single-turn and multi-turn use cases.": "学习如何用 `deepeval` 评估基于模型上下文协议（MCP）的应用，单轮与多轮场景皆可。",
 "MCP evaluation is different from other evaluations because you can choose to create single-turn test cases or multi-turn test cases based on your application design and architecture.": "MCP 评估之所以不同于其他评估，是因为你可以根据应用的设计与架构，选择创建单轮测试用例或多轮测试用例。",
 "- Track your MCP interactions": "- 追踪你的 MCP 交互",
 "- Create test cases for your application": "- 为你的应用创建测试用例",
 "- Evaluate your MCP based application using MCP metrics": "- 使用 MCP 指标评估基于 MCP 的应用",
 "- Install `deepeval`, an MCP client, and whichever LLM SDK your host calls": "- 安装 `deepeval`、一个 MCP 客户端，以及你的宿主调用的任意 LLM SDK",
 "- A Confident AI API key (recommended). Sign up for one [here](https://app.confident-ai.com)": "- 一个 Confident AI API key（推荐）。在[这里](https://app.confident-ai.com)注册获取",
 "This quickstart's host talks to its server over streamable HTTP and calls Anthropic:": "本快速上手中的宿主通过可流式 HTTP 与其服务器通信，并调用 Anthropic：",
 "## Understanding MCP Evals": "## 理解 MCP 评估",
 "**Model Context Protocol (MCP)** is an open-source framework developed by **Anthropic** to standardize how AI systems, particularly large language models (LLMs), interact with external tools and data sources.": "**模型上下文协议（MCP）**是由 **Anthropic** 开发的开源框架，用于标准化 AI 系统（尤其是大语言模型（LLM））与外部工具及数据源的交互方式。",
 "The MCP architecture is composed of three main components:": "MCP 架构由三个主要组件组成：",
 "- **Host** — The AI application that coordinates and manages one or more MCP clients": "- **宿主（Host）** —— 协调并管理一个或多个 MCP 客户端的 AI 应用",
 "- **Client** — Maintains a one-to-one connection with a server and retrieves context from it for the host to use": "- **客户端（Client）** —— 与服务器保持一对一连接，并从服务器检索上下文供宿主使用",
 "- **Server** — Paired with a single client, providing the context the client passes to the host": "- **服务器（Server）** —— 与单个客户端配对，提供客户端传递给宿主的上下文",
 "![MCP Architecture Image](https://deepeval-docs.s3.amazonaws.com/mcp-architecture.png)": "![MCP 架构图](https://deepeval-docs.s3.amazonaws.com/mcp-architecture.png)",
 "`deepeval` allows you to evaluate the MCP host on various criterion like its primitive usage, argument generation and task completion.": "`deepeval` 允许你按多种标准评估 MCP 宿主，例如其原语（primitive）使用、参数生成与任务完成度。",
 "## Run Your First MCP Eval": "## 运行你的第一次 MCP 评估",
 "In `deepeval` MCP evaluations can be done using either single-turn or multi-turn test cases. In code, you'll have to track all MCP interactions and finally create a test case after the execution of your application.": "在 `deepeval` 中，MCP 评估既可以用单轮测试用例，也可以用多轮测试用例。在代码里，你需要追踪所有 MCP 交互，并在应用执行完成后创建测试用例。",
 "MCP metrics judge with an LLM, which defaults to OpenAI. You can judge with any model `deepeval` supports instead:": "MCP 指标使用 LLM 裁判，默认为 OpenAI。你也可以改用 `deepeval` 支持的任意模型：",
 "### Create an MCP server": "### 创建 MCP 服务器",
 "Connect your application to MCP servers and create the `MCPServer` object for all the MCP servers you're using.": "把你的应用连接到 MCP 服务器，并为你使用的所有 MCP 服务器创建 `MCPServer` 对象。",
 "### Track your MCP interactions": "### 追踪你的 MCP 交互",
 "In your MCP application's main file, you need to track all the MCP interactions during run time. This includes adding `tools_called`, `resources_called` and `prompts_called` whenever your host uses them.": "在你 MCP 应用的主文件中，你需要在运行时追踪所有 MCP 交互。包括在宿主使用 `tools_called`、`resources_called` 和 `prompts_called` 时把它们加上。",
 "![MCP Interaction tracking](https://deepeval-docs.s3.us-east-1.amazonaws.com/docs:evaluation-mcp-tools.png)": "![MCP 交互追踪](https://deepeval-docs.s3.us-east-1.amazonaws.com/docs:evaluation-mcp-tools.png)",
 "You can also track any [resources](https://www.deepeval.com/docs/evaluation-mcp#resources) or [prompts](https://www.deepeval.com/docs/evaluation-mcp#prompts) if you use them. You are now tracking all the MCP interactions during run time of your application.": "如果你用到了[资源（resources）](https://www.deepeval.com/docs/evaluation-mcp#resources)或[提示词（prompts）](https://www.deepeval.com/docs/evaluation-mcp#prompts)，也可以一并追踪。现在你已经在应用运行期间追踪了所有 MCP 交互。",
 "You can now create a test case for your MCP application using the above interactions.": "现在你可以使用上述交互为你的 MCP 应用创建测试用例了。",
 "The test cases must be created after the execution of your application.": "测试用例必须在应用执行完成之后创建。",
 "Click here to see a [full example on how to create single-turn test cases](https://github.com/confident-ai/deepeval/blob/main/examples/mcp_evaluation/mcp_eval_single_turn.py) for MCP evaluations.": "点击[这里](https://github.com/confident-ai/deepeval/blob/main/examples/mcp_evaluation/mcp_eval_single_turn.py)查看如何为 MCP 评估创建单轮测试用例的完整示例。",
 "You can make your `main()` function return the servers and the interactions you tracked. This helps you import your MCP application anywhere and create test cases easily in different test files.": "你可以让你的 `main()` 函数返回服务器与你追踪到的交互。这便于你在任何地方导入 MCP 应用，并在不同测试文件中轻松创建测试用例。",
 "### Define metrics": "### 定义指标",
 "You can now use the [`MCPUseMetric`](/docs/metrics-mcp-use) to run evals on your single-turn your test case.": "现在你可以使用 [`MCPUseMetric`](/docs/metrics-mcp-use) 对你的单轮测试用例运行评估。",
 "Run an evaluation on the test cases you previously created using the metrics defined above.": "使用上面定义的指标，对你先前创建的测试用例运行评估。",
 "🎉🥳 **Congratulations!** You just ran your first single-turn MCP evaluation. Here's what happened:": "🎉🥳 **恭喜！**你刚完成了第一次单轮 MCP 评估。发生的具体过程如下：",
 "- When you call `evaluate()`, `deepeval` runs all your `metrics` against all `test_cases`": "- 当你调用 `evaluate()` 时，`deepeval` 会对你所有的 `test_cases` 运行你的所有 `metrics`",
 "- All `metrics` outputs a score between `0-1`, with a `threshold` defaulted to `0.5`": "- 所有 `metrics` 输出一个介于 `0-1` 之间的分数，`threshold` 默认为 `0.5`",
 "- The `MCPUseMetric` first evaluates your test case on its primitive usage to see how well your application has utilized the MCP capabilities given to it.": "- `MCPUseMetric` 首先按原语使用评估你的测试用例，看你的应用对交给它的 MCP 能力利用得如何",
 "- It then evaluates the argument correctness to see if the inputs generated for your primitive usage were correct and accurate for the task.": "- 接着评估参数正确性，看为原语使用生成的输入对任务而言是否正确且准确",
 "- The `MCPUseMetric` then finally takes the minimum of the both scores to give a final score to your test case.": "- 最后 `MCPUseMetric` 取两个分数中的较小者，作为测试用例的最终分数",
 "<figcaption>Inspect MCP eval results test case by test case.</figcaption>": "  <figcaption>逐个测试用例检查 MCP 评估结果。</figcaption>",
 "## Multi-Turn MCP Evals": "## 多轮 MCP 评估",
 "For multi-turn MCP evals, you are required to add the `mcp_tools_called`, `mcp_resources_called` and `mcp_prompts_called` in the `Turn` object for each turn of the assistant. (if any)": "对于多轮 MCP 评估，你需要在助手每个轮次的 `Turn` 对象中加入 `mcp_tools_called`、`mcp_resources_called` 和 `mcp_prompts_called`（如果有的话）。",
 "During the interactive session of your application, you need to track all the MCP interactions. This includes adding `tools_called`, `resources_called` and `prompts_called` whenever your host uses them.": "在应用的交互会话期间，你需要追踪所有 MCP 交互。包括在宿主使用 `tools_called`、`resources_called` 和 `prompts_called` 时把它们加上。",
 "You can now create a test case for your MCP application using the above `turns` and `mcp_servers`.": "现在你可以使用上述 `turns` 与 `mcp_servers` 为你的 MCP 应用创建测试用例了。",
 "The test cases must be created after the execution of the application.": "测试用例必须在应用执行完成之后创建。",
 "Click here to see a [full example on how to create multi-turn test cases](https://github.com/confident-ai/deepeval/blob/main/examples/mcp_evaluation/mcp_eval_multi_turn.py) for MCP evaluations.": "点击[这里](https://github.com/confident-ai/deepeval/blob/main/examples/mcp_evaluation/mcp_eval_multi_turn.py)查看如何为 MCP 评估创建多轮测试用例的完整示例。",
 "You can make your `main()` function return `turns` and `mcp_servers`. This helps you import your MCP application anywhere and create test cases easily in different test files.": "你可以让你的 `main()` 函数返回 `turns` 与 `mcp_servers`。这便于你在任何地方导入 MCP 应用，并在不同测试文件中轻松创建测试用例。",
 "You can now use the [MCP metrics](/docs/metrics-multi-turn-mcp-use) to run evals on your test cases. There's two metrics for multi-turn test cases that support MCP evals.": "现在你可以使用 [MCP 指标](/docs/metrics-multi-turn-mcp-use)对你的测试用例运行评估。支持 MCP 评估的多轮测试用例指标有两个。",
 "🎉🥳 **Congratulations!** You just ran your first multi-turn MCP evaluation. Here's what happened:": "🎉🥳 **恭喜！**你刚完成了第一次多轮 MCP 评估。发生的具体过程如下：",
 "- You used the `MultiTurnMCPUseMetric` and `MCPTaskCompletionMetric` for testing your MCP application": "- 你使用了 `MultiTurnMCPUseMetric` 与 `MCPTaskCompletionMetric` 来测试你的 MCP 应用",
 "- The `MultiTurnMCPUseMetric` evaluates your application's capability on primitive usage and argument generation to get the final score.": "- `MultiTurnMCPUseMetric` 评估应用在原语使用与参数生成上的能力，得出最终分数",
 "- The `MCPTaskCompletionMetric` evaluates whether your application has satisfied the given task for all the interactions between user and assistant.": "- `MCPTaskCompletionMetric` 评估你的应用是否在用户与助手的全部交互中完成了给定任务",
 "<figcaption>Evaluate full conversations between your MCP client and server.</figcaption>": "  <figcaption>评估 MCP 客户端与服务器之间的完整对话。</figcaption>",
 "Now that you have run your first MCP eval, you should:": "现在你已经运行了第一次 MCP 评估，接下来应当：",
 "1. **Customize your metrics**: You can change the threshold of your metrics to be more strict to your use-case.": "1. **自定义你的指标**：你可以调紧指标的阈值，使其更贴合你的使用场景。",
 "2. **Prepare a dataset**: If you don't have one, [generate one](/docs/golden-synthesizer) as a starting point to store your inputs as goldens.": "2. **准备数据集**：如果还没有，[生成一个](/docs/golden-synthesizer)作为起点，把你的输入存为金标（golden）。",
 "3. **Setup Tracing**: If you created your own custom MCP server, you can [setup tracing](https://documentation.confident-ai.com/docs/llm-tracing/tracing-features/span-types) on your tool definitions.": "3. **设置追踪**：如果你创建了自定义 MCP 服务器，可以[为你的工具定义设置追踪](https://documentation.confident-ai.com/docs/llm-tracing/tracing-features/span-types)。",
 "<figcaption>Evaluate individual MCP tool calls inside every trace.</figcaption>": "  <figcaption>评估每条追踪内的单个 MCP 工具调用。</figcaption>",
 "You can [learn more about MCP here](/docs/evaluation-mcp).": "你可以[在这里进一步了解 MCP](/docs/evaluation-mcp)。",
 "title: Introduction to LLM Evals": "title: LLM 评估简介",
 "Evaluation refers to the process of testing your LLM application outputs, and requires the following components:": "评估指的是测试你的 LLM 应用输出的过程，它需要以下组件：",
 "- Test cases": "- 测试用例",
 "- Metrics": "- 指标",
 "- Evaluation dataset": "- 评估数据集",
 "Here's a diagram of what an ideal evaluation workflow looks like using `deepeval`:": "下面是使用 `deepeval` 的理想评估工作流示意图：",
 "There are **TWO** types of LLM evaluations in `deepeval`:": "`deepeval` 中有**两种** LLM 评估：",
 "- [End-to-end evaluation](/docs/evaluation-end-to-end-llm-evals): The overall input and outputs of your LLM system.": "- [端到端评估](/docs/evaluation-end-to-end-llm-evals)：你的 LLM 系统的整体输入与输出。",
 "- [Component-level evaluation](/docs/evaluation-component-level-llm-evals): The individual inner workings of your LLM system.": "- [组件级评估](/docs/evaluation-component-level-llm-evals)：你的 LLM 系统的内部运行细节。",
 "Both can be done using either `deepeval test run` in CI/CD pipelines, or via the `evaluate()` function in your scripts.": "两者都可以在 CI/CD 流水线中用 `deepeval test run` 完成，也可以在脚本中通过 `evaluate()` 函数完成。",
 "Running an LLM evaluation creates a **test run** — a collection of test cases that benchmarks your LLM application at a specific point in time. If you're logged into Confident AI, you'll also receive a fully sharable [LLM testing report](https://www.confident-ai.com/docs/llm-evaluation/dashboards/testing-reports) on the cloud.": "运行一次 LLM 评估会创建一个**测试运行（test run）**——一组在特定时间点为你的 LLM 应用做基准测试的测试用例。如果你登录了 Confident AI，还会在云端得到一份完全可分享的 [LLM 测试报告](https://www.confident-ai.com/docs/llm-evaluation/dashboards/testing-reports)。",
 "`deepeval` offers 30+ evaluation metrics, most of which are evaluated using LLMs (visit the [metrics section](/docs/metrics-introduction#types-of-metrics) to learn why).": "`deepeval` 提供 30+ 个评估指标，其中大多数通过 LLM 来评估（访问[指标板块](/docs/metrics-introduction#types-of-metrics)了解原因）。",
 "You'll need to create a test case to run `deepeval`'s metrics.": "你需要创建一个测试用例来运行 `deepeval` 的指标。",
 "In `deepeval`, a test case represents an [LLM interaction](/docs/evaluation-test-cases#what-is-an-llm-interaction) and allows you to use evaluation metrics you have defined to unit test LLM applications.": "在 `deepeval` 中，测试用例表示一次 [LLM 交互](/docs/evaluation-test-cases#what-is-an-llm-interaction)，让你可以用已定义的评估指标对 LLM 应用做单元测试。",
 "In this example, `input` mimics an user interaction with a RAG-based LLM application, where `actual_output` is the output of your LLM application and `retrieval_context` is the retrieved nodes in your RAG pipeline. Creating a test case allows you to evaluate using `deepeval`'s default metrics:": "在本例中，`input` 模拟用户与基于 RAG 的 LLM 应用的交互，`actual_output` 是你的 LLM 应用的输出，`retrieval_context` 是 RAG 流水线中检索到的节点。创建测试用例后即可使用 `deepeval` 的默认指标进行评估：",
 "A test case **passes** only if every metric that carries a verdict succeeds. Two flags let you control which verdicts count:": "只有当每个携带判定结果的指标都成功时，测试用例才**通过**。有两个开关可以控制哪些判定生效：",
 "Datasets in `deepeval` is a collection of goldens. It provides a centralized interface for you to evaluate a collection of test cases using one or multiple metrics.": "`deepeval` 中的数据集是金标（golden）的集合。它提供一个集中式接口，让你用一条或多条指标对一组测试用例进行评估。",
 "You don't need to create an evaluation dataset to evaluate individual test cases. Visit the [test cases section](/docs/evaluation-test-cases#assert-a-test-case) to learn how to assert individual test cases.": "你不需要创建评估数据集也能评估单个测试用例。访问[测试用例板块](/docs/evaluation-test-cases#assert-a-test-case)了解如何断言单个测试用例。",
 "In `deepeval`, the `Synthesizer` allows you to generate synthetic datasets. This is especially helpful if you don't have production data or you don't have a golden dataset to evaluate with.": "在 `deepeval` 中，`Synthesizer` 允许你生成合成数据集。当你没有生产数据、也没有金标数据集可用于评估时，这尤其有用。",
 "`deepeval`'s `Synthesizer` is highly customizable, and you can learn more about it [here.](/docs/golden-synthesizer)": "`deepeval` 的 `Synthesizer` 高度可定制，你可以在[这里](/docs/golden-synthesizer)进一步了解。",
 "It generates synthetic goldens for you when you have neither production data nor a golden dataset. Until it lands, build your `EvaluationDataset` from goldens you write by hand or [pull one from Confident AI](/docs/evaluation-datasets) — everything else on this page works the same.": "当你既没有生产数据也没有金标数据集时，它会为你生成合成金标。在该功能落地之前，请用你手工编写的金标构建 `EvaluationDataset`，或[从 Confident AI 拉取一个](/docs/evaluation-datasets)——本页其余内容完全一致。",
 "Although `deepeval` integrates with Pytest, we highly recommend you to **AVOID** executing `LLMTestCase`s directly via the `pytest` command to avoid any unexpected errors.": "虽然 `deepeval` 与 Pytest 集成，但我们强烈建议你**避免**直接通过 `pytest` 命令执行 `LLMTestCase`，以避免意外错误。",
 "`deepeval` allows you to run evaluations as if you're using Pytest via our Pytest integration. Simply create a test file:": "`deepeval` 通过我们的 Pytest 集成让你像使用 Pytest 一样运行评估。只需创建一个测试文件：",
 "And run the test file in the CLI using `deepeval test run`:": "然后在 CLI 中用 `deepeval test run` 运行该测试文件：",
 "There are **TWO** mandatory and **ONE** optional parameter when calling the `assert_test()` function:": "调用 `assert_test()` 函数时有**两个**必填参数和**一个**可选参数：",
 "- `test_case`: an `LLMTestCase`": "- `test_case`：一个 `LLMTestCase`",
 "- `metrics`: a list of metrics of type `BaseMetric`": "- `metrics`：类型为 `BaseMetric` 的指标列表",
 "- [Optional] `run_async`: a boolean which when set to `True`, enables concurrent evaluation of all metrics. Defaulted to `True`.": "- [可选] `run_async`：一个布尔值，设为 `True` 时启用所有指标的并发评估。默认为 `True`。",
 "`@pytest.mark.parametrize` is a decorator offered by Pytest. It simply loops through your `EvaluationDataset` to evaluate each test case individually.": "`@pytest.mark.parametrize` 是 Pytest 提供的装饰器。它会遍历你的 `EvaluationDataset`，逐个评估每个测试用例。",
 "`deepeval` runs evaluations as Vitest tests through the `toPass()` matcher. Simply create a test file:": "`deepeval` 通过 `toPass()` 匹配器以 Vitest 测试的形式运行评估。只需创建一个测试文件：",
 "And run the test file in the CLI using `npx deepeval test run`:": "然后在 CLI 中用 `npx deepeval test run` 运行该测试文件：",
 "The test case goes to `expect()`, and `toPass()` takes one argument:": "测试用例传入 `expect()`，`toPass()` 接受一个参数：",
 "- `metrics`: an array of metrics of type `BaseMetric`, run concurrently against the test case": "- `metrics`：类型为 `BaseMetric` 的指标数组，并发地对该测试用例运行",
 "`it.each` is offered by Vitest. It loops through your `EvaluationDataset` to register one test per test case, and `%$` in the title interpolates that case's position in the dataset so each test is named uniquely.": "`it.each` 由 Vitest 提供。它会遍历你的 `EvaluationDataset`，为每个测试用例注册一个测试，标题中的 `%$` 会插入该用例在数据集中的位置，让每个测试都有唯一的名称。",
 "You can find the full documentation on running evals in CI/CD, for both [end-to-end](/docs/evaluation-end-to-end-llm-evals#use-deepeval-test-run-in-cicd-pipelines) and [component-level](/docs/evaluation-component-level-llm-evals#use-deepeval-test-run-in-cicd-pipelines) evaluation by clicking on their respective links.": "你可以在各自的链接中找到在 CI/CD 中运行评估的完整文档，涵盖[端到端](/docs/evaluation-end-to-end-llm-evals#use-deepeval-test-run-in-cicd-pipelines)与[组件级](/docs/evaluation-component-level-llm-evals#use-deepeval-test-run-in-cicd-pipelines)评估。",
 "You can include the command as a step in a `.yaml` file in your CI/CD workflows to run pre-deployment checks on your LLM application.": "你可以把该命令作为步骤加入 CI/CD 工作流的 `.yaml` 文件中，对 LLM 应用做部署前检查。",
 "Alternately, you can use `deepeval`'s `evaluate` function. This approach avoids the CLI (if you're in a notebook environment), and allows for parallel test execution as well.": "或者，你也可以使用 `deepeval` 的 `evaluate` 函数。这种方式可以避开 CLI（如果你在笔记本环境中），并且支持并行测试执行。",
 "There are **TWO** mandatory and **SIX** optional parameters when calling the `evaluate()` function:": "调用 `evaluate()` 函数时有**两个**必填参数和**六个**可选参数：",
 "- `test_cases`: a list of `LLMTestCase`s **OR** `ConversationalTestCase`s, or an `EvaluationDataset`. You cannot evaluate `LLMTestCase`s and `ConversationalTestCase`s in the same test run.": "- `test_cases`：`LLMTestCase` **或** `ConversationalTestCase` 的列表，或一个 `EvaluationDataset`。你不能在同一次测试运行中同时评估 `LLMTestCase` 和 `ConversationalTestCase`。",
 "- `metrics`: a list of metrics of type `BaseMetric`.": "- `metrics`：类型为 `BaseMetric` 的指标列表。",
 "- [Optional] `hyperparameters`: a dict of type `dict[str, Union[str, int, float]]`. You can log any arbitrary hyperparameter associated with this test run to pick the best hyperparameters for your LLM application on Confident AI.": "- [可选] `hyperparameters`：类型为 `dict[str, Union[str, int, float]]` 的字典。你可以记录与本次测试运行相关的任意超参数，以便在 Confident AI 上为你的 LLM 应用挑选最佳超参数。",
 "- [Optional] `identifier`: a string that allows you to better identify your test run on Confident AI.": "- [可选] `identifier`：一个字符串，帮助你在 Confident AI 上更好地识别本次测试运行。",
 "- [Optional] `async_config`: an instance of type `AsyncConfig` that allows you to [customize the degree concurrency](/docs/evaluation-flags-and-configs#async-configs) during evaluation. Defaulted to the default `AsyncConfig` values.": "- [可选] `async_config`：一个 `AsyncConfig` 类型的实例，允许你[自定义评估期间的并发程度](/docs/evaluation-flags-and-configs#async-configs)。默认为默认的 `AsyncConfig` 值。",
 "- [Optional] `display_config`:an instance of type `DisplayConfig` that allows you to [customize what is displayed](/docs/evaluation-flags-and-configs#display-configs) to the console during evaluation. Defaulted to the default `DisplayConfig` values.": "- [可选] `display_config`：一个 `DisplayConfig` 类型的实例，允许你[自定义评估期间在控制台显示的内容](/docs/evaluation-flags-and-configs#display-configs)。默认为默认的 `DisplayConfig` 值。",
 "- [Optional] `error_config`: an instance of type `ErrorConfig` that allows you to [customize how to handle errors](/docs/evaluation-flags-and-configs#error-configs) during evaluation. Defaulted to the default `ErrorConfig` values.": "- [可选] `error_config`：一个 `ErrorConfig` 类型的实例，允许你[自定义评估期间的错误处理方式](/docs/evaluation-flags-and-configs#error-configs)。默认为默认的 `ErrorConfig` 值。",
 "- [Optional] `cache_config`: an instance of type `CacheConfig` that allows you to [customize the caching behavior](/docs/evaluation-flags-and-configs#cache-configs) during evaluation. Defaulted to the default `CacheConfig` values.": "- [可选] `cache_config`：一个 `CacheConfig` 类型的实例，允许你[自定义评估期间的缓存行为](/docs/evaluation-flags-and-configs#cache-configs)。默认为默认的 `CacheConfig` 值。",
 "Alternately, you can use `deepeval`'s `evaluate` function. This approach avoids the CLI, and still runs your metrics concurrently.": "或者，你也可以使用 `deepeval` 的 `evaluate` 函数。这种方式可以避开 CLI，且依然并发运行你的指标。",
 "There are **TWO** mandatory arguments and **FIVE** optional options when calling the `evaluate()` function:": "调用 `evaluate()` 函数时有**两个**必填参数和**五个**可选项：",
 "- `testCases`: an array of `LLMTestCase`s **OR** `ConversationalTestCase`s. You cannot evaluate `LLMTestCase`s and `ConversationalTestCase`s in the same test run.": "- `testCases`：`LLMTestCase` **或** `ConversationalTestCase` 的数组。你不能在同一次测试运行中同时评估 `LLMTestCase` 和 `ConversationalTestCase`。",
 "- `metrics`: an array of metrics of type `BaseMetric`.": "- `metrics`：类型为 `BaseMetric` 的指标数组。",
 "- [Optional] `asyncConfig`: an object that lets you cap concurrency through `maxConcurrent`. Defaulted to `20`.": "- [可选] `asyncConfig`：一个对象，通过 `maxConcurrent` 限制并发上限。默认为 `20`。",
 "- [Optional] `displayConfig`: an object that controls what is displayed to the console during evaluation, and whether the report is also written to a file.": "- [可选] `displayConfig`：一个对象，控制评估期间在控制台显示的内容，以及是否将报告写入文件。",
 "- [Optional] `errorConfig`: an object that controls how errors are handled, through `ignoreErrors` and `skipOnMissingParams`.": "- [可选] `errorConfig`：一个对象，通过 `ignoreErrors` 与 `skipOnMissingParams` 控制错误的处理方式。",
 "- [Optional] `cacheConfig`: an object that controls caching, through `writeCache` and `useCache`.": "- [可选] `cacheConfig`：一个对象，通过 `writeCache` 与 `useCache` 控制缓存。",
 "- [Optional] `official`: a boolean which when set to `true`, marks this as the official test run for the dataset on Confident AI.": "- [可选] `official`：一个布尔值，设为 `true` 时，会在 Confident AI 上把这次标记为该数据集的正式测试运行。",
 "You can find the full documentation on `evaluate()`, for both [end-to-end](/docs/evaluation-end-to-end-llm-evals#use-evaluate-in-python-scripts) and [component-level](/docs/evaluation-component-level-llm-evals#use-evaluate-in-python-scripts) evaluation by clicking on their respective links.": "你可以在各自的链接中找到 `evaluate()` 的完整文档，涵盖[端到端](/docs/evaluation-end-to-end-llm-evals#use-evaluate-in-python-scripts)与[组件级](/docs/evaluation-component-level-llm-evals#use-evaluate-in-python-scripts)评估。",
 "You can also replace `dataset` with a list of test cases, as shown in the [test cases section.](/docs/evaluation-test-cases#evaluate-test-cases-in-bulk)": "你也可以把 `dataset` 替换为测试用例列表，如[测试用例板块](/docs/evaluation-test-cases#evaluate-test-cases-in-bulk)所示。",
 "You can also run metrics on nested components by setting up tracing in `deepeval`, and requires under 10 lines of code:": "你也可以通过在 `deepeval` 中设置追踪来对嵌套组件运行指标，只需不到 10 行代码：",
 "This is very useful especially if you:": "这在以下情况尤其有用：",
 "- Want to run a different set of metrics on different components": "- 想在不同的组件上运行不同的指标集合",
 "- Wish to evaluate multiple components at once": "- 希望一次评估多个组件",
 "- Don't want to rewrite your codebase just to bubble up returned variables to create an `LLMTestCase`": "- 不想为了把返回变量冒泡上来构建 `LLMTestCase` 而重写你的代码库",
 "Framework integrations capture your application's traces and spans so `deepeval` metrics can evaluate complete agent trajectories or individual components without you recreating the execution structure manually.": "框架集成会捕获你应用的追踪与 Span，让 `deepeval` 指标可以评估完整的智能体轨迹或单个组件，而无需你手动重建执行结构。",
 "A [test case](/docs/evaluation-test-cases) (the `input` and `actual_output` to score), one or more [metrics](/docs/metrics-introduction) , and optionally an [evaluation dataset](/docs/evaluation-datasets) to run those metrics across many test cases at once.": "一个[测试用例](/docs/evaluation-test-cases)（要打分的 `input` 与 `actual_output`）、一条或多条[指标](/docs/metrics-introduction)，以及可选的一个[评估数据集](/docs/evaluation-datasets)，用于一次性对大量测试用例运行这些指标。",
 "Use [end-to-end](/docs/evaluation-end-to-end-llm-evals) when you only care about your system's final input and output. Use [component-level](/docs/evaluation-component-level-llm-evals) (via `observe` ) when you need to score individual retrievers, tools, or agents to see exactly where things break.": "当你只关心系统的最终输入与输出时，用[端到端评估](/docs/evaluation-end-to-end-llm-evals)。当你需要对单个检索器、工具或智能体打分、看清问题出在哪里时，用[组件级评估](/docs/evaluation-component-level-llm-evals)（通过 `observe`）。",
 "No. `deepeval` runs entirely locally. The only key you typically need is an LLM provider key such as `OPENAI_API_KEY` for metrics that use an LLM judge. A Confident AI account is optional and only needed to push results to the cloud.": "不需要。`deepeval` 完全在本地运行。你通常唯一需要的密钥是 LLM 提供商的密钥（例如使用 LLM 裁判的指标所需的 `OPENAI_API_KEY`）。Confident AI 账号是可选的，只有想把结果推送到云端时才需要。",
 "Use `deepeval test run` when you want evals gated in CI/CD, and `evaluate()` for scripts where you want parallel execution without the CLI. Both run the same metrics on the same test cases.": "想在 CI/CD 中把关评估时用 `deepeval test run`；在想要并行执行又不想用 CLI 的脚本中用 `evaluate()`。两者对同样的测试用例运行同样的指标。",
 "Most of `deepeval` 's 30+ metrics use an LLM judge with techniques like G-Eval, DAG, and QAG rather than string matching, so a correct answer phrased differently from the `expected_output` can still pass.": "`deepeval` 的 30+ 指标大多使用 LLM 裁判，采用 G-Eval、DAG、QAG 等技术而非字符串匹配，因此表述不同但正确的答案依然可以通过，即使它与 `expected_output` 不完全一致。",
 "Every test run is local-first, but when you're logged into [Confident AI](https://www.confident-ai.com) (the platform built by the `deepeval` team) the same run produces a sharable [testing report](https://www.confident-ai.com/docs/llm-evaluation/dashboards/testing-reports) on the cloud. That gives a team a shared UI to compare runs, track regressions over time, and inspect failing cases — without changing any of your evaluation code. It's optional; local runs work exactly the same with or without it.": "每次测试运行都是本地优先的；但当你登录 [Confident AI](https://www.confident-ai.com)（由 `deepeval` 团队打造的平台）后，同一次运行会在云端生成一份可分享的[测试报告](https://www.confident-ai.com/docs/llm-evaluation/dashboards/testing-reports)。这让团队可以用共享界面比较运行、追踪回归、检查失败用例——而无需改动任何评估代码。它是可选的；无论是否使用，本地运行的行为完全一致。",
 "title: Single-Turn Test Case": "title: 单轮测试用例",
 "A **single-turn test case** is a blueprint provided by `deepeval` to unit test LLM outputs, and **represents a single, atomic unit of interaction** with your LLM app.": "**单轮测试用例**是 `deepeval` 提供的、用于对 LLM 输出做单元测试的蓝图，**表示与你的 LLM 应用的一次单一、原子化的交互单元**。",
 "Throughout this documentation, you should assume the term 'test case' refers to an `LLMTestCase` instead of `MLLMImage` or `ConversationalTestCase`.": "在本文档中，你应当默认「测试用例」一词指的是 `LLMTestCase`，而不是 `MLLMImage` 或 `ConversationalTestCase`。",
 "An `LLMTestCase` is the most prominent type of test case in `deepeval`. It has **NINE** parameters:": "`LLMTestCase` 是 `deepeval` 中最主要的测试用例类型。它有**九个**参数：",
 "Here's an example implementation of an `LLMTestCase`:": "下面是一个 `LLMTestCase` 的示例实现：",
 "Since `deepeval` is an LLM evaluation framework, the **`input` and `actual_output` are always mandatory.** However, this does not mean they are necessarily used for evaluation, and you can also add additional parameters such as the `tools_called` for each `LLMTestCase`.": "由于 `deepeval` 是一个 LLM 评估框架，**`input` 和 `actual_output` 永远是必填的。**不过这并不意味着它们一定会用于评估，你也可以为每个 `LLMTestCase` 添加 `tools_called` 等额外参数。",
 "To get your own sharable testing report with `deepeval`, [sign up to Confident AI](https://app.confident-ai.com), or run `deepeval login` in the CLI:": "要通过 `deepeval` 获得属于你自己的可分享测试报告，请[注册 Confident AI](https://app.confident-ai.com)，或在 CLI 中运行 `deepeval login`：",
 "An **LLM interaction** is any **discrete exchange** of information between **components of your LLM system** — from a full user request to a single internal step. The scope of interaction is arbitrary and is entirely up to you.": "**LLM 交互**是 **LLM 系统的组件之间**任何**离散的信息交换**——可以是一个完整的用户请求，也可以是单个内部步骤。交互的范围是任意的，完全由你决定。",
 "Since an `LLMTestCase` represents a single, atomic unit of interaction in your LLM app, it is important to understand what this means.": "由于 `LLMTestCase` 表示你的 LLM 应用中一次单一、原子化的交互单元，理解这一点很重要。",
 "Let’s take this LLM system as an example:": "以这个 LLM 系统为例：",
 "There are different ways you scope an interaction:": "你可以用不同方式划定交互的范围：",
 "- **Agent-Level:** The entire process initiated by the agent, including the RAG pipeline and web search tool usage": "- **智能体级：**智能体发起的整个过程，包括 RAG 流水线与网络搜索工具的使用",
 "- **RAG Pipeline:** Just the RAG flow — retriever + LLM": "- **RAG 流水线：**仅 RAG 流程 —— 检索器 + LLM",
 "- **Retriever:** Only test whether relevant documents are being retrieved": "  - **检索器：**只测试是否检索到了相关文档",
 "- **LLM:** Focus purely on how well the LLM generates text from the input/context": "  - **LLM：**只关注 LLM 从输入/上下文生成文本的表现",
 "An interaction is where you want to define your `LLMTestCase`. For example, when using RAG-specific metrics like `AnswerRelevancyMetric`, `FaithfulnessMetric`, or `ContextualRelevancyMetric`, the interaction is best scoped at the RAG pipeline level.": "交互就是你想要定义 `LLMTestCase` 的位置。例如，当使用 `AnswerRelevancyMetric`、`FaithfulnessMetric` 或 `ContextualRelevancyMetric` 等 RAG 专用指标时，交互最好划定在 RAG 流水线层级。",
 "In this case:": "在这种情况下：",
 "- `input` should be the user question or text to embed": "- `input` 应该是用户问题或待嵌入的文本",
 "- `retrieval_context` should be the retrieved documents from the retriever": "- `retrieval_context` 应该是检索器检索到的文档",
 "- `actual_output` should be the final response generated by the LLM": "- `actual_output` 应该是 LLM 生成的最终回复",
 "If you would want to evaluate using the `ToolCorrectnessMetric` however, you'll need to create an `LLMTestCase` at the **Agent-Level**, and supply the `tools_called` parameter instead:": "不过，如果你想使用 `ToolCorrectnessMetric` 来评估，就需要在**智能体级**创建 `LLMTestCase`，并改为提供 `tools_called` 参数：",
 "We'll go through the requirements for an `LLMTestCase` before showing how to create an `LLMTestCase` for an interaction.": "在展示如何为一次交互创建 `LLMTestCase` 之前，我们先过一遍 `LLMTestCase` 的各项要求。",
 "For users starting out, scoping the interaction as the overall LLM application will be the easiest way to run evals.": "对刚上手的用户来说，把交互划定为整个 LLM 应用是运行评估最简单的方式。",
 "An `LLMTestCase` in `deepeval` can be used to unit test interactions within your LLM application (which can just be an LLM itself), which includes use cases such as RAG and LLM agents (for individual components, agents within agents, or the agent altogether). It contains the necessary information (`tools_called` for agents, `retrieval_context` for RAG, etc.) to evaluate your LLM application for a given `input`.": "`deepeval` 中的 `LLMTestCase` 可用于对 LLM 应用内部（也可以只是单个 LLM 本身）的交互做单元测试，涵盖 RAG、LLM 智能体（单个组件、智能体中的智能体，或整个智能体）等使用场景。它包含评估你的 LLM 应用在给定 `input` 下表现所需的信息（智能体的 `tools_called`、RAG 的 `retrieval_context` 等）。",
 "An `LLMTestCase` is used for both end-to-end and component-level evaluation:": "`LLMTestCase` 既用于端到端评估，也用于组件级评估：",
 "- [End-to-end:](/docs/evaluation-end-to-end-llm-evals) An `LLMTestCase` represents the inputs and outputs of your \"black-box\" LLM application": "- [端到端：](/docs/evaluation-end-to-end-llm-evals)`LLMTestCase` 表示你的「黑盒」LLM 应用的输入与输出",
 "- [Component-level:](/docs/evaluation-component-level-llm-evals) Many `LLMTestCase`s represents many interactions in different components": "- [组件级：](/docs/evaluation-component-level-llm-evals)多个 `LLMTestCase` 表示不同组件中的多次交互",
 "**Different metrics will require a different combination of `LLMTestCase` parameters, but they all require an `input` and `actual_output`** - regardless of whether they are used for evaluation or not. For example, you won't need `expected_output`, `context`, `tools_called`, and `expected_tools` if you're just measuring answer relevancy, but if you're evaluating hallucination you'll have to provide `context` in order for `deepeval` to know what the **ground truth** is.": "**不同的指标需要 `LLMTestCase` 参数的不同组合，但它们都要求 `input` 和 `actual_output`** ——无论它们是否真正参与评估。例如，如果你只是测量答案相关性，就不需要 `expected_output`、`context`、`tools_called` 和 `expected_tools`；但如果你要评估幻觉，就必须提供 `context`，让 `deepeval` 知道**基准真值**是什么。",
 "With the exception of conversational metrics, which are metrics to evaluate conversations instead of individual LLM responses, you can use any LLM evaluation metric `deepeval` offers to evaluate an `LLMTestCase`.": "对话式指标是用于评估对话而非单个 LLM 回复的指标；除对话式指标外，`deepeval` 提供的任何 LLM 评估指标都可以用来评估一个 `LLMTestCase`。",
 "You cannot use conversational metrics to evaluate an `LLMTestCase`. Conveniently, most metrics in `deepeval` are non-conversational.": "你不能用对话式指标评估 `LLMTestCase`。好消息是，`deepeval` 中的大多数指标都是非对话式的。",
 "Keep reading to learn which parameters in an `LLMTestCase` are required to evaluate different aspects of an LLM applications - ranging from pure LLMs, RAG pipelines, and even LLM agents.": "请继续阅读，了解评估 LLM 应用（从纯 LLM、RAG 流水线到 LLM 智能体）的不同侧面分别需要 `LLMTestCase` 的哪些参数。",
 "The `input` mimics a user interacting with your LLM application. The `input` can contain just text or text with images as well, it is the direct input to your prompt template, and so **SHOULD NOT CONTAIN** your prompt template.": "`input` 模拟用户与你的 LLM 应用交互。`input` 可以只包含文本，也可以包含文本与图片；它是提示词模板的直接输入，因此**不应包含**你的提示词模板本身。",
 "Not all `input`s should include your prompt template, as this is determined by the metric you're using. Furthermore, the `input` should **NEVER** be a json version of the list of messages you are passing into your LLM.": "并非所有 `input` 都要包含提示词模板，这取决于你使用的指标。此外，`input` **绝不能**是你传入 LLM 的消息列表的 JSON 版本。",
 "If you're logged into Confident AI, you can associate hyperparameters such as prompt templates with each test run to easily figure out which prompt template gives the best `actual_output`s for a given `input`:": "如果你登录了 Confident AI，可以把提示词模板等超参数关联到每次测试运行，方便找出对给定 `input` 哪个提示词模板能给出最好的 `actual_output`：",
 "The `actual_output` is an **optional** parameter and represents what your LLM app outputs for a given input. Typically, you would import your LLM application (or parts of it) into your test file, and invoke it at runtime to get the actual output. The `actual_output` can be text or image or both as well depending on what your LLM application outputs.": "`actual_output` 是一个**可选**参数，表示你的 LLM 应用对给定输入的输出。通常，你会把 LLM 应用（或其一部分）导入测试文件，并在运行时调用它来获得实际输出。`actual_output` 可以是文本、图片或两者兼有，取决于你的 LLM 应用输出什么。",
 "The `actual_output` is an optional parameter because some systems (such as RAG retrievers) does not require an LLM output to be evaluated.": "`actual_output` 是可选参数，因为某些系统（例如 RAG 检索器）不需要 LLM 输出即可评估。",
 "You may also choose to evaluate with precomputed `actual_output`s, instead of generating `actual_output`s at evaluation time.": "你也可以选择用预先计算好的 `actual_output` 来评估，而不是在评估时生成 `actual_output`。",
 "The `expected_output` is an **optional** parameter and represents you would want the ideal output to be. Note that this parameter is **optional** depending on the metric you want to evaluate.": "`expected_output` 是一个**可选**参数，表示你希望的理想输出。注意该参数是否**可选**取决于你要评估的指标。",
 "The expected output doesn't have to exactly match the actual output in order for your test case to pass since `deepeval` uses a variety of methods to evaluate non-deterministic LLM outputs. We'll go into more details [in the metrics section.](/docs/metrics-introduction)": "测试用例要通过，`expected_output` 不必与 `actual_output` 完全一致，因为 `deepeval` 使用多种方法评估非确定性的 LLM 输出。我们会在[指标板块](/docs/metrics-introduction)详细展开。",
 "The `context` is an **optional** parameter that represents additional data received by your LLM application as supplementary sources of golden truth. You can view it as the ideal segment of your knowledge base relevant as support information to a specific input. Context is **static** and should not be generated dynamically.": "`context` 是一个**可选**参数，表示你的 LLM 应用收到的、作为金标准真值补充来源的额外数据。你可以把它看作知识库中与特定输入相关的、作为支撑信息的理想片段。上下文是**静态的**，不应动态生成。",
 "Unlike other parameters, a context accepts a list of strings.": "与其他参数不同，context 接受一个字符串列表。",
 "Often times people confuse `expected_output` with `context` since due to their similar level of factual accuracy. However, while both are (or should be) factually correct, `expected_output` also takes aspects like tone and linguistic patterns into account, whereas context is strictly factual.": "人们常常把 `expected_output` 与 `context` 混淆，因为二者的事实准确度相近。然而，虽然两者都（或应当）事实正确，`expected_output` 还会考虑语气、语言风格等因素，而 context 是严格的事实。",
 "The `retrieval_context` is an **optional** parameter that represents your RAG pipeline's retrieval results at runtime. By providing `retrieval_context`, you can determine how well your retriever is performing using `context` as a benchmark.": "`retrieval_context` 是一个**可选**参数，表示你的 RAG 流水线在运行时的检索结果。提供 `retrieval_context` 后，你可以以 `context` 为基准来衡量检索器的表现。",
 "The `retrieval_context` parameter accepts a list of strings or `RetrievedContextData` objects.": "`retrieval_context` 参数接受字符串列表或 `RetrievedContextData` 对象列表。",
 "Remember, `context` is the ideal retrieval results for a given input and typically come from your evaluation dataset, whereas `retrieval_context` is your LLM application's actual retrieval results. So, while they might look similar at times, they are not the same.": "请记住，`context` 是给定输入的理想检索结果，通常来自你的评估数据集；而 `retrieval_context` 是你的 LLM 应用实际的检索结果。因此，尽管它们有时看起来相似，但并不相同。",
 "The `tools_called` parameter is an **optional** parameter that represents the tools your LLM agent actually invoked during execution. By providing `tools_called`, you can evaluate how effectively your LLM agent utilized the tools available to it.": "`tools_called` 参数是一个**可选**参数，表示你的 LLM 智能体在执行期间实际调用的工具。提供 `tools_called` 后，你可以评估你的 LLM 智能体利用可用工具的效果如何。",
 "The `tools_called` parameter accepts a list of `ToolCall` objects.": "`tools_called` 参数接受 `ToolCall` 对象列表。",
 "A `ToolCall` object accepts 1 mandatory and 5 optional parameters:": "`ToolCall` 对象接受 1 个必填参数和 5 个可选参数：",
 "- `name`: a string representing the **name** of the tool.": "- `name`：表示工具**名称**的字符串。",
 "- [Optional] `description`: a string describing the **tool's purpose**.": "- [可选] `description`：描述**工具用途**的字符串。",
 "- [Optional] `reasoning`: A string explaining the **agent's reasoning** to use the tool.": "- [可选] `reasoning`：解释智能体**为何使用该工具**的字符串。",
 "- [Optional] `output`: The tool's **output**, which can be of any data type.": "- [可选] `output`：工具的**输出**，可以是任意数据类型。",
 "- [Optional] `type`: a `ToolCallType` representing **how the tool was called**, either `ToolCallType.FUNCTION` or `ToolCallType.MCP`. Use `ToolCallType.MCP` for tools invoked through an [MCP](/docs/evaluation-mcp) server. Defaulted to `ToolCallType.FUNCTION`.": "- [可选] `type`：表示**工具的调用方式**的 `ToolCallType`，为 `ToolCallType.FUNCTION` 或 `ToolCallType.MCP`。通过 [MCP](/docs/evaluation-mcp) 服务器调用的工具使用 `ToolCallType.MCP`。默认为 `ToolCallType.FUNCTION`。",
 "- [Optional] `input_parameters`: A dictionary with string keys representing the **input parameters** (and respective values) passed into the tool function.": "- [可选] `input_parameters`：以字符串为键的字典，表示传入工具函数的**输入参数**（及对应值）。",
 "`tools_called` and `expected_tools` are LLM test case parameters that are utilized only in **agentic evaluation metrics**. These parameters allow you to assess the [tool usage correctness](/docs/metrics-tool-correctness) of your LLM application and ensure that it meets the expected tool usage standards.": "`tools_called` 与 `expected_tools` 是只在**智能体评估指标**中使用的 LLM 测试用例参数。这些参数让你评估 LLM 应用的[工具使用正确性](/docs/metrics-tool-correctness)，并确保它达到预期的工具使用标准。",
 "The `expected_tools` parameter is an **optional** parameter that represents the tools that ideally should have been used to generate the output. By providing `expected_tools`, you can assess whether your LLM application used the tools you anticipated for optimal performance.": "`expected_tools` 参数是一个**可选**参数，表示理想情况下应当使用哪些工具来生成输出。提供 `expected_tools` 后，你可以评估你的 LLM 应用是否使用了你预期的工具以达到最佳效果。",
 "The `token_cost` is an **optional** parameter and is of type float that allows you to log the cost of a particular LLM interaction for a particular `LLMTestCase`. No metrics use this parameter by default, and it is most useful for either:": "`token_cost` 是一个**可选**参数，类型为浮点数，允许你记录特定 `LLMTestCase` 中某次 LLM 交互的成本。默认没有指标使用该参数，它最有用的场景是：",
 "1. Building custom metrics that relies on `token_cost`": "1. 构建依赖 `token_cost` 的自定义指标",
 "2. Logging `token_cost` on Confident AI": "2. 在 Confident AI 上记录 `token_cost`",
 "The `completion_time` is an **optional** parameter and is similar to the `token_cost` is of type float that allows you to log the time in **SECONDS** it took for a LLM interaction for a particular `LLMTestCase` to complete. No metrics use this parameter by default, and it is most useful for either:": "`completion_time` 是一个**可选**参数，与 `token_cost` 类似，类型为浮点数，允许你记录特定 `LLMTestCase` 中某次 LLM 交互完成所用的**秒数**。默认没有指标使用该参数，它最有用的场景是：",
 "1. Building custom metrics that relies on `completion_time`": "1. 构建依赖 `completion_time` 的自定义指标",
 "2. Logging `completion_time` on Confident AI": "2. 在 Confident AI 上记录 `completion_time`",
 "By default `deepeval` supports passing both text and images inside your test cases using the `MLLMImage` object. The `MLLMImage` class in `deepeval` is used to reference multimodal images in your test cases. It allows you to create test cases using local images, remote URLs and `base64` data.": "默认情况下，`deepeval` 支持通过 `MLLMImage` 对象在测试用例中同时传入文本与图片。`deepeval` 中的 `MLLMImage` 类用于在测试用例中引用多模态图片。它让你可以使用本地图片、远程 URL 和 `base64` 数据创建测试用例。",
 "Multimodal test cases are automatically detected when you include `MLLMImage` objects in your inputs or outputs. You can use them with various multimodal supported metrics like the [RAG metrics](/docs/metrics-answer-relevancy) and [multimodal-specific metrics](/docs/multimodal-metrics-image-coherence).": "当你在输入或输出中包含 `MLLMImage` 对象时，多模态测试用例会被自动识别。你可以将它们与各种支持多模态的指标一起使用，例如 [RAG 指标](/docs/metrics-answer-relevancy)与[多模态专用指标](/docs/multimodal-metrics-image-coherence)。",
 "Here's the data model of the `MLLMImage` in `deepeval`:": "以下是 `deepeval` 中 `MLLMImage` 的数据模型：",
 "All the `MLLMImage` instances are converted to a special `deepeval` slug, (e.g `[DEEPEVAL:IMAGE:uuid]`). This is how your `MLLMImage`s look like in your test cases after you embed them in f-strings:": "所有 `MLLMImage` 实例都会被转换成一个特殊的 `deepeval` 占位符（例如 `[DEEPEVAL:IMAGE:uuid]`）。这就是把 `MLLMImage` 嵌入 f 字符串后它在测试用例中的样子：",
 "This outputs the following:": "它输出如下内容：",
 "Users who'd like to access their images themselves for any ETL can use the `convert_to_multi_modal_array` method to convert your test cases to a list of strings and `MLLMImage` in order. Here's how to use it:": "想自行访问图片做任何 ETL 处理的用户，可以使用 `convert_to_multi_modal_array` 方法把你的测试用例按顺序转换为字符串与 `MLLMImage` 的列表。用法如下：",
 "This will output the following:": "输出如下：",
 "The `[DEEPEVAL:IMAGE:awefv234fvbnhg456]` here is actually the instance of `MLLMImage` you passed inside your test case.": "这里的 `[DEEPEVAL:IMAGE:awefv234fvbnhg456]` 实际上就是你传入测试用例的那个 `MLLMImage` 实例。",
 "This is most useful for test cases you know are noisy — for example ones with borderline scores that flip between passing and failing across runs — that you still want to keep evaluating and tracking without letting them gate deployments. The `flaky` status is also logged on Confident AI so you can monitor how often flaky test cases actually fail.": "这最适合那些你明知有噪声的测试用例——例如分数处于临界、多次运行在通过与失败之间来回摆动，但你仍想继续评估与追踪、又不想让它们阻塞部署的用例。`flaky` 状态也会记录在 Confident AI 上，方便你监控 flaky 测试用例实际的失败频率。",
 "Metrics can also be marked as flaky. A flaky **metric's** failure never decides its test case's pass/fail status, which is why every evaluation requires at least one non-flaky metric with a `threshold`.": "指标也可以被标记为 flaky。flaky **指标**的失败永远不会决定其测试用例的通过/失败状态，因此每次评估至少需要一个带 `threshold` 的非 flaky 指标。",
 "The optional `name` parameter allows you to provide a string identifier to label `LLMTestCase`s and `ConversationalTestCase`s for you to easily search and filter for on Confident AI. This is particularly useful if you're importing test cases from an external datasource.": "可选的 `name` 参数允许你提供一个字符串标识来标记 `LLMTestCase` 与 `ConversationalTestCase`，方便你在 Confident AI 上搜索与过滤。当你从外部数据源导入测试用例时尤其有用。",
 "Alternatively, you can also tag test cases for filtering and searching on Confident AI:": "你也可以为测试用例打标签，以便在 Confident AI 上过滤与搜索：",
 "You can create test cases for three types of evaluation:": "你可以为三种类型的评估创建测试用例：",
 "- [End-to-end](/docs/evaluation-end-to-end-llm-evals) - Treats your LLM app as a black-box, and evaluates the overall system inputs and outputs. Your test case lives at the **system level** and covers the entire application": "- [端到端](/docs/evaluation-end-to-end-llm-evals) - 把你的 LLM 应用当作黑盒，评估整个系统的输入与输出。你的测试用例位于**系统级**，覆盖整个应用",
 "- [Component-level](/docs/evaluation-component-level-llm-evals) - Evaluates individual components within your LLM system using the `@observe` decorator. Your test case lives at the **component level** and focuses on specific parts of your system": "- [组件级](/docs/evaluation-component-level-llm-evals) - 使用 `@observe` 装饰器评估 LLM 系统内的单个组件。你的测试用例位于**组件级**，聚焦系统的特定部分",
 "- One-Off Standalone - Executes individual metrics on single test cases for debugging or custom evaluation pipelines": "- 一次性独立评估 - 在单个测试用例上执行单个指标，用于调试或自定义评估流水线",
 "Click on each of the links to learn how to use test cases for evals.": "点击各个链接了解如何在评估中使用测试用例。",
 "Only `input` and `actual_output` are always mandatory. Every other parameter — `expected_output` , `context` , `retrieval_context` , `tools_called` , and so on — is optional and only needed by the specific metrics you're running.": "只有 `input` 和 `actual_output` 永远是必填的。其他所有参数——`expected_output`、`context`、`retrieval_context`、`tools_called` 等——都是可选的，只有你运行的特定指标需要它们时才提供。",
 "`context` is the ideal, static ground truth for an input (usually from your dataset), while `retrieval_context` is what your RAG pipeline actually retrieved at runtime. Comparing the two is how you measure retriever quality.": "`context` 是一个输入的理想、静态基准真值（通常来自你的数据集），而 `retrieval_context` 是你的 RAG 流水线在运行时实际检索到的内容。比较两者正是衡量检索器质量的方法。",
 "Use an `LLMTestCase` for a single, atomic interaction (one request and response). Use a [ConversationalTestCase](/docs/evaluation-multiturn-test-cases) when you need to evaluate a back-and-forth conversation across multiple turns.": "单次、原子化的交互（一问一答）用 `LLMTestCase`。需要评估跨越多个轮次的往返对话时，用 [ConversationalTestCase](/docs/evaluation-multiturn-test-cases)。",
 "Yes. You can populate `actual_output` (and `retrieval_context` , `tools_called` , etc.) ahead of time and evaluate them later, or generate them dynamically at evaluation time by invoking your app inside your test file.": "可以。你可以提前填充 `actual_output`（以及 `retrieval_context`、`tools_called` 等）稍后评估，也可以在评估时通过在测试文件中调用你的应用动态生成它们。",
 "Yes. Embed `MLLMImage` objects (local files, remote URLs, or base64) into the `input` or `actual_output` and `deepeval` automatically treats it as a multimodal test case for multimodal-supported metrics.": "可以。把 `MLLMImage` 对象（本地文件、远程 URL 或 base64）嵌入 `input` 或 `actual_output`，`deepeval` 会自动将其视为多模态测试用例，供支持多模态的指标使用。",
 "Locally you can label test cases with `name` and `tags` for filtering. If your team wants a shared, cloud-hosted view, those same labels carry over to [Confident AI](https://www.confident-ai.com) , where you can search, filter, and visualize test cases and their results in a UI — useful when importing from external data sources or collaborating across a team. It's optional and your test cases run identically without it.": "在本地，你可以用 `name` 与 `tags` 标记测试用例以便过滤。如果你的团队想要共享的云端视图，这些标签会同步到 [Confident AI](https://www.confident-ai.com)，你可以在界面中搜索、过滤并可视化测试用例及其结果——从外部数据源导入或跨团队协作时很有用。它是可选的；不使用它，你的测试用例运行方式完全不变。",
 "title: Prompts": "title: 提示词",
 "`deepeval` lets you evaluate prompts by associating them with test runs. A `Prompt` in `deepeval` contains the prompt template and model parameters used for generation. By linking a `Prompt` to a test run, you can attribute metric scores to specific prompts, enabling metrics-driven prompt selection and optimization for your LLM application.": "`deepeval` 让你通过把提示词关联到测试运行来评估提示词。`deepeval` 中的 `Prompt` 包含提示词模板与用于生成的模型参数。把 `Prompt` 关联到测试运行后，你可以把指标分数归因到特定提示词，为你的 LLM 应用实现指标驱动的提示词选择与优化。",
 "There are two types of evaluations in `deepeval`:": "`deepeval` 中有两类评估：",
 "- End-to-End Testing": "- 端到端测试",
 "- Component-level Testing": "- 组件级测试",
 "This means you can evaluate prompts **end-to-end** or on the **component-level**.": "也就是说，你可以**端到端**评估提示词，也可以在**组件级**评估提示词。",
 "[End-to-end testing](#end-to-end) is useful when you want to evaluate the prompt's impact on the entire LLM application, since metric scores in end-to-end tests are calculated on the final output. [Component-level testing](#component-level) is useful when you want to evaluate prompts for specific LLM generation processes, since metric scores in component-level tests are calculated on the component-level.": "当你想评估提示词对整个 LLM 应用的影响时，[端到端测试](#end-to-end)很有用，因为端到端测试的指标分数是在最终输出上计算的。当你想评估特定 LLM 生成过程中的提示词时，[组件级测试](#component-level)很有用，因为组件级测试的指标分数是在组件级计算的。",
 "## Evaluating Prompts": "## 评估提示词",
 "### End-to-End": "### 端到端",
 "You can evaluate prompts end-to-end by running the `evaluate` function in Python or `assert_test` in CI/CD pipelines.": "你可以在 Python 中运行 `evaluate` 函数、或在 CI/CD 流水线中运行 `assert_test` 来端到端评估提示词。",
 "<Tab title=\"In Python\">": "<Tab title=\"在 Python 中\">",
 "To evaluate a prompt during end-to-end evaluation, pass your test cases and metrics to the `evaluate` function, and include the prompt object in the `hyperparameters` dictionary with any string key.": "要在端到端评估中评估提示词，把你的测试用例与指标传给 `evaluate` 函数，并用任意字符串键把提示词对象放进 `hyperparameters` 字典。",
 "You can log multiple prompts in the `hyperparameters` dictionary if your LLM application uses multiple prompts.": "如果你的 LLM 应用使用多个提示词，你可以在 `hyperparameters` 字典中记录多个提示词。",
 "<Tab title=\"In CI/CD\">": "<Tab title=\"在 CI/CD 中\">",
 "To evaluate a prompt during end-to-end evaluation in CI/CD pipelines, use the `assert_test` function with your test cases and metrics, and include the prompt object in the hyperparameters dictionary.": "要在 CI/CD 流水线中端到端评估提示词，使用带你的测试用例与指标的 `assert_test` 函数，并把提示词对象放进超参数字典。",
 "Based on the metric scores, you can iterate on different prompts to identify the highest-performing version and optimize your LLM application accordingly.": "基于指标分数，你可以迭代不同的提示词，找出表现最好的版本，并据此优化你的 LLM 应用。",
 "### Component-Level": "### 组件级",
 "`deepeval` also supports component-level prompt evaluation to assess specific LLM generations within your application. To enable this, first [set up tracing](/docs/evaluation-llm-tracing), then call `update_llm_span` with the prompts you want to evaluate for each LLM span. Additionally, supply the metrics you want to use in the `@observe` decorator for each span.": "`deepeval` 也支持组件级提示词评估，以评估应用内特定的 LLM 生成过程。为此，先[设置追踪](/docs/evaluation-llm-tracing)，然后为每个 LLM span 调用 `update_llm_span` 并传入你想评估的提示词。另外，在 `@observe` 装饰器中为每个 span 提供你想使用的指标。",
 "Since `update_llm_span` can only be called inside an LLM span, prompt evaluation is limited to LLM spans only.": "由于 `update_llm_span` 只能在 LLM span 内调用，提示词评估仅限于 LLM span。",
 "Then run the `evals_iterator` to evaluate the prompts configured for each LLM span.": "然后运行 `evals_iterator`，评估为每个 LLM span 配置的提示词。",
 "### Arena": "### 竞技场",
 "You can also evaluate prompts side-by-side using `ArenaGEval` to pick the best-performing prompt for your given criteria. Simply include the prompts in the `hyperparameters` field of each `Contestant`.": "你还可以使用 `ArenaGEval` 并排评估提示词，为给定标准挑选表现最好的提示词。只需把提示词放进每个 `Contestant` 的 `hyperparameters` 字段。",
 "## Creating Prompts": "## 创建提示词",
 "### Loading Prompts": "### 加载提示词",
 "<Tab title=\"From JSON\">": "<Tab title=\"从 JSON\">",
 "When loading prompts from `.json` files, the file name is automatically taken as the alias, if unspecified.": "从 `.json` 文件加载提示词时，如果未指定别名，文件名会自动作为别名。",
 "<summary>Click to see `example.json`</summary>": "  <summary>点击查看 `example.json`</summary>",
 "<Tab title=\"From TXT\">": "<Tab title=\"从 TXT\">",
 "When loading prompts from `.txt` files, the file name is automatically taken as the alias, if unspecified.": "从 `.txt` 文件加载提示词时，如果未指定别名，文件名会自动作为别名。",
 "<summary>Click to see `example.txt`</summary>": "  <summary>点击查看 `example.txt`</summary>",
 "When evaluating prompts, you must call `load` or `pull` before passing the prompt to the `hyperparameters` dictionary for end-to-end evaluation, and before calling `update_llm_span` for component-level evaluations.": "评估提示词时，必须在把提示词传入 `hyperparameters` 字典（端到端评估）之前、以及调用 `update_llm_span`（组件级评估）之前，先调用 `load` 或 `pull`。",
 "### From Scratch": "### 从零创建",
 "You can create a prompt in code by instantiating a `Prompt` object with an `alias`. Supply either a list of messages for a message-based prompt, or a text string for a text-based prompt.": "你可以在代码中通过实例化带 `alias` 的 `Prompt` 对象来创建提示词。基于消息的提示词提供消息列表，基于文本的提示词提供文本字符串。",
 "<Tab title=\"Messages\">": "<Tab title=\"消息式\">",
 "<Tab title=\"Text\">": "<Tab title=\"文本式\">",
 "## Additional Attributes": "## 附加属性",
 "In addition to prompt templates, you can associate model and output settings with a `Prompt`.": "除了提示词模板，你还可以把模型与输出设置关联到一个 `Prompt`。",
 "### Model Settings": "### 模型设置",
 "Model settings include the model provider and name, as well as generation parameters such as temperature:": "模型设置包括模型提供商与名称，以及 temperature 等生成参数：",
 "You can configure the following **nine** model settings for a prompt:": "你可以为提示词配置以下**九**项模型设置：",
 "- `provider`: An `ModelProvider` enum specifying the model provider to use for generation.": "- `provider`：`ModelProvider` 枚举，指定用于生成的模型提供商。",
 "- `name`: The string specifying the model name to use for generation.": "- `name`：字符串，指定用于生成的模型名称。",
 "- `temperature`: A float between 0.0 and 2.0 specifying the randomness of the generated response.": "- `temperature`：0.0 到 2.0 之间的浮点数，指定生成响应的随机性。",
 "- `top_p`: A float between 0.0 and 1.0 specifying the nucleus sampling parameter.": "- `top_p`：0.0 到 1.0 之间的浮点数，指定核采样（nucleus sampling）参数。",
 "- `frequency_penalty`: A float between -2.0 and 2.0 specifying the frequency penalty.": "- `frequency_penalty`：-2.0 到 2.0 之间的浮点数，指定频率惩罚。",
 "- `presence_penalty`: A float between -2.0 and 2.0 specifying the presence penalty.": "- `presence_penalty`：-2.0 到 2.0 之间的浮点数，指定存在惩罚。",
 "- `max_tokens`: An integer specifying the maximum number of tokens to generate.": "- `max_tokens`：整数，指定要生成的最大 token 数。",
 "- `verbosity`: A `Verbosity` enum specifying the response detail level.": "- `verbosity`：`Verbosity` 枚举，指定响应的详细程度。",
 "- `reasoning_effort`: An `ReasoningEffort` enum specifying the thinking depth for reasoning models.": "- `reasoning_effort`：`ReasoningEffort` 枚举，指定推理模型的思考深度。",
 "- `stop_sequences`: A list of strings specifying custom stop tokens.": "- `stop_sequences`：字符串列表，指定自定义停止 token。",
 "### Output Settings": "### 输出设置",
 "The output settings include the output type and optionally the output schema, if the output type is `OutputType.SCHEMA`.": "输出设置包括输出类型，以及（当输出类型为 `OutputType.SCHEMA` 时的）输出 schema。",
 "There are **TWO** output settings you can associate with a prompt:": "你可以为提示词关联**两**项输出设置：",
 "- `output_type`: The string specifying the model to use for generation.": "- `output_type`：字符串，指定用于生成的模型。",
 "- `output_schema`: The schema of type `BaseModel` of the output, if `output_type` is `OutputType.SCHEMA`.": "- `output_schema`：类型为 `BaseModel` 的输出 schema（当 `output_type` 为 `OutputType.SCHEMA` 时）。",
 "### Tools": "### 工具",
 "The tools in a prompt are used to specify the tools your agent has access to, all tools are identified using their name and hence must be unique.": "提示词中的工具用于指定你的智能体可以访问的工具；所有工具都以名称标识，因此必须唯一。",
 "Pass your `Prompt` object into the `hyperparameters` dictionary of `evaluate()` for end-to-end evals, or via `update_llm_span` for component-level evals. `deepeval` then ties each test run's scores to that prompt so you can pick the best one.": "把你的 `Prompt` 对象传入 `evaluate()` 的 `hyperparameters` 字典（端到端评估），或通过 `update_llm_span` 传入（组件级评估）。`deepeval` 随后会把每次测试运行的分数与该提示词绑定，方便你挑选最好的一个。",
 "Yes. You can log multiple prompts under different keys, e.g. `hyperparameters=&#123;\"prompt_1\": prompt_1, \"prompt_2\": prompt_2&#125;` , which is common when your app chains several prompts.": "可以。你可以用不同的键记录多个提示词，例如 `hyperparameters={\"prompt_1\": prompt_1, \"prompt_2\": prompt_2}`，这在你的应用串联多个提示词时很常见。",
 "You can build a `Prompt` from scratch in code, load one from a local `.json` or `.txt` file, or `pull()` a specific version from Confident AI. You must call `load()` or `pull()` before using the prompt in an eval.": "你可以在代码中从零构建 `Prompt`、从本地 `.json` 或 `.txt` 文件加载，或从 Confident AI `pull()` 特定版本。在评估中使用提示词之前必须先调用 `load()` 或 `pull()`。",
 "Yes. A `Prompt` can carry model settings (like temperature and model name), output settings, and a list of `Tool` s your agent has access to, so the full generation configuration is versioned alongside the template.": "可以。`Prompt` 可以携带模型设置（如 temperature 与模型名称）、输出设置，以及你的智能体可访问的 `Tool` 列表，让完整的生成配置与模板一起被版本化管理。",
 "Prompts work entirely locally from files or code. If your team prefers a shared, versioned prompt store, [Confident AI](https://www.confident-ai.com) (the platform from the `deepeval` team) lets you edit and version prompts in a UI and `pull()` a specific version into your evals by alias — so non-engineers can iterate on prompts while your code just references a version. It's optional and file/code-based prompts behave identically.": "提示词完全可以只在本地以文件或代码的形式使用。如果你的团队更想要共享的、带版本管理的提示词仓库，[Confident AI](https://www.confident-ai.com)（`deepeval` 团队的平台）让你在界面中编辑提示词并做版本管理，然后按别名 `pull()` 特定版本进入你的评估——非工程师也能迭代提示词，而代码只需引用一个版本。它是可选的；基于文件/代码的提示词行为完全一致。",
 "title: Datasets": "title: 数据集",
 "In `deepeval`, an evaluation dataset, or just dataset, is a collection of goldens. A golden is a precursor to a test case. At evaluation time, you would first convert all goldens in your dataset to test cases, before running evals on these test cases.": "在 `deepeval` 中，评估数据集（或简称数据集）是金标（golden）的集合。金标是测试用例的前身。在评估时，你会先把数据集中的所有金标转换为测试用例，再对这些测试用例运行评估。",
 "There are two approaches to running evals using datasets in `deepeval`:": "在 `deepeval` 中使用数据集运行评估有两种方式：",
 "1. Using `deepeval test run`": "1. 使用 `deepeval test run`",
 "2. Using `evaluate`": "2. 使用 `evaluate`",
 "Depending on the type of goldens you supply, datasets are either **single-turn** or **mult-turn**. Evaluating a dataset means exactly the same as evaluating your LLM system, because by definition a dataset contains all the information produced by your LLM needed for evaluation.": "根据你提供的金标类型，数据集分为**单轮**或多轮。评估一个数据集与评估你的 LLM 系统含义完全相同，因为按定义，数据集包含了评估所需的、由你的 LLM 产出的全部信息。",
 "What are the best practices for curating an evaluation dataset?": "  评估数据集的整理有哪些最佳实践？",
 "- **Ensure telling test coverage:** Include diverse real-world inputs, varying complexity levels, and edge cases to properly challenge the LLM.": "- **确保有说服力的测试覆盖：**纳入多样的真实输入、不同的复杂度级别与边界场景，真正难住 LLM。",
 "- **Focused, quantitative test cases:** Design with clear scope that enables meaningful performance metrics without being too broad or narrow.": "- **聚焦、可量化的测试用例：**设计上范围清晰，既能产生有意义的性能指标，又不过宽或过窄。",
 "- **Define clear objectives:** Align datasets with specific evaluation goals while avoiding unnecessary fragmentation.": "- **定义清晰的目标：**让数据集与特定评估目标对齐，同时避免不必要的碎片化。",
 "If you don't already have an `EvaluationDataset`, a great starting point is to simply write down the prompts you're currently using to manually eyeball your LLM outputs. You can also do this on Confident AI, which integrates 100% with `deepeval`:": "如果你还没有 `EvaluationDataset`，一个很好的起点是把你目前用来人工检查 LLM 输出的提示词写下来。你也可以在 Confident AI 上这样做，它与 `deepeval` 100% 集成：",
 "<figcaption>Create, edit, and collaborate on goldens in the dataset editor.</figcaption>": "  <figcaption>在数据集编辑器中创建、编辑金标并与团队协作。</figcaption>",
 "Full documentation for datasets on [Confident AI": "数据集的完整文档见 [Confident AI",
 "here.](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens)": "这里](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens)。",
 "## Create A Dataset": "## 创建数据集",
 "An `EvaluationDataset` in `deepeval` is simply a collection of goldens. You can initialize an empty dataset to start with:": "`deepeval` 中的 `EvaluationDataset` 就是金标的集合。你可以先初始化一个空数据集：",
 "A dataset can either be a single-turn one, **or** a multi-turn one (but not both). During initialization supplying your dataset with a list of `Golden`s will make it a single-turn one, whereas supplying it with `ConversationalGolden`s will make it multi-turn:": "数据集要么是单轮的，**要么**是多轮的（不能兼有）。初始化时，给数据集提供 `Golden` 列表会使其成为单轮数据集，提供 `ConversationalGolden` 则会使其成为多轮数据集：",
 "## Run Evals On Dataset": "## 在数据集上运行评估",
 "You run evals on test cases in datasets, which you'll create at evaluation time using the goldens in the same dataset.": "你在数据集中的测试用例上运行评估，这些测试用例会在评估时用同一数据集中的金标创建。",
 "First step is to load in the goldens to your dataset. This example will load datasets from Confident AI, but you can also explore [other options below.](#load-dataset)": "第一步是把金标加载到你的数据集。本例从 Confident AI 加载数据集，你也可以探索[下方的其他选项](#load-dataset)。",
 "Your dataset is either single or multi-turn the moment you pull your dataset.": "你拉取数据集的那一刻，它就已经是单轮或多轮的了。",
 "Once you have your dataset and can see a non-empty list of goldens, you can start generating outputs and **add it back to your dataset** as test cases via the `add_test_case()` method:": "拿到数据集并看到非空的金标列表后，你就可以开始生成输出，并通过 `add_test_case()` 方法**把它们作为测试用例加回你的数据集**：",
 "Lastly, you can run evaluations on the list of test cases in your dataset:": "最后，你可以在数据集中的测试用例列表上运行评估：",
 "And execute the test file:": "然后执行该测试文件：",
 "You can learn more about `assert_test` in [this section.](/docs/evaluation-end-to-end-llm-evals#use-deepeval-test-run-in-cicd-pipelines)": "关于 `assert_test` 的更多内容见[这一节](/docs/evaluation-end-to-end-llm-evals#use-deepeval-test-run-in-cicd-pipelines)。",
 "You can learn more about `toPass()` in [this section.](/docs/evaluation-end-to-end-llm-evals#use-deepeval-test-run-in-cicd-pipelines)": "关于 `toPass()` 的更多内容见[这一节](/docs/evaluation-end-to-end-llm-evals#use-deepeval-test-run-in-cicd-pipelines)。",
 "You can learn more about `evaluate` in [this section.](/docs/evaluation-end-to-end-llm-evals#use-evaluate-in-python-scripts)": "关于 `evaluate` 的更多内容见[这一节](/docs/evaluation-end-to-end-llm-evals#use-evaluate-in-python-scripts)。",
 "## Manage Your Dataset": "## 管理你的数据集",
 "Dataset management is an essential part of your evaluation lifecycle. We recommend Confident AI as the choice for your dataset management workflow as it comes with dozens of collaboration features out of the box, but you can also do it locally as well.": "数据集管理是评估生命周期的重要一环。我们推荐把 Confident AI 作为你的数据集管理工作流之选，它开箱即带数十项协作功能；不过你也完全可以在本地管理。",
 "### Save Dataset": "### 保存数据集",
 "You can store both single-turn and multi-turn datasets with `deepeval`. The single-turn datasets contains a list of `Golden`s and the multi-turn would contain `ConversationalGolden`s instead.": "`deepeval` 既可以存储单轮数据集，也可以存储多轮数据集。单轮数据集包含 `Golden` 列表，多轮数据集则包含 `ConversationalGolden`。",
 "You can save your dataset on the cloud by using the `push` method:": "你可以使用 `push` 方法把数据集保存到云端：",
 "This pushes all goldens in your evaluation dataset to Confident AI. If you're unsure whether your goldens are ready for evaluation, you should set `finalized` to `False` instead:": "这会把评估数据集中的所有金标推送到 Confident AI。如果你不确定金标是否已可评估，应把 `finalized` 设为 `False`：",
 "This means they won't be pulled until you've manually marked them as finalized on the platform. You can learn more on Confident AI's docs [here.](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens)": "这意味着在你于平台上手动把它们标记为已定稿之前，它们不会被拉取。更多内容见 Confident AI 的文档[这里](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens)。",
 "You can also push multi-turn datasets exactly the same way.": "你也可以用完全相同的方式推送多轮数据集。",
 "You can save your dataset locally to a JSON file by using the `save_as()` method:": "你可以使用 `save_as()` 方法把数据集在本地保存为 JSON 文件：",
 "There are **TWO** mandatory and **TWO** optional parameter when calling the `save_as()` method:": "调用 `save_as()` 方法时有**两个**必填参数和**两个**可选参数：",
 "- `file_type`: a string of either `\"csv\"`, `\"json\"`, or `\"jsonl\"` and specifies which file format to save `Golden`s in.": "- `file_type`：字符串，取值为 `\"csv\"`、`\"json\"` 或 `\"jsonl\"`，指定保存 `Golden` 的文件格式。",
 "- `directory`: a string specifying the path of the directory you wish to save `Golden`s at.": "- `directory`：字符串，指定希望保存 `Golden` 的目录路径。",
 "- `file_name`: a string specifying the custom filename for the dataset file. Defaulted to the \"YYYYMMDD_HHMMSS\" format of time now.": "- `file_name`：字符串，指定数据集文件的自定义文件名。默认为当前的 \"YYYYMMDD_HHMMSS\" 时间格式。",
 "- `include_test_cases`: a boolean which when set to `True`, will also save any test cases within your dataset. Defaulted to `False`.": "- `include_test_cases`：布尔值，设为 `True` 时会一并保存数据集中的测试用例。默认为 `False`。",
 "By default the `save_as()` method only saves the `Golden`s within your `EvaluationDataset` to file. If you wish to save test cases as well, set `include_test_cases` to `True`.": "默认情况下，`save_as()` 方法只把 `EvaluationDataset` 中的 `Golden` 保存到文件。如果你还想保存测试用例，把 `include_test_cases` 设为 `True`。",
 "You can save your dataset locally to a JSON file by using the `saveAs()` method:": "你可以使用 `saveAs()` 方法把数据集在本地保存为 JSON 文件：",
 "There are **TWO** mandatory and **TWO** optional parameters when calling the `saveAs()` method:": "调用 `saveAs()` 方法时有**两个**必填参数和**两个**可选参数：",
 "- `fileType`: a string of either `\"csv\"`, `\"json\"`, or `\"jsonl\"` and specifies which file format to save `Golden`s in.": "- `fileType`：字符串，取值为 `\"csv\"`、`\"json\"` 或 `\"jsonl\"`，指定保存 `Golden` 的文件格式。",
 "- `directory`: a string specifying the path of the directory you wish to save `Golden`s at. It is created if it does not exist.": "- `directory`：字符串，指定希望保存 `Golden` 的目录路径。目录不存在时会自动创建。",
 "- `fileName`: a string specifying the custom filename for the dataset file. Defaulted to the \"YYYYMMDD_HHMMSS\" format of time now.": "- `fileName`：字符串，指定数据集文件的自定义文件名。默认为当前的 \"YYYYMMDD_HHMMSS\" 时间格式。",
 "- `includeTestCases`: a boolean which when set to `true`, will also save any test cases within your dataset. Defaulted to `false`.": "- `includeTestCases`：布尔值，设为 `true` 时会一并保存数据集中的测试用例。默认为 `false`。",
 "`saveAs()` returns the full path it wrote to.": "`saveAs()` 返回它写入的完整路径。",
 "By default the `saveAs()` method only saves the `Golden`s within your `EvaluationDataset` to file. If you wish to save test cases as well, set `includeTestCases` to `true`.": "默认情况下，`saveAs()` 方法只把 `EvaluationDataset` 中的 `Golden` 保存到文件。如果你还想保存测试用例，把 `includeTestCases` 设为 `true`。",
 "You can save your dataset locally to a CSV file by using the `save_as()` method:": "你可以使用 `save_as()` 方法把数据集在本地保存为 CSV 文件：",
 "You can save your dataset locally to a CSV file by using the `saveAs()` method:": "你可以使用 `saveAs()` 方法把数据集在本地保存为 CSV 文件：",
 "A CSV holds one column per golden field, with lists such as `context` joined on `|` and objects such as `tools_called` written as JSON.": "CSV 为金标的每个字段保存一列，`context` 这类列表以 `|` 连接，`tools_called` 这类对象以 JSON 写入。",
 "You can save your dataset locally to a JSONL file by using the `save_as()` method:": "你可以使用 `save_as()` 方法把数据集在本地保存为 JSONL 文件：",
 "Each line holds one golden, which makes the file cheap to append to and to stream back in with [`add_goldens_from_jsonl_file()`](#load-dataset).": "每行保存一个金标，这让文件易于追加，也便于用 [`add_goldens_from_jsonl_file()`](#load-dataset) 流式读回。",
 "You can save your dataset locally to a JSONL file by using the `saveAs()` method:": "你可以使用 `saveAs()` 方法把数据集在本地保存为 JSONL 文件：",
 "Each line holds one golden, which makes the file cheap to append to and to stream back in with [`addGoldensFromJSONL()`](#load-dataset).": "每行保存一个金标，这让文件易于追加，也便于用 [`addGoldensFromJSONL()`](#load-dataset) 流式读回。",
 "### Load Dataset": "### 加载数据集",
 "`deepeval` offers support for loading datasets stored in JSON, JSONL, CSV, and hugging face datasets into an `EvaluationDataset` as either test cases or goldens.": "`deepeval` 支持把存储在 JSON、JSONL、CSV 以及 Hugging Face 数据集中的数据加载进 `EvaluationDataset`，可作为测试用例或金标。",
 "`deepeval` offers support for loading datasets stored on Confident AI, or in JSON, JSONL, and CSV files, into an `EvaluationDataset` as either test cases or goldens.": "`deepeval` 支持把存储在 Confident AI、JSON、JSONL 和 CSV 文件中的数据加载进 `EvaluationDataset`，可作为测试用例或金标。",
 "You can loading an existing `EvaluationDataset` you might have generated elsewhere by supplying a `file_path` to your `.json` file as **either test cases or goldens**. Your `.json` file should contain an array of objects (or list of dictionaries).": "通过提供 `.json` 文件的 `file_path`，你可以加载可能在别处生成的现成 `EvaluationDataset`，**既可以作为测试用例，也可以作为金标**。你的 `.json` 文件应包含对象数组（即字典列表）。",
 "If your JSON file has different keys from `deepeval`'s conventional `Golden` or `ConversationalGolden` parameters. You can supply your custom key names in the [function parameters](https://github.com/confident-ai/deepeval/blob/main/deepeval/dataset/dataset.py#L584).": "如果你的 JSON 文件的键与 `deepeval` 约定的 `Golden` 或 `ConversationalGolden` 参数不同，可以在[函数参数](https://github.com/confident-ai/deepeval/blob/main/deepeval/dataset/dataset.py#L584)中提供自定义键名。",
 "You can also add single-turn `LLMTestCase`s to your dataset from a JSON file.": "你也可以从 JSON 文件向数据集添加单轮 `LLMTestCase`。",
 "Loading datasets as goldens are especially helpful if you're looking to generate LLM `actual_output`s at evaluation time. You might find yourself in this situation if you are generating data for testing or using historical data from production.": "以金标形式加载数据集尤其有用，当你打算在评估时生成 LLM 的 `actual_output` 时。如果你在为测试生成数据，或在使用来自生产环境的历史数据，你可能会遇到这种情况。",
 "You can load an existing `EvaluationDataset` you might have generated elsewhere by supplying a `filePath` to your `.json` file as **either test cases or goldens**. Your `.json` file should contain an array of objects.": "通过提供 `.json` 文件的 `filePath`，你可以加载可能在别处生成的现成 `EvaluationDataset`，**既可以作为测试用例，也可以作为金标**。你的 `.json` 文件应包含对象数组。",
 "An object carrying a `scenario` becomes a `ConversationalGolden` and everything else becomes a `Golden`, so a single file cannot mix the two. Keys default to the snake_case names `deepeval` writes to file, with the camelCase spelling of each accepted as a fallback — a dataset saved by either SDK loads without configuration. If your file uses different names, override only the keys that differ:": "带 `scenario` 的对象会成为 `ConversationalGolden`，其余成为 `Golden`，因此单个文件不能混用两者。键默认采用 `deepeval` 写文件时使用的 snake_case 命名，同时接受各自的 camelCase 拼写作为回退——任一 SDK 保存的数据集都无需配置即可加载。如果你的文件使用了不同的名称，只需覆盖不同的那些键：",
 "You can also add single-turn `LLMTestCase`s to your dataset from a JSON file. Every object must have an input and an actual output, since a test case cannot be evaluated without one.": "你也可以从 JSON 文件向数据集添加单轮 `LLMTestCase`。每个对象必须有输入与实际输出，因为测试用例缺少其一便无法评估。",
 "Loading datasets as goldens is especially helpful if you're looking to generate LLM `actualOutput`s at evaluation time. You might find yourself in this situation if you are generating data for testing or using historical data from production.": "以金标形式加载数据集尤其有用，当你打算在评估时生成 LLM 的 `actualOutput` 时。如果你在为测试生成数据，或在使用来自生产环境的历史数据，你可能会遇到这种情况。",
 "You can load existing `Golden`s or `ConversationalGolden`s from a `.jsonl` file by supplying a `file_path`. Each line should contain one JSON object that maps to either a `Golden` or a `ConversationalGolden`.": "通过提供 `file_path`，你可以从 `.jsonl` 文件加载现成的 `Golden` 或 `ConversationalGolden`。每行应包含一个 JSON 对象，对应一个 `Golden` 或 `ConversationalGolden`。",
 "For single-turn goldens, each line can look like:": "对于单轮金标，每行可以形如：",
 "For multi-turn goldens, each line can look like:": "对于多轮金标，每行可以形如：",
 "An `EvaluationDataset` can contain either single-turn or multi-turn goldens, but not both. If a JSONL file mixes `Golden` and `ConversationalGolden` rows, `deepeval` will raise an error.": "一个 `EvaluationDataset` 只能包含单轮或多轮金标中的一种，不能混用。如果 JSONL 文件混有 `Golden` 与 `ConversationalGolden` 行，`deepeval` 会抛出错误。",
 "You can load existing `Golden`s or `ConversationalGolden`s from a `.jsonl` file by supplying a `filePath`. Each line should contain one JSON object that maps to either a `Golden` or a `ConversationalGolden`.": "通过提供 `filePath`，你可以从 `.jsonl` 文件加载现成的 `Golden` 或 `ConversationalGolden`。每行应包含一个 JSON 对象，对应一个 `Golden` 或 `ConversationalGolden`。",
 "Blank lines are skipped, and a line that doesn't parse fails with its line number. A `context` written as one delimited string rather than an array is split on `contextDelimiter`, which defaults to `|`.": "空行会被跳过；无法解析的行会带行号报错。`context` 若写成一个带分隔符的字符串而非数组，会按 `contextDelimiter` 拆分，默认为 `|`。",
 "You can add test cases or goldens into your `EvaluationDataset` by supplying a `file_path` to your `.csv` file. Your `.csv` file should contain rows that can be mapped into `Golden` or `ConversationalGolden` through their column names.": "通过提供 `.csv` 文件的 `file_path`，你可以向 `EvaluationDataset` 添加测试用例或金标。你的 `.csv` 文件应包含能通过列名映射为 `Golden` 或 `ConversationalGolden` 的行。",
 "Remember, parameters such as `context` should be a list of strings, and in the context of CSV files that means a cell holding a list is split on `context_col_delimiter`, which defaults to `|` — the same delimiter [`save_as()`](#save-dataset) joins on, so a saved dataset reloads without any configuration. Supply it only if your file uses something else.": "请记住，`context` 等参数应为字符串列表；在 CSV 文件中，这意味着持有列表的单元格会按 `context_col_delimiter` 拆分，默认为 `|`——与 [`save_as()`](#save-dataset) 连接时使用的分隔符相同，因此保存过的数据集无需任何配置即可重新加载。只有当你的文件使用其他分隔符时才需要提供它。",
 "If your CSV file has different column names from `deepeval`'s conventional `Golden` or `ConversationalGolden` parameters. You can supply your custom column names in the [function parameters](https://github.com/confident-ai/deepeval/blob/main/deepeval/dataset/dataset.py#L433).": "如果你的 CSV 文件的列名与 `deepeval` 约定的 `Golden` 或 `ConversationalGolden` 参数不同，可以在[函数参数](https://github.com/confident-ai/deepeval/blob/main/deepeval/dataset/dataset.py#L433)中提供自定义列名。",
 "You can also add single-turn `LLMTestCase`s to your dataset from a CSV file.": "你也可以从 CSV 文件向数据集添加单轮 `LLMTestCase`。",
 "Since `expected_output`, `context`, `retrieval_context`, `tools_called`, and `expected_tools` are optional parameters for an `LLMTestCase`, these fields are similarly **optional** parameters when adding test cases from an existing dataset. A column your file doesn't have leaves its field as `None` rather than empty, so a metric that requires it still reports it as missing.": "由于 `expected_output`、`context`、`retrieval_context`、`tools_called` 与 `expected_tools` 是 `LLMTestCase` 的可选参数，从现有数据集添加测试用例时这些字段同样是**可选**的。文件中缺少的列会让对应字段保持 `None` 而非空值，需要它的指标仍会报告其缺失。",
 "You can add test cases or goldens into your `EvaluationDataset` by supplying a `filePath` to your `.csv` file. Your `.csv` file should contain rows that can be mapped into `Golden` or `ConversationalGolden` through their column names.": "通过提供 `.csv` 文件的 `filePath`，你可以向 `EvaluationDataset` 添加测试用例或金标。你的 `.csv` 文件应包含能通过列名映射为 `Golden` 或 `ConversationalGolden` 的行。",
 "Remember, parameters such as `context` hold a list of strings, so a cell holding a list is split on `contextDelimiter`, which defaults to `|` — the same delimiter [`saveAs()`](#save-dataset) joins on, so a saved dataset reloads without any configuration. Pass it only if your file uses something else.": "请记住，`context` 等参数持有字符串列表，因此持有列表的单元格会按 `contextDelimiter` 拆分，默认为 `|`——与 [`saveAs()`](#save-dataset) 连接时使用的分隔符相同，因此保存过的数据集无需任何配置即可重新加载。只有当你的文件使用其他分隔符时才需要传入它。",
 "Column names default to `deepeval`'s own, so a file that already uses them needs no configuration. If yours differ, name only the columns that do, through `keys`:": "列名默认采用 `deepeval` 自己的命名，因此已使用这些命名的文件无需任何配置。如果列名不同，通过 `keys` 只需列出不同的那些：",
 "A row with a `scenario` becomes a `ConversationalGolden`, and its `turns` cell is read as JSON. Cells such as `toolsCalled`, `additionalMetadata`, and `customColumnKeyValues` are read as JSON too, which is the shape [`saveAs()`](#save-dataset) writes.": "带 `scenario` 的行会成为 `ConversationalGolden`，其 `turns` 单元格按 JSON 读取。`toolsCalled`、`additionalMetadata`、`customColumnKeyValues` 等单元格也按 JSON 读取，这正是 [`saveAs()`](#save-dataset) 写出的形状。",
 "Since `expectedOutput`, `context`, `retrievalContext`, `toolsCalled`, and `expectedTools` are optional parameters for an `LLMTestCase`, these columns are similarly **optional** when adding test cases from an existing dataset. A column your file doesn't have leaves its field unset rather than empty, so a metric that requires it still reports it as missing.": "由于 `expectedOutput`、`context`、`retrievalContext`、`toolsCalled` 与 `expectedTools` 是 `LLMTestCase` 的可选参数，从现有数据集添加测试用例时这些列同样是**可选**的。文件中缺少的列会让对应字段保持未设置而非空值，需要它的指标仍会报告其缺失。",
 "An `EvaluationDataset` can contain either single-turn or multi-turn goldens, but not both. If a CSV file mixes `Golden` and `ConversationalGolden` rows, `deepeval` will raise an error.": "一个 `EvaluationDataset` 只能包含单轮或多轮金标中的一种，不能混用。如果 CSV 文件混有 `Golden` 与 `ConversationalGolden` 行，`deepeval` 会抛出错误。",
 "## Generate A Dataset": "## 生成数据集",
 "Sometimes, you might not have datasets ready to use, and that's ok. `deepeval` provides two options for both single-turn and multi-turn use cases:": "有时你可能没有现成可用的数据集，这没关系。针对单轮与多轮场景，`deepeval` 提供两个选择：",
 "- `Synthesizer` for generating single-turn goldens": "- `Synthesizer` 用于生成单轮金标",
 "- `ConversationSimulator` for generating `turn`s in a [`ConversationalTestCase`](/docs/evaluation-multiturn-test-cases#conversational-test-case)": "- `ConversationSimulator` 用于在 [`ConversationalTestCase`](/docs/evaluation-multiturn-test-cases#conversational-test-case) 中生成 `turn`",
 "### Synthesizer": "### 合成器",
 "`deepeval` offers anyone the ability to easily generate synthetic datasets from documents locally on your machine. This is especially helpful if you don't have an evaluation dataset prepared beforehand.": "`deepeval` 让任何人都能轻松在你本地机器上从文档生成合成数据集。如果你事先没有准备评估数据集，这尤其有帮助。",
 "In this example, we've used the `generate_goldens_from_docs` method, which is one of the four generation methods offered by `deepeval`'s `Synthesizer`. The four methods include:": "本例使用了 `generate_goldens_from_docs` 方法，它是 `deepeval` 的 `Synthesizer` 提供的四种生成方法之一。这四种方法包括：",
 "- [`generate_goldens_from_docs()`](/docs/synthesizer-generate-from-docs): useful for generating goldens to evaluate your LLM application based on contexts extracted from your knowledge base in the form of documents.": "- [`generate_goldens_from_docs()`](/docs/synthesizer-generate-from-docs)：适合基于从知识库中以文档形式提取的上下文，生成评估你的 LLM 应用的金标。",
 "- [`generate_goldens_from_contexts()`](/docs/synthesizer-generate-from-contexts): useful for generating goldens to evaluate your LLM application based on a list of prepared context.": "- [`generate_goldens_from_contexts()`](/docs/synthesizer-generate-from-contexts)：适合基于一组准备好的上下文，生成评估你的 LLM 应用的金标。",
 "- [`generate_goldens_from_scratch()`](/docs/synthesizer-generate-from-scratch): useful for generating goldens to evaluate your LLM application without relying on contexts from a knowledge base.": "- [`generate_goldens_from_scratch()`](/docs/synthesizer-generate-from-scratch)：适合不依赖知识库上下文，生成评估你的 LLM 应用的金标。",
 "- [`generate_goldens_from_goldens()`](/docs/synthesizer-generate-from-goldens): useful for generating goldens by augmenting a known set of goldens.": "- [`generate_goldens_from_goldens()`](/docs/synthesizer-generate-from-goldens)：适合通过扩增一组已知金标来生成金标。",
 "`deepeval`'s `Synthesizer` uses a series of evolution techniques to complicate and make generated goldens more realistic to human prepared data.": "`deepeval` 的 `Synthesizer` 使用一系列演化技术，让生成的金标更复杂、更接近人工准备的数据。",
 "For more information on how `deepeval`'s `Synthesizer` works, visit the [Golden Synthesizer section.](/docs/golden-synthesizer#how-does-it-work)": "关于 `deepeval` 的 `Synthesizer` 如何工作的更多信息，访问[金标合成器板块](/docs/golden-synthesizer#how-does-it-work)。",
 "It generates synthetic goldens from your documents, contexts, or from scratch, which is what you reach for when you have no dataset prepared beforehand. Until it lands, write your goldens by hand or [pull a dataset from Confident AI](#load-dataset), where synthetic goldens generated elsewhere can be stored and shared.": "它可以从你的文档、上下文或从零生成合成金标——这正是你事先没有准备数据集时的选择。在该功能落地之前，请手工编写金标，或[从 Confident AI 拉取数据集](#load-dataset)，在别处生成的合成金标也可以存储和分享在那里。",
 "While a `Synthesizer` generates goldens, the `ConversationSimulator` works slightly different as it generates `turns` in a `ConversationalTestCase` instead:": "`Synthesizer` 生成金标，而 `ConversationSimulator` 略有不同——它生成的是 `ConversationalTestCase` 中的 `turn`：",
 "You can learn more in the [conversation simulator page.](/docs/conversation-simulator)": "更多内容见[对话模拟器页面](/docs/conversation-simulator)。",
 "## What Are Goldens?": "## 什么是金标？",
 "Goldens represent a more flexible alternative to test cases in the `deepeval`, and **is the preferred way to initialize a dataset**. Unlike test cases, goldens:": "在 `deepeval` 中，金标是测试用例更灵活的替代形式，**是初始化数据集的首选方式**。与测试用例不同，金标：",
 "- Only require `input`/`scenario` to initialize": "- 只需要 `input`/`scenario` 即可初始化",
 "- Store expected results like `expected_output`/`expected_outcome`": "- 存储预期结果，如 `expected_output`/`expected_outcome`",
 "- Serve as templates before becoming fully-formed test cases": "- 作为模板存在，在成为完整的测试用例之前",
 "Goldens excel in development workflows where you need to:": "金标擅长以下开发工作流：",
 "- Evaluate changes across different iterations of your LLM application": "- 评估 LLM 应用不同迭代之间的变更",
 "- Compare performance between model versions": "- 比较不同模型版本之间的表现",
 "- Test with `input`s that haven't yet been processed by your LLM": "- 用尚未被你的 LLM 处理过的 `input` 进行测试",
 "Think of goldens as \"pending test cases\" - they contain all the input data and expected results, but are missing the dynamic elements (`actual_output`, `retrieval_context`, `tools_called`) that will be generated when your LLM processes them.": "把金标想成「待处理的测试用例」——它们包含全部输入数据与预期结果，只缺那些在你的 LLM 处理它们时才会生成的动态元素（`actual_output`、`retrieval_context`、`tools_called`）。",
 "### Data model": "### 数据模型",
 "The golden data model is nearly identical to their single/multi-turn test case counterparts (aka. `LLMTestCase` and `ConversationalTestCase`).": "金标的数据模型与其单轮/多轮测试用例对应物（即 `LLMTestCase` 与 `ConversationalTestCase`）几乎一致。",
 "For single-turn `Golden`s:": "对于单轮 `Golden`：",
 "The `actual_output`, `retrieval_context`, and `tools_called` are meant to be populated dynamically instead of passed directly from a golden to test case at evaluation time.": "`actual_output`、`retrieval_context` 与 `tools_called` 应当在评估时动态填充，而不是从金标直接传给测试用例。",
 "For multi-turn `ConversationalGolden`s:": "对于多轮 `ConversationalGolden`：",
 "`user_description` is deprecated in favor of [`persona`](/docs/conversation-simulator-voice-personas). Goldens and dataset files that still carry it keep loading — the string is promoted to a persona built from those characteristics, and the two stay in sync — but the parameter will be removed in a future release.": "`user_description` 已被弃用，改用 [`persona`](/docs/conversation-simulator-voice-personas)。仍携带该字段的金标与数据集文件可以继续加载——字符串会被提升为由这些特征构建的一个人设，两者保持同步——但该参数将在未来版本中移除。",
 "You can easily add and edit custom columns on [Confident AI.](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens#custom-dataset-columns)": "你可以在 [Confident AI](https://www.confident-ai.com/docs/llm-evaluation/dataset-management/create-goldens#custom-dataset-columns) 上轻松添加和编辑自定义列。",
 "The `turns` parameter should **100%** be generated at evaluation time in your `ConversationalTestCase` instead. However, the `turns` parameter exists in case users want to either:": "`turns` 参数**绝对应该**在评估时在你的 `ConversationalTestCase` 中生成。不过 `turns` 参数的存在是为了让用户可以：",
 "- [Simulate turns](/docs/conversation-simulator) starting from a certain point of a prior conversation that was previously left off": "- 从先前对话的某个中断点开始[模拟轮次](/docs/conversation-simulator)",
 "- Continue from a specific turn when test cases usually fail at the last turn where agents are calling multiple tools": "- 当测试用例通常在最后一轮（智能体连续调用多个工具时）失败的情况下，从特定轮次继续",
 "A golden only needs an `input` (or `scenario` ) plus expected results, and acts as a \"pending test case.\" It becomes a full test case once your LLM app generates the dynamic fields like `actual_output` , `retrieval_context` , and `tools_called` at evaluation time.": "金标只需要 `input`（或 `scenario`）加上预期结果，扮演「待处理的测试用例」。一旦你的 LLM 应用在评估时生成了 `actual_output`、`retrieval_context`、`tools_called` 等动态字段，它就成为完整的测试用例。",
 "Goldens are the preferred way to build a dataset because they're decoupled from any single run. You can re-run the same goldens across model versions and app iterations, generating fresh outputs each time, which makes regression testing and comparisons clean.": "用金标构建数据集是首选，因为它们与任何单次运行解耦。你可以在不同模型版本和应用迭代之间重跑同一批金标，每次生成全新输出，让回归测试与对比干净利落。",
 "**Python**： No. You can generate synthetic goldens with the [Synthesizer](/docs/golden-synthesizer) from documents or from scratch, or simulate multi-turn conversations with the [conversation simulator](/docs/conversation-simulator) . **TypeScript**： No. You can simulate multi-turn conversations with the [conversation simulator](/docs/conversation-simulator) , or write goldens by hand and grow the dataset from there. The `Synthesizer` that generates single-turn goldens for you has not landed in TypeScript yet.": "**Python**：不需要。你可以用[合成器](/docs/golden-synthesizer)从文档或从零生成合成金标，或用[对话模拟器](/docs/conversation-simulator)模拟多轮对话。**TypeScript**：不需要。你可以用[对话模拟器](/docs/conversation-simulator)模拟多轮对话，或手工编写金标并逐步扩充数据集。为你生成单轮金标的 `Synthesizer` 尚未在 TypeScript 中提供。",
 "**Python**： Yes. You can save and load datasets locally, including importing goldens from JSON and JSONL files, and convert them to test cases when you run evals. **TypeScript**： Yes. You can load goldens from JSON, JSONL, and CSV with `addGoldensFromJSON()` , `addGoldensFromJSONL()` , and `addGoldensFromCSV()` , test cases with `addTestCasesFromJSON()` and `addTestCasesFromCSV()` , and write a dataset back to disk with `saveAs()` .": "**Python**：可以。你可以在本地保存和加载数据集，包括从 JSON 与 JSONL 文件导入金标，并在运行评估时把它们转换为测试用例。**TypeScript**：可以。你可以用 `addGoldensFromJSON()`、`addGoldensFromJSONL()` 和 `addGoldensFromCSV()` 加载金标，用 `addTestCasesFromJSON()` 和 `addTestCasesFromCSV()` 加载测试用例，并用 `saveAs()` 把数据集写回磁盘。",
 "Yes. Goldens support `additional_metadata` , `comments` , and `custom_column_key_values` for arbitrary metadata you want to carry through evaluation.": "可以。金标支持 `additional_metadata`、`comments` 与 `custom_column_key_values`，可携带你想在评估中保留的任意元数据。",
 "Datasets are fully usable locally. For teams that want a shared source of truth, [Confident AI](https://www.confident-ai.com) (the platform from the `deepeval` team) lets you store datasets in the cloud, edit goldens and custom columns in a UI, and have non-engineers contribute — then pull the same dataset back into code. It's optional and local datasets behave identically.": "数据集在本地完全可用。对于想要共享唯一事实源的团队，[Confident AI](https://www.confident-ai.com)（`deepeval` 团队的平台）让你把数据集存到云端、在界面中编辑金标与自定义列、让非工程师参与贡献——再把同一数据集拉回代码。它是可选的；本地数据集的行为完全一致。",
 "title: Model Context Protocol (MCP)": "title: 模型上下文协议（MCP）",
 "## Architecture": "## 架构",
 "- **Host** – The AI application that coordinates and manages one or more MCP clients.": "- **宿主（Host）** —— 协调并管理一个或多个 MCP 客户端的 AI 应用。",
 "- **Client** – Maintains a one-to-one connection with a server and retrieves context from it for the host to use.": "- **客户端（Client）** —— 与服务器保持一对一连接，并从服务器检索上下文供宿主使用。",
 "- **Server** – Paired with a single client, providing the context the client passes to the host.": "- **服务器（Server）** —— 与单个客户端配对，提供客户端传递给宿主的上下文。",
 "For example, Claude acts as the MCP host. When Claude connects to an MCP server such as Google Sheets, the Claude runtime instantiates an MCP client that maintains a dedicated connection to that server. When Claude subsequently connects to another MCP server, such as Google Docs, it instantiates an additional MCP client to maintain that second connection. This preserves a one-to-one relationship between MCP clients and MCP servers, with the host (Claude) orchestrating multiple clients.": "例如，Claude 充当 MCP 宿主。当 Claude 连接到诸如 Google Sheets 的 MCP 服务器时，Claude 运行时会实例化一个与该服务器保持专用连接的 MCP 客户端。当 Claude 随后连接到另一个 MCP 服务器（如 Google Docs）时，它会实例化额外的 MCP 客户端来维护这第二个连接。这在 MCP 客户端与 MCP 服务器之间保持一对一关系，由宿主（Claude）编排多个客户端。",
 "## Primitives": "## 原语",
 "`deepeval` adheres to MCP primitives. You'll need to use these primitives to create an `MCPServer` class in `deepeval` before evaluation.": "`deepeval` 遵循 MCP 原语。在评估之前，你需要使用这些原语在 `deepeval` 中创建 `MCPServer` 类。",
 "There are three core primitives that MCP servers can expose:": "MCP 服务器可以暴露三种核心原语：",
 "- **Tools**: Executable functions that LLM apps can invoke to perform actions": "- **工具（Tools）**：LLM 应用可以调用以执行操作的可执行函数",
 "- **Resources**: Data sources that provide contextual information to LLM apps": "- **资源（Resources）**：为 LLM 应用提供上下文信息的数据源",
 "- **Prompts**: Reusable templates that help structure interactions with language models": "- **提示词（Prompts）**：帮助构建与语言模型交互的可复用模板",
 "You can get all three primitives from `mcp`'s `ClientSession`:": "你可以从 `mcp` 的 `ClientSession` 获得全部三种原语：",
 "It is the MCP **server developer's** job to expose these primitives for you to leverage for evaluation. This means that you might not always have control over the MCP server you're interacting with.": "把这些原语暴露出来供你评估是 MCP **服务器开发者**的职责。这意味着与你交互的 MCP 服务器不一定总在你的控制之下。",
 "## MCP Server": "## MCP 服务器",
 "The `MCPServer` class is an abstraction **provided by `deepeval`** to contain information about different MCP servers and the primitives they provide which can be used during evaluations.": "`MCPServer` 类是 **`deepeval` 提供的**抽象，用于承载不同 MCP 服务器及其提供的原语的信息，可在评估期间使用。",
 "Here's how how to create a `MCPServer` instance:": "创建 `MCPServer` 实例的方法如下：",
 "The `MCPServer` accepts **FIVE** parameters:": "`MCPServer` 接受**五个**参数：",
 "- `server_name`: an optional string you can provide to store details about your MCP server.": "- `server_name`：可选字符串，用于记录你的 MCP 服务器的细节。",
 "- [Optional] `transport`: an optional literal that stores on the type of transport your MCP server uses. This information does not affect the evaluation of your MCP test case.": "- [可选] `transport`：可选字面量，记录你的 MCP 服务器使用的传输类型。该信息不影响 MCP 测试用例的评估。",
 "- [Optional] `available_tools`: an optional list of tools that your MCP server enables you to use.": "- [可选] `available_tools`：可选列表，你的 MCP 服务器让你可用的工具。",
 "- [Optional] `available_prompts`: an optional list of prompts that your MCP server enables you to use.": "- [可选] `available_prompts`：可选列表，你的 MCP 服务器让你可用的提示词。",
 "- [Optional] `available_resources`: an optional list of resources that your MCP server enables you to use.": "- [可选] `available_resources`：可选列表，你的 MCP 服务器让你可用的资源。",
 "You need to make sure to provide the `.tools`, `.resources` and `.prompts` from the `list` method's response. They are each of type `Tool`, `Resource` and `Prompt` respectively from `mcp.types` and they are standardized from the official [MCP python sdk](https://github.com/modelcontextprotocol/python-sdk).": "你须确保提供 `list` 方法响应中的 `.tools`、`.resources` 与 `.prompts`。它们分别是来自 `mcp.types` 的 `Tool`、`Resource` 与 `Prompt` 类型，并由官方 [MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk) 标准化。",
 "## MCP At Runtime": "## 运行时的 MCP",
 "During runtime, you'll inevitably be calling your MCP server which will then invoke tools, prompts, and resources. To run evaluation on MCP powered LLM apps, you'll need to format each of these primitives that were called for a given input.": "运行期间，你不可避免地会调用 MCP 服务器，进而调用工具、提示词与资源。要对基于 MCP 的 LLM 应用运行评估，你需要把针对给定输入调用过的每个原语格式化。",
 "Provide a list of `MCPToolCall` objects for every tool your agent invokes during the interaction. The example below shows invoking a tool and constructing the corresponding `MCPToolCall`:": "为智能体在交互期间调用的每个工具提供一个 `MCPToolCall` 对象列表。下面的示例展示调用一个工具并构造对应的 `MCPToolCall`：",
 "The `result` returned by `session.call_tool()` is a `CallToolResult` from `mcp.types`.": "`session.call_tool()` 返回的 `result` 是来自 `mcp.types` 的 `CallToolResult`。",
 "### Resources": "### 资源",
 "Provide a list of `MCPResourceCall` objects for every resource your agent reads. The example below shows reading a resource and constructing the corresponding `MCPResourceCall`:": "为智能体读取的每个资源提供一个 `MCPResourceCall` 对象列表。下面的示例展示读取一个资源并构造对应的 `MCPResourceCall`：",
 "The `result` returned by `session.read_resource()` is a `ReadResourceResult` from `mcp.types`.": "`session.read_resource()` 返回的 `result` 是来自 `mcp.types` 的 `ReadResourceResult`。",
 "### Prompts": "### 提示词",
 "Provide a list of `MCPPromptCall` objects for every prompt your agent retrieves. The example below shows fetching a prompt and constructing the corresponding `MCPPromptCall`:": "为智能体获取的每个提示词提供一个 `MCPPromptCall` 对象列表。下面的示例展示获取一个提示词并构造对应的 `MCPPromptCall`：",
 "The `result` returned by `session.get_prompt()` is a `GetPromptResult` from `mcp.types`.": "`session.get_prompt()` 返回的 `result` 是来自 `mcp.types` 的 `GetPromptResult`。",
 "## Evaluating MCP": "## 评估 MCP",
 "You can evaluate MCPs for both **single and multi-turn** use cases. Evaluating MCP involves 4 steps:": "你可以对**单轮与多轮**两种场景评估 MCP。评估 MCP 包含 4 个步骤：",
 "- Defining an `MCPServer`, and": "- 定义一个 `MCPServer`，以及",
 "- Piping runtime primitives data into `deepeval`": "- 把运行时的原语数据接入 `deepeval`",
 "- Creating a single-turn or multi-turn test case using these data": "- 用这些数据创建单轮或多轮测试用例",
 "- Running MCP metrics on the test cases you've defined": "- 在你定义的测试用例上运行 MCP 指标",
 "### Single-Turn": "### 单轮",
 "The [`LLMTestCase`](/docs/evaluation-test-cases#llm-test-case) is a single-turn test case and accepts the following optional parameters to support MCP evaluations:": "[`LLMTestCase`](/docs/evaluation-test-cases#llm-test-case) 是单轮测试用例，它接受以下可选参数以支持 MCP 评估：",
 "Typically all MCP parameters in a test case is optional. However if you wish to use MCP metrics such as the `MCPUseMetric`, you'll have to provide some of the following:": "通常测试用例中的所有 MCP 参数都是可选的。但如果你想使用 `MCPUseMetric` 等 MCP 指标，则需要提供以下部分内容：",
 "- `mcp_servers` — a list of `MCPServer`s": "- `mcp_servers` —— `MCPServer` 列表",
 "- `mcp_tools_called` — a list of `MCPToolCall` objects that your LLM app has used": "- `mcp_tools_called` —— 你的 LLM 应用使用过的 `MCPToolCall` 对象列表",
 "- `mcp_resources_called` — a list of `MCPResourceCall` objects that your LLM app has used": "- `mcp_resources_called` —— 你的 LLM 应用使用过的 `MCPResourceCall` 对象列表",
 "- `mcp_prompts_called` — a list of `MCPPromptCall` objects that your LLM app has used": "- `mcp_prompts_called` —— 你的 LLM 应用使用过的 `MCPPromptCall` 对象列表",
 "You can learn more about the `MCPUseMetric` [here.](/docs/metrics-mcp-use)": "关于 `MCPUseMetric` 的更多内容见[这里](/docs/metrics-mcp-use)。",
 "### Multi-Turn": "### 多轮",
 "The [`ConversationalTestCase`](/docs/evaluation-multiturn-test-cases#conversational-test-case) accepts an optional parameter called `mcp_server` to add your `MCPServer` instances, which tells `deepeval` how your MCP interactions should be evaluated:": "[`ConversationalTestCase`](/docs/evaluation-multiturn-test-cases#conversational-test-case) 接受名为 `mcp_server` 的可选参数来添加你的 `MCPServer` 实例，告诉 `deepeval` 应当如何评估你的 MCP 交互：",
 "Click here to see how to set MCP primitives for turns at runtime": "  点击查看如何在运行时为轮次设置 MCP 原语",
 "To set primitives at runtime, the `Turn` object accepts optional parameters like `mcp_tools_called`, `mcp_resources_called` and `mcp_prompts_called`, just like in an `LLMTestCase`:": "要在运行时设置原语，`Turn` 对象接受 `mcp_tools_called`、`mcp_resources_called` 与 `mcp_prompts_called` 等可选参数，与 `LLMTestCase` 中一样：",
 "### Shared MCP Servers": "### 共享 MCP 服务器",
 "`mcp_servers` on `evaluate()` is currently available in Python only.": "`evaluate()` 上的 `mcp_servers` 目前仅在 Python 中可用。",
 "If every test case in a run talks to the same MCP servers, you can provide them once to `evaluate()` instead of repeating them on each test case:": "如果一次运行中的每个测试用例都与相同的 MCP 服务器通信，你可以把它们一次性提供给 `evaluate()`，而不必在每个测试用例上重复：",
 "`mcp_servers` on a test case takes precedence, so a test case that defines its own servers is left untouched.": "测试用例上的 `mcp_servers` 优先，因此定义了自己服务器的测试用例不会被改动。",
 "If you built your server with the official MCP Python SDK, pass that object straight in — `deepeval` reads its tools, resources, and prompts for you, so there's no need to construct a `deepeval` `MCPServer` by hand.": "### 使用官方 MCP SDK",
 "Note that the SDK's server class is *also* called `MCPServer`, so alias one of them when you import both:": "注意 SDK 的服务器类*也*叫 `MCPServer`，因此同时导入两者时请给其中一个起别名。",
 "Both forms are accepted anywhere `mcp_servers` is, including on a test case directly, and you can mix them in one list. An official server is converted to a `deepeval` `MCPServer` by calling its `list_tools()`, `list_resources()`, and `list_prompts()`, so `test_case.mcp_servers` always holds `deepeval` `MCPServer`s once evaluation starts.": "两种形式都可以用在任何接受 `mcp_servers` 的地方，包括直接放在测试用例上，并且可以在一个列表中混用。官方服务器会通过调用其 `list_tools()`、`list_resources()` 与 `list_prompts()` 转换为 `deepeval` 的 `MCPServer`，因此评估开始后 `test_case.mcp_servers` 中始终是 `deepeval` 的 `MCPServer`。",
 "Only the SDK's **server** object is supported. A `ClientSession` — what you hold when your app *consumes* someone else's MCP server rather than defining its own — is not accepted yet. For that case, keep building a `deepeval` `MCPServer` and pass the primitives from `session.list_tools()` as shown [above](#mcp-server).": "仅支持 SDK 的**服务器**对象。`ClientSession`——即你的应用*消费*他人 MCP 服务器（而非自己定义）时持有的对象——目前不被接受。对于这种情况，请继续构建 `deepeval` 的 `MCPServer`，并按[上文](#mcp-server)所示传入 `session.list_tools()` 的原语。",
 "Providing `mcp_servers` also tells `deepeval` how to **classify each tool call**. Any `ToolCall` whose `name` matches an `available_tools` entry on one of your MCP servers is tagged as `ToolCallType.MCP`, and everything else stays `ToolCallType.FUNCTION`:": "提供 `mcp_servers` 还会告诉 `deepeval` 如何**分类每个工具调用**。任何 `name` 匹配你某个 MCP 服务器上 `available_tools` 条目的 `ToolCall` 都会被标记为 `ToolCallType.MCP`，其余保持 `ToolCallType.FUNCTION`：",
 "This applies to `tools_called` and `expected_tools` on an `LLMTestCase`, and to `tools_called` on each `Turn` of a `ConversationalTestCase`. The type is included when your test run is sent to Confident AI, where MCP tool calls are labelled separately from regular function calls.": "这适用于 `LLMTestCase` 的 `tools_called` 与 `expected_tools`，以及 `ConversationalTestCase` 每个 `Turn` 上的 `tools_called`。测试运行发送到 Confident AI 时会包含该类型，MCP 工具调用会与普通函数调用分开标注。",
 "Tool calls are matched **by name**. A regular function that shares its name with an MCP tool on one of your servers will be tagged as `ToolCallType.MCP`.": "工具调用按**名称**匹配。与你的某个服务器上的 MCP 工具同名的普通函数也会被标记为 `ToolCallType.MCP`。",
 "The [`ToolCorrectnessMetric`](/docs/metrics-tool-correctness) matches on the type alongside the name, so an `MCP` tool call is not deemed correct against an expected `FUNCTION` tool call of the same name.": "[`ToolCorrectnessMetric`](/docs/metrics-tool-correctness) 在匹配名称的同时也匹配类型，因此 `MCP` 工具调用不会因同名就被视为与预期的 `FUNCTION` 工具调用匹配。",
 "✅ Done. You can now use the [MCP metrics](/docs/metrics-multi-turn-mcp-use) to run evaluations on your MCP based application.": "✅ 完成。你现在可以使用 [MCP 指标](/docs/metrics-multi-turn-mcp-use)对基于 MCP 的应用运行评估了。",
 "It evaluates how your app uses MCP primitives — tools, resources, and prompts — by capturing the calls made to an MCP server and scoring them with MCP-specific metrics, for both single-turn and multi-turn interactions.": "它评估你的应用如何使用 MCP 原语——工具、资源与提示词——方式是捕获对 MCP 服务器的调用，并用 MCP 专用指标打分，单轮与多轮交互皆可。",
 "**Tools** (functions the model can call), **resources** (data the server exposes), and **prompts** (server-provided prompt templates). Each is captured at runtime and formatted into `deepeval` 's test case objects.": "**工具**（模型可以调用的函数）、**资源**（服务器暴露的数据）与**提示词**（服务器提供的提示词模板）。每一种都会在运行时捕获，并格式化成 `deepeval` 的测试用例对象。",
 "For single-turn, populate the MCP call parameters on your `LLMTestCase` . For multi-turn, pass your `MCPServer` instances to a `ConversationalTestCase` via `mcp_servers` , and optionally set per-turn calls like `mcp_tools_called` on each `Turn` .": "单轮：在 `LLMTestCase` 上填充 MCP 调用参数。多轮：把你的 `MCPServer` 实例通过 `mcp_servers` 传入 `ConversationalTestCase`，并可选地在每个 `Turn` 上设置 `mcp_tools_called` 等逐轮调用。",
 "Use the [MCP metrics](/docs/metrics-multi-turn-mcp-use) (such as `MultiTurnMCPMetric` ) to score whether your app called the right tools, resources, and prompts correctly.": "使用 [MCP 指标](/docs/metrics-multi-turn-mcp-use)（如 `MultiTurnMCPMetric`）为你的应用是否正确调用了工具、资源与提示词打分。",
 "MCP evals run locally. For teams that want to inspect which tools, resources, and prompts were invoked across runs, [Confident AI](https://www.confident-ai.com) (the platform from the `deepeval` team) can visualize MCP interactions and their scores in a shared cloud UI — optional, with no change to your evaluation code.": "MCP 评估在本地运行。对于想跨运行检查调用了哪些工具、资源与提示词的团队，[Confident AI](https://www.confident-ai.com)（`deepeval` 团队的平台）可以在共享的云端界面中可视化 MCP 交互及其分数——可选，且不改变你的评估代码。"
}