---
title: Winogrande
---

**Winogrande** 是一个包含 44,000 道二选一问题的数据集，灵感来自用于常识推理的原始 WinoGrad Schema Challenge（WSC）基准，并在规模和难度上均做了提升。

<Info>
了解 WinoGrande 的构建方式见[这里](https://arxiv.org/pdf/1907.10641)。
</Info>

## Arguments

使用 `Winogrande` 基准时有**两个**可选参数：

- [可选] `n_problems`：用于模型评估的题目数量。默认为 1267（所有题目）。
- [可选] `n_shots`：用于少样本学习的示例数量。**默认设为 5**，**不能超过 5**。

## Usage

以下代码在 `Winogrande` 的 10 道题上使用 3-shot CoT 提示评估自定义 `mistral_7b` 模型（[点击这里](/docs/benchmarks-introduction#benchmarking-your-llm)了解如何使用**任意**自定义 LLM）。

```python
from deepeval.benchmarks import Winogrande

# Define benchmark with n_problems and shots
benchmark = Winogrande(
    n_problems=10,
    n_shots=3,
)

# Replace 'mistral_7b' with your own custom model
benchmark.evaluate(model=mistral_7b)
print(benchmark.overall_score)
```

该基准的 `overall_score` 范围为 0 到 1，1 表示完美表现，0 表示全部答错。模型分数基于**精确匹配**，通过确定模型给出精确正确答案（即 'A' 或 'B'）的题目占总题数的比例来计算。

<Tip>
因此，使用更多少样本提示（`n_shots`）可以大幅提升模型以精确正确格式生成答案的稳健性，并提高总分。
</Tip>

## FAQs

<AccordionGroup>
<Accordion title="What does the Winogrande benchmark measure?">
Winogrande 通过 44,000 道二选一问题衡量常识推理，灵感来自原始 WinoGrad Schema Challenge 但在规模和难度上均做了提升。跨基准的打分方式见[基准简介](/docs/benchmarks-introduction)。
</Accordion>
<Accordion title="How is Winogrande scored?">
打分基于**精确匹配**：`overall_score` 是模型给出精确正确答案（即 'A' 或 'B'）的题目占总数的比例。范围为 0 到 1，1 表示完美表现。
</Accordion>
<Accordion title="What is the default n_shots for Winogrande?">
它默认为 `5`，不能超过该数量。使用更多少样本提示可以提升模型以精确正确格式生成答案的稳健性，并提高总分。
</Accordion>
<Accordion title="How many problems does Winogrande evaluate by default?">
默认 `n_problems` 设为 1267，评估所有题目。可以传入较小的 `n_problems` 值来评估更少的题目。
</Accordion>
<Accordion title="What arguments does the Winogrande benchmark accept?">
有两个可选参数：`n_problems`（用于评估的题目数量）与 `n_shots`（少样本学习的示例数量）。
</Accordion>
<Accordion title="How do I run Winogrande on a custom LLM?">
定义基准，然后用你的模型调用 `evaluate()` 并读取 `overall_score`。使用任意自定义 LLM 见[基准测试指南](/docs/benchmarks-introduction#benchmarking-your-llm)。
</Accordion>
</AccordionGroup>
