---
title: Goal Accuracy
sidebarTitle: "目标准确率"
---

**标签**：LLM-as-a-judge · 多轮 · 无参考 · 智能体 · 多模态

目标准确率（goal accuracy）指标是一个多轮智能体指标，评估你的 LLM 智能体**规划并执行计划以完成任务或达成目标**的能力。它是会自我解释的评估，会输出指标分数的理由。

## Required Arguments

要使用 `GoalAccuracyMetric`，你需要在创建 [`ConversationalTestCase`](https://www.deepeval.com/docs/evaluation-multiturn-test-cases) 时提供以下参数：

- `turns`

其计算方式的更多信息见[这里](#how-is-it-calculated)。

## Usage

`GoalAccuracyMetric()` 可用于智能体的[端到端](/docs/evaluation-end-to-end-llm-evals)多轮评估：

<Tabs>

<Tab title="Python">

```python
from deepeval.test_case import Turn, ConversationalTestCase, ToolCall
from deepeval.metrics import GoalAccuracyMetric
from deepeval import evaluate

convo_test_case = ConversationalTestCase(
    turns=[
        Turn(role="...", content="..."), 
        Turn(role="...", content="...", tools_called=[...])
    ],
)
metric = GoalAccuracyMetric(threshold=0.5)

# To run metric as a standalone
# metric.measure(convo_test_case)
# print(metric.score, metric.reason)

evaluate(test_cases=[convo_test_case], metrics=[metric])
```

</Tab>

<Tab title="TypeScript">

```typescript
import { Turn, ConversationalTestCase, ToolCall } from "deepeval/test-case";
import { GoalAccuracyMetric } from "deepeval/metrics";
import { evaluate } from "deepeval";

const convoTestCase = new ConversationalTestCase({
  turns: [
    new Turn({ role: "user", content: "..." }),
    new Turn({ role: "assistant", content: "...", toolsCalled: [new ToolCall({ name: "..." })] }),
  ],
});
const metric = new GoalAccuracyMetric({ threshold: 0.5 });

// To run metric as a standalone
// await metric.measure(convoTestCase);
// console.log(metric.score, metric.reason);

await evaluate([convoTestCase], [metric]);
```

</Tab>

</Tabs>

<Tabs>

<Tab title="Python">

创建 `GoalAccuracyMetric` 时有**七个**可选参数：

</Tab>

<Tab title="TypeScript">

创建 `GoalAccuracyMetric` 时有**六个**可选参数：

</Tab>

</Tabs>

- [Optional] `threshold`: a number representing the minimum passing threshold. Can also be set to `None`（TypeScript 中为 `null`） to run the metric in [score-only mode](/docs/metrics-introduction#metric-thresholds). Defaulted to `0.5`.
- [可选] `model`：字符串，指定使用 OpenAI 的哪款 GPT 模型，**或**类型为 `DeepEvalBaseLLM` 的[任意自定义 LLM 模型](/docs/metrics-introduction#using-a-custom-llm)。默认为 `gpt-5.4`。
- [可选] `include_reason`：布尔值，设为 `True`（TypeScript 中为 `true`）时，会为评估分数附带理由。默认为 `True`（TypeScript 中为 `true`）。
- [可选] `strict_mode`：布尔值，设为 `True`（TypeScript 中为 `true`）时，把指标分数强制为二元：完美为 1，否则为 0。它还会覆盖当前阈值并将其设为 1。默认为 `False`（TypeScript 中为 `false`）。
- [可选] `async_mode`：布尔值，设为 `True` 时，启用[`measure()` 方法内的并发执行](/docs/metrics-introduction#measuring-a-metric-in-async)。默认为 `True`。
  
- [可选] `verbose_mode`：布尔值，设为 `True`（TypeScript 中为 `true`）时，把计算该指标所用的中间步骤打印到控制台，具体见 [How Is It Calculated](#how-is-it-calculated) 一节。默认为 `False`（TypeScript 中为 `false`）。
- [可选] `flaky`：布尔值，设为 `True`（TypeScript 中为 `true`）时，[把指标标记为 flaky](/docs/metrics-introduction#flaky-metrics)。默认为 `False`（TypeScript 中为 `false`）。

### 独立运行

你还可以把 `GoalAccuracyMetric` 作为独立的一次性执行，运行在单个测试用例上：

<Tabs>

<Tab title="Python">

```python
...

metric.measure(convo_test_case)
print(metric.score, metric.reason)
```

</Tab>

<Tab title="TypeScript">

```typescript
...

await metric.measure(convoTestCase);
console.log(metric.score, metric.reason);
```

</Tab>

</Tabs>

<Warning>
这非常适合调试，或者你想构建自己的评估流水线；但你会**失去** `evaluate()` 函数或 `deepeval test run` 提供的种种好处（测试报告）与全部优化（速度、缓存、计算）。
</Warning>

## How Is It Calculated

`GoalAccuracyMetric` 的分数按以下步骤计算：

- 找出你的 LLM 智能体在每次用户-助手交互中采取的**单独目标与步骤**。
- 用评估模型为每个「目标-步骤」对求出**目标准确率分数**。
- 用评估模型为每个「目标-步骤」对求出**计划质量与计划遵循分数**。

$$
\text{Goal Accuracy Score} = \frac{\text{Goal Evaluation Score + Plan Evaluation Score}}{\text{2}}
$$

<Info>
`GoalAccuracyMetric` 从每次交互的用户消息中抽取任务，评估 LLM 智能体为达成计划所采取的步骤，以及它在该交互中完成任务/达成目标的准确程度。
</Info>

## FAQs

<AccordionGroup>
<Accordion title="My agent answered every message but still failed the user's actual goal — will this catch it?">
会。`GoalAccuracyMetric` 会从用户消息中抽取底层任务，并判断智能体的计划与步骤是否真正达成了它。一段对话可能逐轮看起来都有回应，但只要目标从未达成，分数就会很低。
</Accordion>
<Accordion title="What's the difference between the goal score and the plan score?">
最终分数是**目标评估分数**（是否达成目标）与**计划评估分数**（计划质量与遵循度）的平均值。该平均同时反映目标是否达成以及规划的好坏。
</Accordion>
<Accordion title="How does it know the goal if I never pass an expected outcome?">
它从 `"user"` 消息中推断目标，然后评估为满足目标所采取的步骤。你在 `ConversationalTestCase` 上只需提供 `turns`。
</Accordion>
<Accordion title="Does it account for tool calls when scoring the plan?">
会。轮次可以包含 `tools_called`，指标会把工具使用纳入它重建的计划，以及该计划达成目标的程度。
</Accordion>
</AccordionGroup>
